import assert from "node:assert/strict";
import fs from "node:fs";
import { readFile } from "node:fs/promises";
import { readServiceWorkerShellAssets } from "../scripts/service-worker-cache.mjs";

await import("../scripts/check-contracts.mjs");

const html = fs.readFileSync(new URL("../index.html", import.meta.url), "utf8");
const manifest = JSON.parse(fs.readFileSync(new URL("../manifest.webmanifest", import.meta.url), "utf8"));
const release = JSON.parse(fs.readFileSync(new URL("../version.json", import.meta.url), "utf8"));
const graphSchema = JSON.parse(fs.readFileSync(new URL("../schema/graph.schema.json", import.meta.url), "utf8"));
const feedbackSchema = JSON.parse(fs.readFileSync(new URL("../schema/feedback.schema.json", import.meta.url), "utf8"));
const shareSchema = JSON.parse(fs.readFileSync(new URL("../schema/share.schema.json", import.meta.url), "utf8"));
const backupSchema = JSON.parse(fs.readFileSync(new URL("../schema/backup.schema.json", import.meta.url), "utf8"));
const diffSchema = JSON.parse(fs.readFileSync(new URL("../schema/diff.schema.json", import.meta.url), "utf8"));
const evaluationSchema = JSON.parse(fs.readFileSync(new URL("../schema/evaluation.schema.json", import.meta.url), "utf8"));
const evaluationComparisonSchema = JSON.parse(fs.readFileSync(new URL("../schema/evaluation-comparison.schema.json", import.meta.url), "utf8"));
const healthSchema = JSON.parse(fs.readFileSync(new URL("../schema/health.schema.json", import.meta.url), "utf8"));
const jsonLdSchema = JSON.parse(fs.readFileSync(new URL("../schema/jsonld.schema.json", import.meta.url), "utf8"));
const vaultManifestSchema = JSON.parse(fs.readFileSync(new URL("../schema/vault-manifest.schema.json", import.meta.url), "utf8"));
const learningLoopSchema = JSON.parse(fs.readFileSync(new URL("../schema/learning-loop.schema.json", import.meta.url), "utf8"));
const requestSchema = JSON.parse(fs.readFileSync(new URL("../schema/extractor-request.schema.json", import.meta.url), "utf8"));
const app = fs.readFileSync(new URL("../app.js", import.meta.url), "utf8");
const curriculum = fs.readFileSync(new URL("../curriculum.js", import.meta.url), "utf8");
const styles = fs.readFileSync(new URL("../styles.css", import.meta.url), "utf8");
const jsonLdProjection = fs.readFileSync(new URL("../jsonld-projection.js", import.meta.url), "utf8");
const projection = fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8");
const graphStore = fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8");
const rebuildAdapter = fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8");
const graphCore = fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8");
const evaluation = fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8");
const serviceWorker = fs.readFileSync(new URL("../sw.js", import.meta.url), "utf8");
const publicAssets = fs.readFileSync(new URL("../scripts/public-assets.mjs", import.meta.url), "utf8");
const bugTemplate = fs.readFileSync(new URL("../.github/ISSUE_TEMPLATE/bug_report.yml", import.meta.url), "utf8");
const featureTemplate = fs.readFileSync(new URL("../.github/ISSUE_TEMPLATE/feature_request.yml", import.meta.url), "utf8");
const learningNoteTemplate = fs.readFileSync(new URL("../.github/ISSUE_TEMPLATE/learning_note.yml", import.meta.url), "utf8");
const artifactTemplate = fs.readFileSync(new URL("../.github/ISSUE_TEMPLATE/artifact.yml", import.meta.url), "utf8");
const artifacts = fs.readFileSync(new URL("../ARTIFACTS.md", import.meta.url), "utf8");
const artifactSubmission = fs.readFileSync(new URL("../ARTIFACT_SUBMISSION.md", import.meta.url), "utf8");
const security = fs.readFileSync(new URL("../SECURITY.md", import.meta.url), "utf8");
const runbook = fs.readFileSync(new URL("../RUNBOOK.md", import.meta.url), "utf8");
const contributing = fs.readFileSync(new URL("../CONTRIBUTING.md", import.meta.url), "utf8");
const productionStatus = fs.readFileSync(new URL("../PRODUCTION_STATUS.md", import.meta.url), "utf8");
const readme = fs.readFileSync(new URL("../README.md", import.meta.url), "utf8");
const conduct = fs.readFileSync(new URL("../CODE_OF_CONDUCT.md", import.meta.url), "utf8");
const notesIndex = fs.readFileSync(new URL("../notes/README.md", import.meta.url), "utf8");
const llms = fs.readFileSync(new URL("../llms.txt", import.meta.url), "utf8");
const robots = fs.readFileSync(new URL("../robots.txt", import.meta.url), "utf8");
const server = fs.readFileSync(new URL("../server.mjs", import.meta.url), "utf8");
const publicOrigin = fs.readFileSync(new URL("../scripts/public-origin.mjs", import.meta.url), "utf8");
const dockerfile = fs.readFileSync(new URL("../Dockerfile", import.meta.url), "utf8");
const dockerignore = fs.readFileSync(new URL("../.dockerignore", import.meta.url), "utf8");
const gitignore = fs.readFileSync(new URL("../.gitignore", import.meta.url), "utf8");
const notFound = fs.readFileSync(new URL("../404.html", import.meta.url), "utf8");
const notesDirectory = new URL("../notes/", import.meta.url);
const dependabot = fs.readFileSync(new URL("../.github/dependabot.yml", import.meta.url), "utf8");
const codeowners = fs.readFileSync(new URL("../.github/CODEOWNERS", import.meta.url), "utf8");
const codeqlWorkflow = fs.readFileSync(new URL("../.github/workflows/codeql.yml", import.meta.url), "utf8");
const scorecardWorkflow = fs.readFileSync(new URL("../.github/workflows/scorecard.yml", import.meta.url), "utf8");
const packageManifest = JSON.parse(fs.readFileSync(new URL("../package.json", import.meta.url), "utf8"));
const packageLock = JSON.parse(fs.readFileSync(new URL("../package-lock.json", import.meta.url), "utf8"));
const notePage = fs.readFileSync(new URL("../scripts/note-page.mjs", import.meta.url), "utf8");
const testRunner = fs.readFileSync(new URL("../scripts/run-tests.mjs", import.meta.url), "utf8");
const serverSmoke = fs.readFileSync(new URL("../scripts/smoke-server.mjs", import.meta.url), "utf8");
const containerSmoke = fs.readFileSync(new URL("../scripts/smoke-container.mjs", import.meta.url), "utf8");
const loadServer = fs.readFileSync(new URL("../scripts/load-server.mjs", import.meta.url), "utf8");
const browserSmoke = fs.readFileSync(new URL("../scripts/browser-smoke.mjs", import.meta.url), "utf8");
const browserDiagnostics = fs.readFileSync(new URL("../scripts/browser-diagnostics.mjs", import.meta.url), "utf8");

const htmlIds = [...html.matchAll(/\bid="([^"]+)"/g)].map((match) => match[1]);
const duplicateHtmlIds = [...new Set(htmlIds.filter((id, index) => htmlIds.indexOf(id) !== index))];
assert.equal(duplicateHtmlIds.length, 0, `index.html contains duplicate IDs: ${duplicateHtmlIds.join(", ")}`);
const htmlIdSet = new Set(htmlIds);
for (const match of html.matchAll(/\b(?:aria-(?:controls|describedby|labelledby|owns|activedescendant))="([^"]+)"/g)) {
  for (const referencedId of match[1].split(/\s+/).filter(Boolean)) {
    assert(htmlIdSet.has(referencedId), `ARIA reference points to a missing element: ${referencedId}`);
  }
}
for (const id of ["workbench", "try-sample", "app-error", "app-error-message", "download-error-backup", "reload-app", "app-update", "reload-update", "document-file", "document-uri", "ingest-document", "rebuild-sources", "extractor-endpoint", "cancel-extraction", "retry-build", "clear-graph", "forget-local-data", "undo-graph", "graph-search", "manual-node-label", "add-manual-node", "manual-edge-source", "manual-edge-target", "add-manual-edge", "graph-canvas", "node-list", "relation-list", "source-list", "inspector-panel", "graph-health", "review-queue", "revision-diff-preview", "download-markdown", "copy-markdown", "copy-redacted-markdown", "share-redacted-markdown", "share-redacted-link", "download-redacted-markdown", "download-redacted-html", "share-redacted-html", "download-vault", "download-canvas", "download-redacted-canvas", "download-redacted-vault", "download-json", "download-health", "download-redacted", "download-backup", "download-feedback", "download-compact-feedback", "download-diff", "release-version"]) {
  assert(html.includes(`id="${id}"`), `missing required element: ${id}`);
}
assert(app.includes("function downloadRuntimeRecovery") && app.includes("readRecovery()") && app.includes("readHistoryRecovery()") && app.includes("download-error-backup"), "runtime error recovery should preserve raw graph/history snapshots or offer a full backup before reload");
assert((app.match(/error instanceof Error \? error\.message/g) || []).length === 1 && app.includes("boundedOperationDiagnostic(error,") && app.includes("MAX_OPERATION_DIAGNOSTIC_CHARS"), "browser-facing error surfaces should use the bounded diagnostic normalizer instead of rendering unbounded exception messages");
const copyHelperStart = app.indexOf("async function copyText");
const copyHelperEnd = app.indexOf("\n\nconst browserStorage", copyHelperStart);
const copyHelper = copyHelperStart >= 0 && copyHelperEnd > copyHelperStart ? app.slice(copyHelperStart, copyHelperEnd) : "";
assert(copyHelper.includes("document.execCommand(\"copy\")") && (app.slice(0, copyHelperStart).includes("navigator.clipboard") === false) && (app.slice(copyHelperEnd).includes("navigator.clipboard") === false), "copy and share actions should share one Clipboard API fallback boundary");
assert(html.includes('id="inspector-panel" tabindex="-1" aria-live="polite"') && app.includes('document.querySelector("#inspector-panel").focus()'), "review navigation should move focus to the inspector");
assert(html.includes('id="graph-health" role="status" aria-live="polite" aria-label="Graph health"') && html.includes('id="review-queue" role="region" aria-live="polite" aria-label="Review queue"'), "live graph diagnostics and review queue should have accessible semantics");
for (const match of html.matchAll(/<label[^>]+for="([^"]+)"/g)) {
  assert(html.includes(`id="${match[1]}"`), `label target is missing: ${match[1]}`);
}
for (const match of html.matchAll(/<button\b[^>]*>/g)) {
  assert(/\btype="button"/.test(match[0]), `static button should declare type=button: ${match[0]}`);
}
for (const match of html.matchAll(/<([a-z0-9-]+)\b([^>]*\baria-label="[^"]+"[^>]*)>/gi)) {
  const tag = match[1].toLowerCase();
  const attrs = match[2];
  const semanticTags = new Set(["a", "button", "dialog", "footer", "header", "img", "input", "main", "nav", "section", "select", "svg", "textarea"]);
  assert(semanticTags.has(tag) || /\brole="[^"]+"/.test(attrs), `ARIA-labeled generic element should declare a role: <${tag}>`);
}
for (const source of [html, app, notePage]) {
  for (const match of source.matchAll(/<a\b([^>]*\btarget="_blank"[^>]*)>/gi)) {
    assert(/\brel="[^"]*\bnoopener\b[^"]*"/i.test(match[1]), `new-tab link must declare rel=noopener: ${match[0]}`);
    assert(/\brel="[^"]*\bnoreferrer\b[^"]*"/i.test(match[1]), `new-tab link must declare rel=noreferrer: ${match[0]}`);
  }
}
assert(/id="document-file"[^>]*multiple/.test(html), "document input should support batches");
assert(/id="document-title"[^>]*maxlength="200"/.test(html), "document titles should match the request contract bound");
assert(/id="extractor-endpoint"[^>]*maxlength="2048"/.test(html), "extractor endpoint input should have an explicit storage and request bound");
assert(html.includes('id="file-queue"'), "file queue status should be visible");
for (const match of app.matchAll(/<button\b[^>]*>/g)) {
  assert(/\btype="button"/.test(match[0]), `rendered app button should declare type=button: ${match[0]}`);
}
assert(!app.match(/<button\b[^>]*>\s*<div\b/), "rendered app buttons should not contain block-level div children");
for (const asset of ["./index.html", "./styles.css", "./app.js", "./curriculum.js", "./graph-core.js", "./graph-store.js", "./extractor-adapter.js", "./projection-adapter.js", "./jsonld-projection.js", "./storage-adapter.js", "./evaluation.js", "./manifest.webmanifest", "./icon.svg", "./icon-192.png", "./icon-512.png", "./social-card.svg", "./social-card.png", "./version.json", "./schema/graph.schema.json", "./schema/feedback.schema.json", "./schema/share.schema.json", "./schema/backup.schema.json", "./schema/diff.schema.json", "./schema/extractor-request.schema.json", "./schema/evaluation.schema.json", "./schema/evaluation-comparison.schema.json", "./schema/health.schema.json", "./schema/jsonld.schema.json"]) {
  assert(serviceWorker.includes(asset), `service worker missing shell asset: ${asset}`);
}
assert.equal(manifest.name, "LLM Field Notes");
assert.equal(release.version, packageManifest.version, "the public release manifest should match package.json");
assert.equal(packageManifest.license, "CC-BY-4.0", "package metadata should declare the published Creative Commons license");
assert.equal(packageManifest.scripts.test, "npm run runtime:check && node scripts/run-tests.mjs", "npm test should enforce the runtime and execute the behavioral test runner");
assert.equal(packageManifest.scripts["browser:smoke"], "npm run runtime:check && node scripts/browser-smoke.mjs", "browser smoke should enforce the runtime before launching a browser");
assert.equal(packageManifest.scripts["assets:generate"], "node scripts/generate-raster-assets.mjs", "raster assets should have a reproducible generator command");
assert(codeowners.includes("* @humblemat810")
  && codeowners.includes("/.github/workflows/ @humblemat810")
  && codeowners.includes("/Dockerfile @humblemat810")
  && codeowners.includes("/server.mjs @humblemat810")
  && codeowners.includes("/SECURITY.md @humblemat810")
  && codeowners.includes("/schema/ @humblemat810"), "production-sensitive workflow, runtime, security, and graph-contract paths should have explicit CODEOWNERS review coverage");
assert(fs.existsSync(new URL("../scripts/generate-raster-assets.mjs", import.meta.url))
  && fs.readFileSync(new URL("../scripts/generate-raster-assets.mjs", import.meta.url), "utf8").includes("social-card.svg")
  && fs.readFileSync(new URL("../scripts/generate-raster-assets.mjs", import.meta.url), "utf8").includes("icon-192.png")
  && fs.readFileSync(new URL("../scripts/generate-raster-assets.mjs", import.meta.url), "utf8").includes("fileURLToPath")
  && fs.readFileSync(new URL("../scripts/generate-raster-assets.mjs", import.meta.url), "utf8").includes("page.screenshot"), "published raster assets should be reproducible from their vector sources");
assert(testRunner.includes("collectJavaScriptFiles") && testRunner.includes("IGNORED_DIRECTORIES") && testRunner.includes('readdirSync("tests")') && testRunner.includes('.filter((file) => file.endsWith(".mjs"))') && testRunner.includes('spawnSync(process.execPath') && testRunner.includes('["scripts/check-release.mjs"]'), "the test runner should discover all JavaScript syntax and behavioral test modules and exercise release contracts in isolated Node processes");
assert.equal(release.channel, "unreleased", "the current release manifest should declare its release channel");
assert(release.date <= new Date().toISOString().slice(0, 10), "the public release manifest should not be future-dated");
assert(app.includes("version.json") && app.includes("release-version") && app.includes("releaseInfo.revision.slice(0, 12)") && app.includes("value.revision.toLowerCase()"), "the public footer should use the release manifest and expose a canonical bounded source revision when available");
assert(app.includes("llm-field-notes-extractor-endpoint") && app.includes("validateExtractorEndpoint") && app.includes("same-origin extractor endpoint"), "the optional extractor configuration should persist only after same-origin validation");
assert(html.includes('id="release-version">release metadata unavailable</span>'), "the public footer should use a neutral static release fallback");
assert(html.includes('id="extractor-mode">LOCAL</span>') && app.includes('extractorMode.textContent = extractorState') && app.includes('extractorState = "INVALID"') && app.includes("no document will be sent remotely until it is corrected"), "the ingest mode indicator should distinguish local, model, and invalid endpoint states without overstating network behavior");
assert(html.includes('<meta name="repository-url" content="https://github.com/humblemat810/llm-wiki"')
  && app.includes("buildGraphCorrectionUrl")
  && app.includes("buildGraphCorrectionTemplate")
  && app.includes("data-copy-graph-correction")
  && app.includes("safeMarkdownLabel(item.label)")
  && app.includes("Privacy-safe correction template copied")
  && app.includes("Do not paste private source text")
  && app.includes('meta[name="repository-url"]'), "the workbench should expose a fork-aware, privacy-safe graph-correction path and copyable item-specific correction template from inspected items");
assert(html.includes('id="privacy-live" role="status" aria-live="polite"') && app.includes("privacyLive.dataset.state !== extractorState"), "privacy-boundary changes should be announced once per state transition without repeatedly voicing endpoint keystrokes");
assert(app.includes('extractorMode.parentElement?.setAttribute("data-mode", extractorState)') && styles.includes('.live-label[data-mode="INVALID"]'), "visual extractor mode styling should distinguish model and invalid states");
assert(html.includes('href="artifacts.html"'), "the landing page should expose the community artifact gallery");
assert(html.includes('href="https://github.com/humblemat810/llm-wiki/issues/new?template=learning_note.yml"')
  && html.includes('href="https://github.com/humblemat810/llm-wiki/issues/new?template=artifact.yml"')
  && html.includes('href="https://github.com/humblemat810/llm-wiki/issues/new?template=graph_correction.yml"')
  && html.includes('href="https://github.com/humblemat810/llm-wiki/fork"')
  && html.includes('target="_blank" rel="noopener noreferrer"'), "the landing page should offer direct, safe contribution issue-form links");
assert((html.match(/<a class="brand" href="\.\/" aria-label="LLM Field Notes home">/g) || []).length === 2, "landing-page brand links should resolve to the stable canonical home path");
assert(fs.existsSync(new URL("../artifacts.html", import.meta.url)) && publicAssets.includes('"artifacts.html"') && serviceWorker.includes("./artifacts.html"), "the public artifact gallery should be a deployed and offline-safe page");
const artifactHtml = fs.readFileSync(new URL("../artifacts.html", import.meta.url), "utf8");
assert(artifactHtml.includes('href="https://github.com/humblemat810/llm-wiki/issues/new?template=artifact.yml"')
  && artifactHtml.includes('href="https://github.com/humblemat810/llm-wiki/issues/new?template=graph_correction.yml"')
  && artifactHtml.includes('href="https://github.com/humblemat810/llm-wiki/fork"')
  && (artifactHtml.match(/target="_blank" rel="noopener noreferrer"/g) || []).length >= 2, "the artifact gallery should offer direct, safely opened artifact issue-form links");
const graphCorrectionTemplate = await readFile(new URL("../.github/ISSUE_TEMPLATE/graph_correction.yml", import.meta.url), "utf8");
assert(graphCorrectionTemplate.includes("name: Correct a graph representation")
  && graphCorrectionTemplate.includes("id: proposed_change")
  && graphCorrectionTemplate.includes("id: evidence")
  && graphCorrectionTemplate.includes("id: privacy")
  && graphCorrectionTemplate.includes("I removed private source text"), "the graph-correction issue form should capture actionable, privacy-safe representation feedback");
const publicRepository = await readFile(new URL("../scripts/public-repository.mjs", import.meta.url), "utf8");
assert(publicRepository.includes("DEFAULT_PUBLIC_REPOSITORY") && publicRepository.includes("credential-free GitHub HTTPS repository URL") && publicRepository.includes('url.protocol !== "https:"'), "public repository configuration should default safely and reject unsafe repository URLs");
assert(contributing.includes("Correct a graph representation") && contributing.includes("report correction") && contributing.includes("serializing source text") && contributing.includes("local graph state"), "contribution guidance should explain the privacy-safe graph-correction workflow");
assert(html.includes('href="artifacts.html#learning-loop"') && artifactHtml.includes('id="learning-loop"'), "the landing page should provide a direct link to the self-improvement walkthrough");
assert(artifactHtml.includes('property="og:title"') && artifactHtml.includes('property="og:image" content="social-card.png"') && artifactHtml.includes('property="og:image:type" content="image/png"') && artifactHtml.includes('name="twitter:card" content="summary_large_image"'), "the public artifact gallery should carry raster share-preview metadata");
const artifactStructuredData = JSON.parse(artifactHtml.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/)?.[1] || "{}");
const artifactItems = artifactStructuredData.mainEntity?.itemListElement;
assert(artifactStructuredData["@type"] === "CollectionPage" && artifactStructuredData.mainEntity?.["@type"] === "ItemList" && Array.isArray(artifactItems), "the artifact gallery should carry parseable structured discovery metadata");
assert(fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("renderOriginAwareArtifactPage") && server.includes("renderOriginAwareArtifactPage") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("() =>") && server.includes("() =>"), "artifact-gallery social metadata should stay absolute and literal-safe on configured Node and Pages origins");
assert(artifactHtml.includes("script-src 'none'") && server.includes("const artifactPageCsp") && server.includes('relative === "artifacts.html"'), "the script-free artifact gallery should have matching document and response-level CSP enforcement");
assert(artifactHtml.includes('"@type":"CollectionPage"') && artifactHtml.includes('"@type":"ItemList"') && artifactHtml.includes('"numberOfItems":21') && artifactHtml.includes('"url":"experiments/learning-loop.mjs"') && artifactHtml.includes('"url":"experiments/verify-graph.mjs"') && artifactHtml.includes('"url":"experiments/verify-diff.mjs"') && artifactHtml.includes('"url":"experiments/verify-backup.mjs"') && artifactHtml.includes('"url":"scripts/verify-canvas.mjs"') && artifactHtml.includes('"url":"scripts/verify-service-health.mjs"') && artifactHtml.includes('"url":"scripts/verify-share.mjs"') && artifactHtml.includes('"url":"examples/sample-graph.json"') && artifactHtml.includes('"url":"sample-graph.html"') && artifactHtml.includes("<code>node experiments/learning-loop.mjs</code>") && artifactHtml.includes("<code>node experiments/verify-graph.mjs --help</code>") && artifactHtml.includes("<code>node experiments/verify-diff.mjs --help</code>") && artifactHtml.includes("<code>node experiments/verify-backup.mjs --help</code>") && artifactHtml.includes("<code>node scripts/verify-canvas.mjs examples/sample-graph.canvas</code>") && artifactHtml.includes("<code>npm run verify:service-health</code>") && artifactHtml.includes("<code>npm run verify:share -- examples/sample-share.json</code>") && artifactHtml.includes("<code>npm run health -- examples/sample-graph.json</code>") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes('replace(/"url":"(experiments\\/[^"]+)"/g'), server.includes('replace(/"url":"(experiments\\/[^"]+)"/g'), "the artifact gallery should expose bounded structured discovery metadata, copy-paste commands, Canvas and service-health verification, share verification, backup verification, graph verification, diff verification, and a downloadable sample graph viewer");
assert(fs.existsSync(new URL("../scripts/check-artifacts.mjs", import.meta.url)) && packageManifest.scripts["artifacts:check"] === "node scripts/check-artifacts.mjs" && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes('import("./check-artifacts.mjs")'), "the public artifact gallery should have an automated consistency gate for cards, structured metadata, and downloadable targets");
assert(fs.existsSync(new URL("../tests/artifacts-smoke.mjs", import.meta.url)) && testRunner.includes('readdirSync("tests")') && artifactHtml.includes("experiments/evaluate-feedback.mjs --help") && artifactHtml.includes("experiments/graph-input.mjs --help") && artifactHtml.includes("experiments/verify-graph.mjs --help"), "every public artifact command should be executable as displayed, including library and argument-requiring tools");
assert(artifacts.includes("artifact contribution guide") && artifactTemplate.includes("Share an artifact") && artifactTemplate.includes("Privacy check") && artifactTemplate.includes("removed private source text") && artifactSubmission.includes("# Artifact submission template") && artifactSubmission.includes("Limitations and failure modes") && artifactSubmission.includes("## Privacy check"), "the project should provide a structured, honest, privacy-aware artifact submission path");
assert(app.includes("setNoteMetadata") && app.includes("restorePageMetadata") && app.includes("meta[property=\"og:title\"]"), "note deep links should update and restore share metadata");
assert.equal(manifest.id, "./");
assert.equal(manifest.scope, "./");
assert.equal(manifest.start_url, "./#workbench");
assert.deepEqual(manifest.categories, ["education", "productivity", "developer"]);
assert(html.includes('<link rel="apple-touch-icon" sizes="180x180" href="icon-192.png" />')
  && html.includes('<meta name="apple-mobile-web-app-capable" content="yes" />')
  && html.includes('<meta name="mobile-web-app-capable" content="yes" />'), "the public shell should expose cross-platform installed-app metadata");
assert.deepEqual(manifest.icons.slice(0, 2), [
  { src: "icon-192.png", sizes: "192x192", type: "image/png", purpose: "any maskable" },
  { src: "icon-512.png", sizes: "512x512", type: "image/png", purpose: "any maskable" }
], "the installable app should publish explicit raster icons for common PWA installers");
assert.equal(manifest.shortcuts.length, 3, "the installable app should expose launcher shortcuts");
assert.equal(manifest.shortcuts[2].url, "./#sample", "the installable app should expose a sample graph shortcut");
assert(publicAssets.includes('"LICENSE"') && html.includes('href="LICENSE" rel="license"'), "the public deployment should expose and semantically link its license");
assert(publicAssets.includes('"404.html"') && notFound.includes("That page is not in the graph.") && notFound.includes("script-src 'none'") && notFound.includes('href="./artifacts.html"') && !notFound.includes("<style>") && styles.includes(".not-found-page"), "the public deployment should provide a script-free, CSP-compatible branded recovery page for unknown routes");
assert(publicAssets.includes('"llms.txt"') && html.includes('href="llms.txt"') && llms.includes("./artifacts.html") && llms.includes("./experiments/learning-loop.mjs") && llms.includes("./rebuild-adapter.js") && llms.includes("./ARTIFACT_SUBMISSION.md"), "the public deployment should expose an LLM-readable project map with artifact discovery, self-improvement workflows, and contribution paths");
assert(llms.includes("./schema/jsonld.schema.json") && llms.includes("JSON-LD schema") && llms.includes("./schema/learning-loop.schema.json"), "the project map should document the JSON-LD and learning-loop contracts");
assert(fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("localLlmsLinks") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("llms.txt links to a missing or unpublished asset"), "release verification should keep local llms.txt links resolvable");
assert(publicAssets.includes('"schema/vault-manifest.schema.json"'), "the vault manifest schema should be publicly deliverable");
assert.equal(learningLoopSchema.properties.format.const, "llm-field-notes/learning-loop@1", "learning-loop output should have a versioned schema");
assert(learningLoopSchema.properties.stages.properties.comparison.properties.conceptsRemovedByGuidance, "learning-loop schema should expose a measurable guidance delta");
assert(publicAssets.includes('"rebuild-adapter.js"') && serviceWorker.includes("./rebuild-adapter.js") && fs.existsSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url)), "saved-source rebuild orchestration should be a published, offline-safe, directly tested module");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("MAX_GRAPH_DOCUMENTS") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("Saved source count exceeds"), "saved-source rebuild orchestration should retain the graph document safety bound");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("Saved source records must contain bounded IDs, titles, and text."), "saved-source rebuild orchestration should validate source records before invoking host extractors");
assert(graphCore.includes("stopWords") && graphCore.includes("phraseBoundaryWords") && graphCore.includes("also") && graphCore.includes("connects") && graphCore.includes("MAX_DOCUMENT_TITLE_CHARS") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_DOCUMENT_TITLE_CHARS") && server.includes("MAX_DOCUMENT_TITLE_CHARS"), "document-title limits and separate heuristic phrase-boundary vocabulary should remain explicit across browser and server boundaries");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("replacement returned an invalid graph"), "saved-source rebuild orchestration should validate injected replacement results");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes('"source-linked knowledge"') && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("emptyReplacement"), "saved-source rebuilds should preserve prior source-linked knowledge when a provider returns an empty replacement");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("replacementSourceContributionCount") && fs.readFileSync(new URL("../tests/smoke.mjs", import.meta.url), "utf8").includes("emptySourceReplacement"), "interactive source replacement should preserve prior source-linked knowledge when an extractor returns an empty representation");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("preserveSourceCategories") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("sourceContributionCounts") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("preserveSourceCategories: true") && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("adapterGuardedRelationDrop"), "automatic source rebuilds should fail closed when a provider or injected replacement removes an entire previously represented concept or relation category");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("hasIntegrityAmbiguity") && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("ambiguousReplacement"), "automatic source rebuilds should reject replacement graphs with ambiguous or contradictory identity diagnostics");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("savedSourceRecordsPreserved") && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("unrelatedSourceMutation"), "automatic source rebuilds should preserve unrelated saved source records and metadata");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("reviewedDecisionsPreserved")
  && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("suppressedRejectedDecisions")
  && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("preservedLearningDecisionKeys")
  && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("replacement removed or changed a reviewed decision")
  && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("reviewedRelationDrop")
  && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("suppressionRebuild"), "automatic source rebuilds should preserve accepted decisions, retain reusable rejected memory, and allow explicitly guided suppression to improve inferred representation");
assert(fs.readFileSync(new URL("../scripts/note-page.mjs", import.meta.url), "utf8").includes("while (String(value).includes(linkTokenPrefix)") && fs.readFileSync(new URL("../tests/notes-smoke.mjs", import.meta.url), "utf8").includes("placeholders should not be forgeable"), "learning-note HTML rendering should prevent internal link-token collisions with authored Markdown");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("sourceIds.size === expectedSourceIds.size") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("MAX_GRAPH_EDGES") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("beforeNodes") && app.includes("sourceContributionBefore"), "saved-source rebuilds should preserve stable source identities, validate graph bounds, and expose contribution deltas");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("redacted graph cannot be rebuilt") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("hasPositiveDiagnostic"), "saved-source rebuild orchestration should fail closed on redacted, incomplete, or ambiguous graph integrity");
assert(publicAssets.includes('"schema/learning-loop.schema.json"') && serviceWorker.includes("./schema/learning-loop.schema.json"), "the learning-loop schema should be published and available offline");
assert(!readme.includes("Open `index.html` directly") && readme.includes("There are no runtime dependencies."), "onboarding should describe the dependency-free HTTP workflow accurately");
assert(readme.includes("item-specific correction template"), "README should document the privacy-safe contribution shortcut");
assert(packageLock.lockfileVersion === 3
  && packageLock.packages?.[""]?.name === packageManifest.name
  && packageLock.packages?.[""]?.version === packageManifest.version
  && packageLock.packages?.[""]?.license === packageManifest.license
  && JSON.stringify(packageLock.packages?.[""]?.engines) === JSON.stringify(packageManifest.engines), "npm lockfile should pin the root package metadata");
assert.equal(packageManifest.engines.node, ">=22", "package metadata should require the maintained Node 22+ production baseline");
assert(!app.includes("future model adapter") && !app.includes("local prototype"), "public workbench copy should describe the supported local and model-backed paths accurately");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("npm run serve"), "onboarding should document the dependency-free local server");
for (const artifactAsset of ["experiments/tiny-bpe.mjs", "experiments/tiny-attention.mjs", "experiments/tiny-transformer.mjs", "experiments/evaluate-feedback.mjs", "experiments/project-jsonld.mjs", "experiments/diff-graphs.mjs"]) {
  assert(dockerignore.includes(`!${artifactAsset}`), `Docker context should retain the published artifact source: ${artifactAsset}`);
}
assert(fs.existsSync(new URL("../experiments/learning-loop.mjs", import.meta.url)) && publicAssets.includes('"experiments/learning-loop.mjs"') && serviceWorker.includes("./experiments/learning-loop.mjs") && artifactHtml.includes('href="experiments/learning-loop.mjs"') && artifacts.includes("learning-loop.mjs"), "the end-to-end learning-loop artifact should be published, offline-safe, and discoverable");
assert(publicAssets.includes('"experiments/verify-graph.mjs"') && serviceWorker.includes("./experiments/verify-graph.mjs") && artifactHtml.includes('href="experiments/verify-graph.mjs"') && artifacts.includes("verify-graph.mjs"), "the graph verifier should be published, offline-safe, and discoverable");
assert(publicAssets.includes('"experiments/verify-backup.mjs"') && serviceWorker.includes("./experiments/verify-backup.mjs") && fs.readFileSync(new URL("../experiments/verify-backup.mjs", import.meta.url), "utf8").includes("--password-stdin"), "the backup verifier should be published offline-safe and accept passwords only through stdin");
assert(fs.existsSync(new URL("../examples/sample-graph.json", import.meta.url)) && fs.existsSync(new URL("../examples/sample-graph.canvas", import.meta.url)) && fs.existsSync(new URL("../examples/sample-service-health.json", import.meta.url)) && fs.existsSync(new URL("../schema/canvas.schema.json", import.meta.url)) && fs.existsSync(new URL("../schema/service-health.schema.json", import.meta.url)) && fs.existsSync(new URL("../scripts/verify-canvas.mjs", import.meta.url)) && fs.existsSync(new URL("../scripts/verify-service-health.mjs", import.meta.url)) && fs.existsSync(new URL("../tests/sample-graph-smoke.mjs", import.meta.url)) && fs.existsSync(new URL("../tests/service-health-smoke.mjs", import.meta.url)) && fs.existsSync(new URL("../scripts/sample-graph-page.mjs", import.meta.url)) && fs.existsSync(new URL("../scripts/sample-graph-canvas.mjs", import.meta.url)) && publicAssets.includes('"examples/sample-graph.json"') && publicAssets.includes('"examples/sample-graph.canvas"') && publicAssets.includes('"examples/sample-service-health.json"') && publicAssets.includes('"schema/canvas.schema.json"') && publicAssets.includes('"schema/service-health.schema.json"') && publicAssets.includes('"scripts/verify-canvas.mjs"') && publicAssets.includes('"scripts/verify-service-health.mjs"') && serviceWorker.includes("./examples/sample-graph.json") && serviceWorker.includes("./examples/sample-graph.canvas") && serviceWorker.includes("./examples/sample-service-health.json") && serviceWorker.includes("./schema/canvas.schema.json") && serviceWorker.includes("./schema/service-health.schema.json") && serviceWorker.includes("./scripts/verify-canvas.mjs") && serviceWorker.includes("./scripts/verify-service-health.mjs") && fs.readFileSync(new URL("../scripts/public-assets.mjs", import.meta.url), "utf8").includes("GENERATED_PUBLIC_ASSETS") && fs.readFileSync(new URL("../scripts/public-assets.mjs", import.meta.url), "utf8").includes('"sample-graph.html"') && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("buildSampleGraphPage") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("buildSampleGraphCanvas") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("verifyCanvasProjection") && server.includes("SAMPLE_GRAPH_PAGE") && server.includes("buildSampleGraphPage") && artifactHtml.includes('href="examples/sample-graph.json"'), "the sample graph export, native Canvas projection, service-health contract and verifiers, and generated crawler-readable explainer should be published, offline-safe, discoverable, and regression-tested");
const sampleGraph = JSON.parse(fs.readFileSync(new URL("../examples/sample-graph.json", import.meta.url), "utf8"));
assert.match(sampleGraph.graphFingerprint, /^fnv64-[0-9a-f]{16}-[0-9]+$/, "the sample graph should carry an integrity fingerprint");
assert.equal(packageManifest.scripts.start, "node server.mjs", "production Node hosts should have a standard start command");
assert.equal(packageManifest.scripts["smoke:server"], "node scripts/smoke-server.mjs", "standalone server lifecycle should have a reusable smoke command");
assert.equal(packageManifest.scripts["browser:smoke"], "npm run runtime:check && node scripts/browser-smoke.mjs", "browser certification should enforce the runtime and have a reproducible smoke command");
assert.equal(packageManifest.scripts["load:server"], "node scripts/load-server.mjs", "deployment-specific capacity probes should have a reusable package command");
assert(loadServer.includes("LOAD_TEST_CONFIRM") && loadServer.includes("LOAD_TEST_DURATION_MS") && loadServer.includes("MAX_DURATION_REQUESTS") && loadServer.includes("normalizeLoadBaseUrl") && loadServer.includes("sanitizeLoadIdentity") && loadServer.includes("target: sanitizeLoadIdentity(config.url)") && loadServer.includes("deploymentBase: config.allowedOrigin ? sanitizeLoadIdentity(config.allowedOrigin) : null") && loadServer.includes("targetDurationMs") && loadServer.includes("failedRequests") && loadServer.includes("failureRate") && loadServer.includes("throughputRps") && loadServer.includes("requests: cursor") && loadServer.includes("latencies.push") && loadServer.includes("LOAD_TEST_MAX_FAILURES") && loadServer.includes("LOAD_TEST_MAX_P95_MS") && loadServer.includes("enforceLoadBudget") && loadServer.includes("MAX_REQUESTS") && loadServer.includes("MAX_CONCURRENCY") && loadServer.includes("MAX_RESPONSE_BYTES") && loadServer.includes("Content-Length") && loadServer.includes("getReader") && loadServer.includes("reader.cancel") && loadServer.includes("Promise.race([fetchPromise, timeoutPromise])") && loadServer.includes("cancelResponseBody") && loadServer.includes("invalid health response contract") && loadServer.includes("llm-field-notes/graph@1") && loadServer.includes("peakInFlight") && loadServer.includes("must be a non-negative integer") && loadServer.includes("must be a positive integer"), "load probes should be explicitly confirmed for non-loopback targets, bind to the exact deployment base path, retain sanitized target identity even for direct callers, reject malformed numeric inputs, support bounded duration mode, report unique failure counts, throughput, and failure rate, include failed attempts in latency evidence, validate health and graph responses, enforce operator-supplied failure and latency budgets, settle fetch/body hangs within the deadline, bound all response paths, and cancel failed streams");
assert.equal(packageManifest.scripts["smoke:container"], "node scripts/smoke-container.mjs", "hardened container smoke should have a reusable package command");
assert(containerSmoke.includes("--read-only") && containerSmoke.includes("--cap-drop=ALL") && containerSmoke.includes("no-new-privileges") && containerSmoke.includes("Config.Env") && containerSmoke.includes("NODE_ENV=production") && containerSmoke.includes("EXTRACTOR_AUTH_TOKEN|METRICS_AUTH_TOKEN") && containerSmoke.includes("Config.Healthcheck.Test") && containerSmoke.includes("/readyz") && containerSmoke.includes("parseConfiguredBoundedInteger") && containerSmoke.includes("setting\\.valid") && containerSmoke.includes("HostConfig.ReadonlyRootfs") && containerSmoke.includes("HostConfig.Tmpfs") && containerSmoke.includes("HostConfig.CapDrop") && containerSmoke.includes("HostConfig.SecurityOpt") && containerSmoke.includes("test ! -w /app") && containerSmoke.includes("MAX_DIAGNOSTIC_LOG_BYTES") && containerSmoke.includes("boundDiagnosticLog") && containerSmoke.includes("printContainerDiagnostics") && containerSmoke.includes("--tail") && containerSmoke.includes("org.opencontainers.image.revision") && containerSmoke.includes("org.opencontainers.image.source") && containerSmoke.includes("CONTAINER_EXPECTED_SOURCE") && containerSmoke.includes("CONTAINER_EXPECTED_DOCUMENTATION") && containerSmoke.includes("PUBLIC_REPOSITORY_URL") && containerSmoke.includes("security/advisories/new") && containerSmoke.includes("State.Health.Status") && containerSmoke.includes("CONTAINER_EXPECTED_REVISION") && containerSmoke.includes("llm-field-notes/graph@1"), "container smoke should exercise image production metadata, secret exclusion, readiness healthcheck, fail-closed port validation, recorded read-only/tmpfs isolation, preserve byte-bounded failure diagnostics, validate release/version/repository identity and Docker health, and probe the configured repository security metadata and authenticated graph contract");
const deploymentSmoke = fs.readFileSync(new URL("../tests/deployment-smoke.mjs", import.meta.url), "utf8");
assert(deploymentSmoke.includes("requestTimeoutMs") && deploymentSmoke.includes("ignores cancellation") && deploymentSmoke.includes("canceledHangingResponse") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("content-encoding") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("readBoundedBody") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("application/javascript"), "Pages deployment smoke should cover fetch and streamed-body hangs that ignore cancellation, correctly handle transparently decoded encoded responses, and accept the platform's JavaScript media type");
assert(serverSmoke.includes("BUILD_REVISION") && serverSmoke.includes("EXTRACTOR_AUTH_TOKEN") && serverSmoke.includes("EXTRACTOR_RATE_LIMIT") && serverSmoke.includes("runLoadProbe") && serverSmoke.includes("healthLoad") && serverSmoke.includes("extractionLoad") && serverSmoke.includes("sustainedExtractionLoad") && serverSmoke.includes("durationMs: 1000") && serverSmoke.includes("authenticatedExtraction") && serverSmoke.includes("concurrentExtractions") && serverSmoke.includes("authenticatedMetrics") && serverSmoke.includes("waitForExit") && serverSmoke.includes("SIGKILL") && serverSmoke.includes("did not exit within the shutdown timeout") && serverSmoke.includes("server-ready") && serverSmoke.includes("server-draining") && serverSmoke.includes("server-stopped"), "standalone server smoke should verify release identity, authenticated success, live bounded and duration-based load probes, and bounded concurrency/rejection and lifecycle paths");
assert(fs.existsSync(new URL("../scripts/browser-smoke.mjs", import.meta.url)) && fs.existsSync(new URL("../.github/workflows/browser.yml", import.meta.url)) && browserSmoke.includes("BROWSER_SMOKE_URL") && browserSmoke.includes("BROWSER_SMOKE_CONFIRM") && browserSmoke.includes("BROWSER_SMOKE_EXPECTED_REVISION") && browserSmoke.includes("BROWSER_SMOKE_LOCAL_REVISION") && browserSmoke.includes("BROWSER_SMOKE_ARTIFACT_DIR") && browserSmoke.includes("manifest.webmanifest") && browserSmoke.includes("icon-192.png") && browserSmoke.includes("icon-512.png") && browserSmoke.includes("apple-touch-icon") && browserSmoke.includes("apple-mobile-web-app-capable") && browserSmoke.includes("page.screenshot") && browserSmoke.includes("chromium") && browserSmoke.includes("firefox") && browserSmoke.includes("webkit") && browserSmoke.includes("pageerror") && browserSmoke.includes("domcontentloaded") && browserSmoke.includes("waitForText") && browserSmoke.includes("waitForServiceWorker") && browserSmoke.includes("setOffline") && browserSmoke.includes("reducedMotion") && browserSmoke.includes("viewport") && browserSmoke.includes("scrollWidth") && browserSmoke.includes("accessible names") && browserSmoke.includes("data-feedback='up'") && browserSmoke.includes("learning:\\s*1 accepted") && browserSmoke.includes("download-vault") && browserSmoke.includes("download-canvas") && browserSmoke.includes("Graph.canvas") && browserSmoke.includes("setInputFiles") && browserSmoke.includes("504b0304") && browserSmoke.includes("clear-graph") && browserSmoke.includes("undo-graph") && browserSmoke.includes("offline emulation") && browserSmoke.includes("visible release footer") && browserSmoke.includes("same-origin model extraction") && browserSmoke.includes("model extraction failure recovery") && browserSmoke.includes("WebKit service-worker fetches bypass") && browserSmoke.includes("route.fulfill") && browserSmoke.includes('fill("/api/extract-graph")') && browserSmoke.includes('waitForText(page.locator("#extractor-mode"), /^MODEL$/)') && browserSmoke.includes('waitForText(page.locator("#extractor-mode"), /^LOCAL$/)') && fs.readFileSync(new URL("../.github/workflows/browser.yml", import.meta.url), "utf8").includes("playwright install --with-deps") && fs.readFileSync(new URL("../.github/workflows/browser.yml", import.meta.url), "utf8").includes("actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02") && fs.readFileSync(new URL("../.github/workflows/browser.yml", import.meta.url), "utf8").includes("BROWSER_ENGINE"), "browser certification should exercise local and explicitly confirmed exact-origin workbenches across Chromium, Firefox, and WebKit with bounded UI readiness, same-origin model extraction mode and failure recovery where the runner supports interception, manifest/icon and Apple installed-app metadata delivery, destructive clear→undo recovery, reduced-motion and narrow-viewport overflow checks, keyboard focus and accessible names, human review becoming reusable learning, direct Graph.canvas and Obsidian ZIP export/import round trips, offline reopening where browser emulation supports it, service-worker activation, console-error capture, release source-revision identity, visible release-footer identity, and failure screenshots uploaded by isolated CI");
assert(serviceWorker.includes('APP_SHELL.includes("./asset-manifest.json")') && fs.readFileSync(new URL("../tests/sw-smoke.mjs", import.meta.url), "utf8").includes("injected asset manifest"), "Pages service-worker integrity metadata should be required when the built shell injects its manifest");
assert(publicAssets.includes('"share.html"') && publicAssets.includes('"share.js"') && publicAssets.includes('"share-projection.js"') && serviceWorker.includes("const SHARE_SHELL") && serviceWorker.includes("./share.html"), "recipient share assets should be published and cached as an explicit offline shell");
assert(shareSchema.properties.format.const === "llm-field-notes/share@1" && shareSchema.additionalProperties === false && publicAssets.includes('"schema/share.schema.json"') && serviceWorker.includes("./schema/share.schema.json"), "the redacted share projection should have a closed published schema and offline delivery");
assert(fs.existsSync(new URL("../scripts/verify-share.mjs", import.meta.url))
  && packageManifest.scripts["verify:share"] === "node scripts/verify-share.mjs"
  && publicAssets.includes('"scripts/verify-share.mjs"')
  && serviceWorker.includes("./scripts/verify-share.mjs"), "redacted share JSON should have a published offline verifier and stable package command");
assert(browserSmoke.includes("prepareDiagnosticScreenshotPath") && browserSmoke.includes("MAX_BROWSER_ERROR_RECORDS") && browserSmoke.includes("MAX_BROWSER_ERROR_CHARS") && browserSmoke.includes("appendBoundedDiagnostic") && browserDiagnostics.includes("lstat") && browserDiagnostics.includes("isSymbolicLink"), "browser failure screenshots and error records should remain bounded and reject symlinked diagnostic directories and screenshot paths");
const pagesPublishWorkflow = fs.readFileSync(new URL("../.github/workflows/pages.yml", import.meta.url), "utf8");
assert(pagesPublishWorkflow.includes("outputs:") && pagesPublishWorkflow.includes("page_url: ${{ steps.deployment.outputs.page_url }}") && pagesPublishWorkflow.includes("deploy:\n    permissions:\n      contents: read\n      pages: write\n      id-token: write\n      attestations: write") && pagesPublishWorkflow.includes("actions/attest@a1948c3f048ba23858d222213b7c278aabede763") && pagesPublishWorkflow.includes("subject-path: dist/asset-manifest.json") && pagesPublishWorkflow.includes("sbom-path: sbom.spdx.json") && pagesPublishWorkflow.includes("browser:\n    name: Smoke deployed origin (${{ matrix.browser }})\n    needs: deploy\n    permissions:\n      contents: read") && pagesPublishWorkflow.includes("BROWSER_SMOKE_URL") && pagesPublishWorkflow.includes("BROWSER_SMOKE_CONFIRM") && pagesPublishWorkflow.includes("BROWSER_SMOKE_EXPECTED_REVISION: ${{ github.sha }}") && pagesPublishWorkflow.includes("BROWSER_SMOKE_ARTIFACT_DIR") && pagesPublishWorkflow.includes("playwright install --with-deps ${{ matrix.browser }}") && pagesPublishWorkflow.includes("browser: [chromium, firefox, webkit]") && pagesPublishWorkflow.includes("npm run browser:smoke") && pagesPublishWorkflow.includes("actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02"), "Pages publication should attest and browser-smoke the exact deployed origin and source revision across Chromium, Firefox, and WebKit after the artifact probe, retain failure screenshots for triage, and keep publish/id-token/attestation permissions confined to the deploy job");
assert(pagesPublishWorkflow.includes("name: Retain Pages publication evidence")
  && pagesPublishWorkflow.includes("pages-publication-evidence-${{ github.run_id }}")
  && pagesPublishWorkflow.includes("sbom.spdx.json")
  && pagesPublishWorkflow.includes("dist/asset-manifest.json")
  && pagesPublishWorkflow.includes("dist/version.json")
  && pagesPublishWorkflow.includes("dist/share.html")
  && pagesPublishWorkflow.includes("dist/share.js")
  && pagesPublishWorkflow.includes("dist/share-projection.js")
  && pagesPublishWorkflow.includes("dist/sw.js")
  && pagesPublishWorkflow.includes("dist/feed.xml")
  && pagesPublishWorkflow.includes("dist/.well-known/security.txt")
  && pagesPublishWorkflow.includes("dist/sample-graph.html")
  && pagesPublishWorkflow.includes("retention-days: 30"), "ordinary Pages deployments should retain exact release metadata for incident comparison and rollback analysis");
const capacityWorkflow = fs.readFileSync(new URL("../.github/workflows/capacity.yml", import.meta.url), "utf8");
assert(capacityWorkflow.includes("workflow_dispatch:") && capacityWorkflow.includes("LOAD_TEST_CONFIRM") && capacityWorkflow.includes("LOAD_TEST_ALLOWED_ORIGIN: ${{ vars.PUBLIC_ORIGIN }}") && capacityWorkflow.includes("LOAD_TEST_DURATION_MS") && capacityWorkflow.includes("LOAD_TEST_MAX_P95_MS") && capacityWorkflow.includes("LOAD_TEST_MAX_FAILURES") && capacityWorkflow.includes("set -o pipefail") && capacityWorkflow.includes("tee capacity-probe.log") && capacityWorkflow.includes("actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02") && capacityWorkflow.includes("capacity-probe-${{ github.run_id }}") && capacityWorkflow.includes("retention-days: 14") && capacityWorkflow.includes("npm run load:server") && capacityWorkflow.includes("I_UNDERSTAND") && !capacityWorkflow.includes("EXTRACTOR_AUTH_TOKEN") && !capacityWorkflow.includes("secrets."), "capacity operations should provide a guarded HTTPS-only health workflow with bounded duration, explicit authorization, deployment-origin binding, SLO budgets, and retained failure/success evidence without sending secrets to arbitrary targets");
assert(productionStatus.includes("Automated browser smoke") && productionStatus.includes("Exact-deployment browser smoke") && productionStatus.includes("Sustained-load and extended browser certification") && productionStatus.includes("does not, by itself, establish a capacity target") && productionStatus.includes("environment-specific load and browser exercises"), "production status should distinguish automated local and exact-origin browser coverage from remaining deployment-specific capacity and browser certification");
assert(fs.readFileSync(new URL("../scripts/browser-smoke.mjs", import.meta.url), "utf8").includes("loading public sample graph page") && fs.readFileSync(new URL("../scripts/browser-smoke.mjs", import.meta.url), "utf8").includes("sample-graph-visual") && fs.readFileSync(new URL("../scripts/browser-smoke.mjs", import.meta.url), "utf8").includes("sample-graph.canvas") && fs.readFileSync(new URL("../scripts/browser-smoke.mjs", import.meta.url), "utf8").includes("sample graph should not overflow"), "browser certification should exercise the public sample graph page's visualization, CTA, direct Canvas projection link, accessibility, and mobile layout");
assert(fs.existsSync(new URL("../tests/build-revision-smoke.mjs", import.meta.url)) && testRunner.includes('readdirSync("tests")'), "deployment revision behavior should have an end-to-end Node smoke test in the standard suite");
assert.equal(packageManifest.scripts.diff, "node experiments/diff-graphs.mjs", "graph diff should have a documented package command");
assert.equal(packageManifest.scripts["learning:loop"], "node experiments/learning-loop.mjs", "the self-improvement walkthrough should have a stable package command");
assert.equal(packageManifest.scripts["learning:check"], "node scripts/check-learning-loop.mjs", "the self-improvement proof should have a production gate command");
assert(fs.existsSync(new URL("../scripts/check-learning-loop.mjs", import.meta.url)), "the self-improvement production gate should remain deliverable");
assert(fs.existsSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url)) && fs.existsSync(new URL("../benchmarks/sample-feedback.json", import.meta.url)) && fs.existsSync(new URL("../benchmarks/local-extraction-feedback.json", import.meta.url)) && fs.existsSync(new URL("../benchmarks/extraction-cases.json", import.meta.url)) && packageManifest.scripts["evaluation:check"] === "node scripts/check-evaluation-benchmark.mjs" && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("representativeCases") && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("representativeCaseCount") && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("extractionCases.cases.length < 5") && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("new Set(caseIds)") && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("requiredCaseIds") && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("relation-quality") && fs.readFileSync(new URL("../scripts/check-evaluation-benchmark.mjs", import.meta.url), "utf8").includes("benchmarkNow"), "the reviewed extraction-quality benchmarks should remain deliverable, diverse, relation-aware, uniquely identified, explicitly timestamped, complete, and production-gated");
assert(fs.existsSync(new URL("../experiments/bounded-file.mjs", import.meta.url)), "experiment inputs should have a shared bounded reader");
assert.equal(packageManifest.scripts["release:check"], "node scripts/check-release.mjs", "release metadata should have an automated consistency command");
assert.equal(packageManifest.scripts["release:tag:check"], "node scripts/check-release-tag.mjs", "versioned releases should have a strict tag-to-package-version command");
assert(fs.existsSync(new URL("../scripts/check-release.mjs", import.meta.url))
  && fs.existsSync(new URL("../scripts/check-workflows.mjs", import.meta.url))
  && packageManifest.devDependencies.yaml === "^2.9.0"
  && packageManifest.scripts["workflows:check"] === "node scripts/check-workflows.mjs"
  && packageLock.packages?.["node_modules/yaml"]?.version === "2.9.0"
  && fs.readFileSync(new URL("../scripts/check-workflows.mjs", import.meta.url), "utf8").includes('parseDocument(workflow, { uniqueKeys: true })')
  && fs.readFileSync(new URL("../scripts/check-workflows.mjs", import.meta.url), "utf8").includes("references an unknown dependency")
  && fs.readFileSync(new URL("../scripts/check-workflows.mjs", import.meta.url), "utf8").includes("steps must be a list of mappings")
  && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("checkWorkflows"), "release consistency checks should share a standalone locked workflow validator covering unique keys, job dependencies, permissions, and step shapes");
assert.equal(packageManifest.scripts["sbom:generate"], "node scripts/generate-sbom.mjs", "dependency SBOM generation should have a reproducible package command");
assert.equal(packageManifest.scripts["verify:sbom"], "node scripts/verify-sbom.mjs", "dependency SBOM verification should have a reproducible package command");
assert(fs.existsSync(new URL("../scripts/generate-sbom.mjs", import.meta.url))
  && fs.existsSync(new URL("../scripts/verify-sbom.mjs", import.meta.url))
  && fs.existsSync(new URL("../tests/sbom-smoke.mjs", import.meta.url)), "dependency SBOM generation, verification, and regression coverage should remain deliverable");
assert(fs.existsSync(new URL("../backup-crypto.js", import.meta.url)) && fs.existsSync(new URL("../tests/backup-crypto-smoke.mjs", import.meta.url)) && packageManifest.scripts["backup:smoke"].includes("node tests/backup-crypto-smoke.mjs") && fs.readFileSync(new URL("../scripts/public-assets.mjs", import.meta.url), "utf8").includes('"backup-crypto.js"') && fs.readFileSync(new URL("../app.js", import.meta.url), "utf8").includes("decryptBackup") && fs.readFileSync(new URL("../app.js", import.meta.url), "utf8").includes("MAX_BACKUP_IMPORT_BYTES") && fs.readFileSync(new URL("../app.js", import.meta.url), "utf8").includes("BACKUP_FORMAT.replace") && fs.readFileSync(new URL("../index.html", import.meta.url), "utf8").includes("download-encrypted-backup"), "password-encrypted browser backups should remain deliverable, wired into bounded import/export, and regression-tested");
assert(fs.existsSync(new URL("../scripts/check-release-tag.mjs", import.meta.url)) && fs.existsSync(new URL("../tests/release-tag-smoke.mjs", import.meta.url)) && testRunner.includes('readdirSync("tests")'), "versioned release tag validation should remain deliverable and regression-tested");
assert(fs.existsSync(new URL("../tests/provider-http-smoke.mjs", import.meta.url)) && testRunner.includes('readdirSync("tests")') && fs.readFileSync(new URL("../tests/provider-http-smoke.mjs", import.meta.url), "utf8").includes("createServer") && fs.readFileSync(new URL("../tests/provider-http-smoke.mjs", import.meta.url), "utf8").includes("createAppServer") && fs.readFileSync(new URL("../tests/provider-http-smoke.mjs", import.meta.url), "utf8").includes("retry-after"), "the provider boundary should have a real localhost gateway-to-provider integration smoke test covering retries and request/response normalization");
assert(fs.existsSync(new URL("../scripts/check-contracts.mjs", import.meta.url)) && packageManifest.scripts["contracts:check"] === "node scripts/check-contracts.mjs", "runtime/schema contract checks should remain deliverable");
assert(fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("MAX_BROADCAST_VALUE_BYTES") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("MAX_PERSISTED_JSON_BYTES"), "storage and graph persistence byte ceilings should remain coupled by the contract check");
assert(fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("PUBLIC_ORIGIN") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("buildStaticSitemap") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("asset-manifest.json") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("generatedShellAssets = [`./${ASSET_MANIFEST}`") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("sha256") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("renderRepositoryAwareSecurityTxt") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes('import("./check-artifacts.mjs")'), "Pages builds should support origin-aware crawler discovery, publish deterministic asset digests, rewrite fork security metadata, cache the manifest offline, and gate the artifact gallery");
assert(fs.existsSync(new URL("../scripts/verify-pages.mjs", import.meta.url)) && packageManifest.scripts["verify:pages"] === "node scripts/verify-pages.mjs" && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("lstat") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("must not be a symbolic link") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("Pages asset manifest does not cover exactly") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("robots policy does not match") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("APP_SHELL is missing generated note page") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("readServiceWorkerShellAssets") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("readServiceWorkerShareShellAssets") && fs.readFileSync(new URL("../scripts/service-worker-cache.mjs", import.meta.url), "utf8").includes("valid JSON string array"), "Pages asset manifests should independently verify exact files, reject symbolic links, strictly parse, bind crawler policy to the deployment origin, and cover the offline shell");
assert(fs.existsSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url)) && fs.existsSync(new URL("../tests/deployment-smoke.mjs", import.meta.url)) && packageManifest.scripts["smoke:pages:deployment"] === "node scripts/smoke-pages-deployment.mjs" && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("redirected outside the deployment origin") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes('redirect: "manual"') && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("MAX_SAME_ORIGIN_REDIRECTS") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("enable GitHub Pages with the Actions source") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("does not match Content-Length") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("cancelResponseBody") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("asset-manifest.json") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("version.json") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("expectedRevision") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("parseJsonWithUniqueKeys") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("MANIFEST_FETCH_CONCURRENCY") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("createHash") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("computeServiceWorkerCacheRevision") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("readServiceWorkerCacheName") && fs.readFileSync(new URL("../tests/deployment-smoke.mjs", import.meta.url), "utf8").includes("stale source revision") && fs.readFileSync(new URL("../tests/deployment-smoke.mjs", import.meta.url), "utf8").includes("service-worker cache revision does not match") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes('path: "404.html"') && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes('path: "manifest.webmanifest"') && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes('path: "sample-graph.html"') && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes('path: "examples/sample-graph.canvas"') && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("Sitemap:") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("github\\.com") && fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("issues\\/new"), "Pages publication should have a post-deploy smoke probe with bounded response reads, declared-length and manifest-digest integrity, bounded same-origin redirects, live service-worker cache identity, release manifest/version identity, branded 404, installable web-app metadata, sample graph HTML and Canvas projections, source-revision identity, early-body cleanup, actionable activation diagnostics, fork/contribution loop, and origin/crawler assertions");
assert(fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8").includes("redirectedUrl.username") && fs.readFileSync(new URL("../tests/deployment-smoke.mjs", import.meta.url), "utf8").includes("credential-bearing same-origin redirects"), "Pages deployment redirects should reject embedded credentials before following same-origin locations");
assert(fs.existsSync(new URL("../scripts/check-accessibility.mjs", import.meta.url)) && fs.existsSync(new URL("../tests/accessibility-smoke.mjs", import.meta.url)) && packageManifest.scripts["accessibility:check"] === "node scripts/check-accessibility.mjs" && testRunner.includes('readdirSync("tests")') && fs.readFileSync(new URL("../scripts/check-accessibility.mjs", import.meta.url), "utf8").includes("duplicate id") && fs.readFileSync(new URL("../scripts/check-accessibility.mjs", import.meta.url), "utf8").includes("aria-labelledby"), "generated HTML should have a dependency-free accessibility release gate for labels, names, alt text, headings, and duplicate IDs");
assert(fs.existsSync(new URL("../scripts/check-performance.mjs", import.meta.url)) && fs.existsSync(new URL("../tests/performance-smoke.mjs", import.meta.url)) && packageManifest.scripts["performance:check"] === "node scripts/check-performance.mjs" && testRunner.includes('readdirSync("tests")') && fs.readFileSync(new URL("../scripts/check-performance.mjs", import.meta.url), "utf8").includes("MAX_CRITICAL_SHELL_BYTES") && fs.readFileSync(new URL("../scripts/check-performance.mjs", import.meta.url), "utf8").includes("MAX_CRITICAL_SCRIPT_BYTES"), "generated Pages releases should enforce a critical browser-shell performance budget with regression coverage");
assert(fs.readFileSync(new URL("../scripts/note-page.mjs", import.meta.url), "utf8").includes("parsed.username") && fs.readFileSync(new URL("../scripts/note-page.mjs", import.meta.url), "utf8").includes("linkTokenPrefix"), "public learning-note links should be explicitly URL-allowlisted and credential-free");
assert(fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("MAX_BUILD_CONCURRENCY") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("mapWithConcurrency") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("for (const asset of assets)"), "Pages builds should bound file concurrency and read learning metadata deterministically");
const pagesBuildWorkflow = fs.readFileSync(new URL("../.github/workflows/pages.yml", import.meta.url), "utf8");
const pagesMonitorWorkflow = fs.readFileSync(new URL("../.github/workflows/pages-monitor.yml", import.meta.url), "utf8");
const pagesDeploymentSmoke = fs.readFileSync(new URL("../scripts/smoke-pages-deployment.mjs", import.meta.url), "utf8");
const browserMonitorWorkflow = fs.readFileSync(new URL("../.github/workflows/browser-monitor.yml", import.meta.url), "utf8");
const releaseWorkflow = fs.readFileSync(new URL("../.github/workflows/release.yml", import.meta.url), "utf8");
assert(releaseWorkflow.includes("dist/share.html") && releaseWorkflow.includes("dist/share.js") && releaseWorkflow.includes("dist/share-projection.js") && releaseWorkflow.includes("dist/sw.js"), "tagged releases should retain the shared viewer and offline shell alongside release metadata");
const verifyWorkflow = fs.readFileSync(new URL("../.github/workflows/verify.yml", import.meta.url), "utf8");
assert(codeqlWorkflow.includes("github/codeql-action/init@99df26d4f13ea111d4ec1a7dddef6063f76b97e9") && codeqlWorkflow.includes("github/codeql-action/analyze@99df26d4f13ea111d4ec1a7dddef6063f76b97e9") && codeqlWorkflow.includes("analyze:\n    name: Analyze JavaScript\n    permissions:\n      actions: read\n      contents: read\n      pull-requests: read\n      security-events: write") && codeqlWorkflow.includes("languages: javascript-typescript") && codeqlWorkflow.includes("queries: security-extended") && codeqlWorkflow.includes("actions: read") && codeqlWorkflow.includes("pull-requests: read") && codeqlWorkflow.includes("security-events: write") && codeqlWorkflow.includes("upload: ${{") && codeqlWorkflow.includes("head.repo.fork") && codeqlWorkflow.includes("'never'") && codeqlWorkflow.includes("'always'") && codeqlWorkflow.includes("persist-credentials: false") && !codeqlWorkflow.includes("pull_request_target") && !codeqlWorkflow.includes("write-all"), "CodeQL should scan the JavaScript surface on pushes, pull requests, and a weekly schedule with job-scoped least-privilege permissions, fork-safe publication behavior, workflow-run metadata access, and immutable action references");
assert(scorecardWorkflow.includes("ossf/scorecard-action@4eaacf0543bb3f2c246792bd56e8cdeffafb205a") && scorecardWorkflow.includes("github/codeql-action/upload-sarif@99df26d4f13ea111d4ec1a7dddef6063f76b97e9") && scorecardWorkflow.includes("analysis:\n    name: Analyze repository posture\n    permissions:\n      actions: read\n      contents: read\n      issues: read\n      id-token: write\n      pull-requests: read\n      security-events: write") && scorecardWorkflow.includes("publish_results: ${{") && scorecardWorkflow.includes("head.repo.fork") && scorecardWorkflow.includes("'false'") && scorecardWorkflow.includes("'true'") && scorecardWorkflow.includes("if: ${{ !(") && scorecardWorkflow.includes("fetch-depth: 0") && scorecardWorkflow.includes("actions: read") && scorecardWorkflow.includes("issues: read") && scorecardWorkflow.includes("id-token: write") && scorecardWorkflow.includes("pull-requests: read") && scorecardWorkflow.includes("security-events: write") && scorecardWorkflow.includes("persist-credentials: false"), "Scorecard should publish repository supply-chain findings on trusted runs, remain fork-safe, and use job-scoped least-privilege access and immutable action references");
assert(pagesBuildWorkflow.includes("id: pages") && pagesBuildWorkflow.includes("steps.pages.outputs.base_url") && pagesBuildWorkflow.includes("Verify GitHub Pages configuration") && pagesBuildWorkflow.includes("Settings → Pages") && pagesBuildWorkflow.indexOf("id: pages") < pagesBuildWorkflow.indexOf("Run canonical production gate") && pagesBuildWorkflow.includes("DEPLOYMENT_MODE: static-pages") && pagesBuildWorkflow.includes("npm run production:check") && pagesBuildWorkflow.includes("PUBLIC_REPOSITORY_URL: https://github.com/${{ github.repository }}") && pagesBuildWorkflow.includes("BUILD_REVISION: ${{ github.sha }}") && pagesBuildWorkflow.includes("Smoke test deployed Pages artifact") && pagesBuildWorkflow.includes("PAGES_DEPLOYMENT_URL: ${{ vars.PUBLIC_ORIGIN || steps.deployment.outputs.page_url }}") && pagesBuildWorkflow.includes("PAGES_EXPECTED_REVISION: ${{ github.sha }}") && pagesBuildWorkflow.includes("BROWSER_SMOKE_EXPECTED_REPOSITORY: https://github.com/${{ github.repository }}") && pagesBuildWorkflow.includes("npm run smoke:pages:deployment"), "Pages deployment should verify activation before building, use the static-pages preflight without server-only secrets, derive crawler URLs before running the canonical production gate, use the current fork repository for contribution links, bind deployed browser smoke to that repository identity, and smoke-test the exact configured public origin and revision after publication");
assert(verifyWorkflow.includes("Verify encrypted backup round trip") && verifyWorkflow.includes("npm run backup:smoke"), "every verification matrix lane should exercise the encrypted backup contract");
assert(pagesMonitorWorkflow.includes("schedule:") && pagesMonitorWorkflow.includes("workflow_dispatch:") && pagesMonitorWorkflow.includes("pages-monitor") && pagesMonitorWorkflow.includes("vars.PUBLIC_ORIGIN") && pagesMonitorWorkflow.includes("github.repository_owner") && pagesMonitorWorkflow.includes("github.event.repository.name") && pagesMonitorWorkflow.includes("ref: ${{ github.event.repository.default_branch }}") && pagesMonitorWorkflow.includes("git rev-parse HEAD") && pagesMonitorWorkflow.includes("PAGES_EXPECTED_REVISION: ${{ steps.revision.outputs.value }}") && pagesMonitorWorkflow.includes("set -o pipefail") && pagesMonitorWorkflow.includes("tee pages-monitor.log") && pagesMonitorWorkflow.includes("actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02") && pagesMonitorWorkflow.includes("pages-monitor-${{ github.run_id }}") && pagesMonitorWorkflow.includes("retention-days: 14") && pagesMonitorWorkflow.includes("npm run smoke:pages:deployment") && pagesMonitorWorkflow.includes("persist-credentials: false"), "published Pages should have a scheduled, manually runnable, read-only monitor with a repository-default URL fallback, default-branch drift detection, and retained failure diagnostics");
assert(browserMonitorWorkflow.includes("schedule:") && browserMonitorWorkflow.includes("workflow_dispatch:") && browserMonitorWorkflow.includes("browser-monitor") && browserMonitorWorkflow.includes("ref: ${{ github.event.repository.default_branch }}") && browserMonitorWorkflow.includes("git rev-parse HEAD") && browserMonitorWorkflow.includes("BROWSER_SMOKE_URL") && browserMonitorWorkflow.includes("BROWSER_SMOKE_CONFIRM") && browserMonitorWorkflow.includes("BROWSER_SMOKE_EXPECTED_REVISION: ${{ steps.revision.outputs.value }}") && browserMonitorWorkflow.includes("BROWSER_SMOKE_EXPECTED_REPOSITORY: https://github.com/${{ github.repository }}") && browserMonitorWorkflow.includes("BROWSER_SMOKE_ARTIFACT_DIR") && browserMonitorWorkflow.includes("playwright install --with-deps") && browserMonitorWorkflow.includes("set -o pipefail") && browserMonitorWorkflow.includes("tee -a browser-monitor.log") && browserMonitorWorkflow.includes("browser-monitor.log") && browserMonitorWorkflow.includes("browser-artifacts") && browserMonitorWorkflow.includes("if-no-files-found: ignore") && browserMonitorWorkflow.includes("browser: [chromium, firefox, webkit]") && browserMonitorWorkflow.includes("npm run browser:smoke") && browserMonitorWorkflow.includes("actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02") && browserMonitorWorkflow.includes("persist-credentials: false"), "published browser behavior should have a scheduled, manually runnable, read-only exact-origin, repository-identity, and default-branch revision monitor across the supported browser matrix with retained failure screenshots and bounded command logs");
assert(releaseWorkflow.includes('tags: ["v*.*.*"]') && releaseWorkflow.includes("validate:\n    name: Validate release artifacts\n    permissions:\n      contents: read\n      id-token: write\n      attestations: write") && releaseWorkflow.includes("browser:\n    name: Certify release workbench (${{ matrix.browser }})\n    needs: validate\n    permissions:\n      contents: read") && releaseWorkflow.includes("release-${{ github.ref }}") && releaseWorkflow.includes("npm run release:tag:check") && releaseWorkflow.includes("npm run production:check") && releaseWorkflow.includes("BUILD_REVISION: ${{ github.sha }}") && releaseWorkflow.includes("actions/attest@a1948c3f048ba23858d222213b7c278aabede763") && releaseWorkflow.includes("subject-path: dist/asset-manifest.json") && releaseWorkflow.includes("sbom-path: sbom.spdx.json") && releaseWorkflow.includes("docker build --build-arg APP_VERSION=\"$version\" --build-arg VCS_REF=\"$GITHUB_SHA\" --build-arg PUBLIC_REPOSITORY_URL=\"$repository\" --build-arg PUBLIC_DOCUMENTATION_URL=\"$documentation\" --tag llm-field-notes:release .") && releaseWorkflow.includes("CONTAINER_EXPECTED_SOURCE: https://github.com/${{ github.repository }}") && releaseWorkflow.includes("CONTAINER_EXPECTED_DOCUMENTATION: https://github.com/${{ github.repository }}/blob/main/RUNBOOK.md") && releaseWorkflow.includes("Smoke test versioned production container") && releaseWorkflow.includes("npm run smoke:container") && releaseWorkflow.includes("CONTAINER_SKIP_BUILD: \"1\"") && releaseWorkflow.includes("CONTAINER_EXPECTED_REVISION") && releaseWorkflow.includes("name: Retain release publication evidence") && releaseWorkflow.includes("release-pages-evidence-${{ github.ref_name }}") && releaseWorkflow.includes("dist/asset-manifest.json") && releaseWorkflow.includes("dist/version.json") && releaseWorkflow.includes("dist/feed.xml") && releaseWorkflow.includes("dist/manifest.webmanifest") && releaseWorkflow.includes("dist/404.html") && releaseWorkflow.includes("dist/robots.txt") && releaseWorkflow.includes("dist/sitemap.xml") && releaseWorkflow.includes("dist/sample-graph.html") && releaseWorkflow.includes("dist/.well-known/security.txt") && releaseWorkflow.includes("retention-days: 90") && releaseWorkflow.includes("name: Certify release workbench (${{ matrix.browser }})") && releaseWorkflow.includes("needs: validate") && releaseWorkflow.includes("browser: [chromium, firefox, webkit]") && releaseWorkflow.includes("BROWSER_SMOKE_LOCAL_REVISION: ${{ github.sha }}") && releaseWorkflow.includes("BROWSER_SMOKE_EXPECTED_REVISION: ${{ github.sha }}") && releaseWorkflow.includes("BROWSER_SMOKE_EXPECTED_REPOSITORY: https://github.com/${{ github.repository }}") && releaseWorkflow.includes("npm run browser:smoke") && releaseWorkflow.includes("actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02") && releaseWorkflow.includes("persist-credentials: false"), "versioned releases should validate the tag, retain complete crawler and sample-graph evidence, attest the manifest and SBOM, preserve source revision in generated artifacts, build a fork-aware traceable image, run reusable hardened container smoke, and certify the release workbench across all supported browser engines with pinned workflow steps");
const productionCheck = fs.readFileSync(new URL("../scripts/production-check.mjs", import.meta.url), "utf8");
assert(productionCheck.includes("environmentForProductionCheck") && productionCheck.includes("STATIC_PUBLICATION_VARIABLES") && productionCheck.includes("Run the complete test and contract suite"), "static Pages publication variables should be isolated from local behavioral and server checks");
assert(runbook.includes("DEPLOYMENT_MODE=static-pages") && runbook.includes("PAGES_DEPLOYMENT_URL") && runbook.includes("server-only"), "the runbook should document the static-pages production gate separately from server authentication");
assert(testRunner.includes("STATIC_PUBLICATION_VARIABLES") && testRunner.includes("STATIC_PUBLICATION_TEST_FILES") && testRunner.includes("isolateStaticPublication") && testRunner.includes("DEPLOYMENT_MODE === \"static-pages\""), "the behavioral test runner should isolate static publication metadata from child tests while preserving artifact commands that intentionally rebuild the publication");
assert(packageManifest.scripts["production:check"] === "node scripts/production-check.mjs" && productionCheck.includes("CHECK_TIMEOUT_MS") && productionCheck.includes("killSignal") && productionCheck.includes("ETIMEDOUT") && productionCheck.includes('"run", "workflows:check"') && productionCheck.includes('"run", "release:check"') && productionCheck.includes('"run", "smoke:server"') && productionCheck.includes('"run", "sbom:generate"') && productionCheck.includes('"run", "verify:sbom"') && productionCheck.includes('"run", "verify:share"') && productionCheck.includes("examples/sample-share.json") && productionCheck.includes("health:sample") && productionCheck.includes("build:pages") && productionCheck.includes("verify:pages") && productionCheck.includes('["Verify the final Pages artifact"') && productionCheck.includes("PAGES_DEPLOYMENT_URL") && productionCheck.includes("PAGES_EXPECTED_REVISION") && productionCheck.includes("^(?:unknown|[0-9a-f]{7,64})$") && productionCheck.includes('"run", "smoke:pages:deployment"') && productionCheck.indexOf('"run", "workflows:check"') < productionCheck.indexOf('"run", "release:check"') && productionCheck.indexOf('"run", "build:pages"') < productionCheck.indexOf('["Run the complete test and contract suite"') && productionCheck.indexOf('"run", "verify:pages"') < productionCheck.indexOf('["Run the complete test and contract suite"') && productionCheck.indexOf('["Verify the final Pages artifact"') > productionCheck.indexOf('["Gate the published sample graph"') && verifyWorkflow.includes("Build Pages artifact for artifact-dependent tests") && verifyWorkflow.includes("Verify Pages artifact for artifact-dependent tests") && verifyWorkflow.includes("Generate dependency SBOM") && verifyWorkflow.includes("dependency-sbom-node-${{ matrix.node-version }}") && verifyWorkflow.indexOf("npm run build:pages") < verifyWorkflow.indexOf("npm test") && verifyWorkflow.indexOf("npm run verify:pages") < verifyWorkflow.indexOf("npm test") && verifyWorkflow.includes("Gate sample graph quality") && verifyWorkflow.includes("npm run health:sample") && releaseWorkflow.includes("npm run production:check") && releaseWorkflow.includes("sbom.spdx.json"), "normal verification and tagged releases should explicitly validate workflow contracts before release metadata, build and verify a fresh Pages artifact, retain a validated dependency inventory, run dist-dependent accessibility/performance tests, and re-verify the final artifact after all tests, while also gating release contracts, standalone server lifecycle, and sample graph health through the canonical production command");
assert(packageManifest.scripts["runtime:check"] === "node scripts/check-runtime.mjs" && fs.existsSync(new URL("../scripts/check-runtime.mjs", import.meta.url)) && fs.readFileSync(new URL("../scripts/check-runtime.mjs", import.meta.url), "utf8").includes("engines") && testRunner.includes('run("Build the Pages artifact for artifact-dependent tests"') && testRunner.includes('run("Verify the Pages artifact for artifact-dependent tests"') && testRunner.indexOf('run("Build the Pages artifact for artifact-dependent tests"') < testRunner.indexOf("for (const file of sourceFiles)") && verifyWorkflow.includes("Verify supported Node runtime") && verifyWorkflow.includes("npm run runtime:check"), "the canonical production gate, standalone npm test command, and every CI runtime lane should fail fast on unsupported runtimes and prepare a verified fresh Pages artifact before artifact-dependent checks");
assert(verifyWorkflow.includes("npm run smoke:container") && verifyWorkflow.includes("fail-fast: false") && verifyWorkflow.includes("test -f /app/experiments/tiny-training.mjs") && verifyWorkflow.includes("test -f /app/scripts/sample-graph-page.mjs") && verifyWorkflow.includes("test -f /app/scripts/verify-share.mjs") && containerSmoke.includes("/api/extract-graph") && containerSmoke.includes("401") && containerSmoke.includes("METRICS_AUTH_TOKEN") && containerSmoke.includes("SIGTERM"), "container CI should use the reusable smoke to verify complete public runtime assets, startup helpers, authentication boundaries, and graceful shutdown while retaining independent Node runtime evidence");
assert(containerSmoke.includes("test ! -e /app/.git") && containerSmoke.includes("test ! -e /app/.codex") && containerSmoke.includes("test ! -e /app/.env") && containerSmoke.includes("test ! -e /app/node_modules") && containerSmoke.includes("test ! -e /app/benchmarks") && containerSmoke.includes("test ! -e /app/tests") && containerSmoke.includes("test ! -e /app/backups") && containerSmoke.includes("test ! -e /app/exports") && containerSmoke.includes("test ! -e /app/sbom.spdx.json") && containerSmoke.includes("test ! -e /app/scripts/check-runtime.mjs") && containerSmoke.includes("test ! -e /app/scripts/smoke-container.mjs") && containerSmoke.includes("test ! -e /app/scripts/load-server.mjs") && containerSmoke.includes("test -f /app/experiments/tiny-training.mjs") && containerSmoke.includes("test -f /app/scripts/public-assets.mjs") && containerSmoke.includes("test -f /app/scripts/sample-graph-page.mjs") && containerSmoke.includes("test -f /app/scripts/verify-share.mjs") && containerSmoke.includes("test -f /app/experiments/verify-backup.mjs"), "the reusable container smoke should reject repository, agent, dependency, private-data, release-evidence, and verification-only leakage while retaining the complete public experiment and startup-helper contracts");
assert(containerSmoke.includes("formatReadinessFailure") && containerSmoke.includes("readBoundedResponseBody") && containerSmoke.includes("reader.cancel"), "container readiness failures should preserve a bounded server diagnostic for actionable CI output");
assert(containerSmoke.includes("PUBLIC_ORIGIN=https://127.0.0.1:${port}") && containerSmoke.includes("process.exit(0)") && containerSmoke.includes("idle undici connection"), "the container smoke should exercise the TLS-at-the-gateway identity contract and terminate cleanly after native fetch probes");
assert(fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("package-lock.json") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("lockedRoot") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("dependencyField") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("out of sync with package.json") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("lstat") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("must not be a symbolic link") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("lstat") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("public asset must not be a symbolic link") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("toISOString().slice(0, 10)") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("readServiceWorkerShellAssets") && fs.readFileSync(new URL("../scripts/service-worker-cache.mjs", import.meta.url), "utf8").includes("valid JSON string array") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("PUBLIC_ASSETS") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("LEARNING_NOTE_ASSETS") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("browserNoteIds") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("browserNoteTitles") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("browserNoteQuestions") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("expectedCurriculumCounts") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("curriculum filter count is out of sync") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("noScriptNoteIds") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("noScriptNoteTitles") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("no-script curriculum title is out of sync") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("curriculum title is out of sync") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("curriculum question is out of sync") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("MAX_STATIC_ASSET_BYTES") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("MAX_PUBLIC_ASSET_BYTES") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("MAX_LEARNING_NOTE_ASSETS") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("metadata.isFile()") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("realpath") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("isContained") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("dockerignoreEntries") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("without an allow-list exception") && fs.readFileSync(new URL("../scripts/check-workflows.mjs", import.meta.url), "utf8").includes("workflowDirectory") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("digest-pinned Node 22 Alpine") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("graphCorrectionTemplate") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("I removed private source text") && dockerfile.includes("org.opencontainers.image.description") && dockerfile.includes("org.opencontainers.image.revision") && dockerfile.includes("org.opencontainers.image.source") && dockerfile.includes("org.opencontainers.image.documentation") && dockerfile.includes("org.opencontainers.image.licenses"), "release checks should validate package-lock dependency declarations, dates, bounded public assets, repository identity, title, question, filter-count, and no-script title parity, graph-correction contribution safety, per-asset bounds, runtime baseline, complete OCI traceability metadata, every workflow, and source symlink rejection");
assert(fs.readFileSync(new URL("../experiments/bounded-file.mjs", import.meta.url), "utf8").includes('TextDecoder("utf-8", { fatal: true })') && server.includes('TextDecoder("utf-8", { fatal: true })') && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes('TextDecoder("utf-8", { fatal: true })') && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("does not expose raw bytes for safe decoding") && !fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("const payload = await response.json()"), "external text boundaries should reject malformed UTF-8 instead of silently replacing bytes");
assert(app.includes("response.arrayBuffer") && app.includes('new TextDecoder("utf-8", { fatal: true })'), "browser text fallbacks should use byte buffers and fatal UTF-8 decoding when available");
assert(app.includes("function downloadFile(filename, content, type, maxBytes = MAX_EXPORT_BYTES)") && app.includes('downloadFile("llm-field-notes-encrypted-backup.json", content, "application/json", MAX_ENCRYPTED_BACKUP_BYTES)'), "encrypted backup downloads should use their larger bounded envelope ceiling instead of the plaintext export limit");
assert(app.includes("const flushDocumentDraft =") && app.includes("saveDocumentDraft();") && app.includes("window.addEventListener(\"pagehide\", flushDocumentDraft)") && app.includes("document.addEventListener(\"freeze\", flushDocumentDraft)"), "draft recovery should flush pending autosaves before page eviction and browser freezing");
assert(app.includes('window.addEventListener("offline", () =>') && app.includes("activeExtractionController?.abort()"), "offline transitions should cancel in-flight remote extraction without canceling local work");
assert(app.includes("if (browserIsOffline())") && app.includes("Remote extraction was canceled because the browser is offline."), "each remote extraction request should recheck connectivity before starting after a batch or rebuild boundary");
assert(app.includes("Remote build canceled because the browser went offline") && app.includes("Remote build stopped because the browser went offline") && app.includes("Rebuild stopped because the browser went offline") && app.includes("Source replacement stopped because the browser went offline"), "offline-triggered extraction cancellation should remain distinguishable across every remote workbench operation");
assert(app.includes("readerCancellationRequested") && app.includes("else cancelReader();"), "browser streamed response readers should cancel pending reads on every exit path without duplicating cancellation");
assert(server.includes("const decodeUtf8 =") && server.includes("decodeUtf8(await readBoundedFile"), "runtime note rendering and readiness should use fatal UTF-8 decoding");
assert(app.includes("async function readBrowserFileBytes") && app.includes("async function readBrowserFileText") && app.includes("MAX_DOCUMENT_BYTES = MAX_DOCUMENT_CHARS * 4") && app.includes("MAX_BATCH_BYTES = MAX_BATCH_CHARS * 4") && app.includes("MAX_FEEDBACK_NOTE_BYTES = MAX_FEEDBACK_NOTE_CHARS * 4") && app.includes("file.slice(0, byteLimit + 1)") && app.includes("if (typeof file?.slice !== \"function\") throw") && app.includes(".arrayBuffer") && app.includes("That file is not valid UTF-8.") && app.includes("This browser cannot validate file size safely.") && app.includes("sliceTextAtCodePointBoundary") && !app.includes("await file.text()") && !app.includes("return file.text()"), "browser file imports and UI projections should use bounded pre-read slices, fatal UTF-8 decoding, and code-point-safe text boundaries");
assert(app.includes("ArrayBuffer.isView(result.value)") && app.includes("Number.isSafeInteger(result.value.byteLength)") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("function asByteView") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("chunks.push(chunk)"), "browser and remote streamed response readers should normalize actual byte views before accounting against size limits");
assert(app.includes("response byte length does not match Content-Length") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("function responseLengthMismatch") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("size !== declaredLength"), "browser and remote response readers should reject truncated bodies when declared and received byte lengths differ");
assert(app.includes("The response Content-Length is invalid.") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("function invalidResponseLength") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("Extractor response Content-Length is invalid."), "browser and remote response readers should reject malformed or unsafe Content-Length headers");
assert(app.includes("The response returned an HTML document instead of text data.") && app.includes("application/xhtml+xml"), "browser text readers should reject same-origin HTML gateway responses before importing them as learning content");
assert(app.includes("const cancelTextResponseBody =") && fs.readFileSync(new URL("../tests/browser-response-smoke.mjs", import.meta.url), "utf8").includes("htmlResponseCancelCalls"), "browser response metadata failures should cancel unread response bodies before surfacing");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("await cancelResponseBody(response)") && fs.readFileSync(new URL("../tests/extractor-smoke.mjs", import.meta.url), "utf8").includes("malformedLengthBodyCancelCalls"), "remote response metadata failures should cancel unread provider bodies before surfacing");
assert(app.includes("assertVaultGraphUnchanged") && app.includes("while the Obsidian vault was being prepared"), "asynchronous Obsidian vault exports should reject mixed graph revisions before download");
assert(app.includes("MAX_LEARNING_VAULT_EXPORT_MS = 30000") && app.includes("const exportDeadline = Date.now() + MAX_LEARNING_VAULT_EXPORT_MS") && app.includes("const remainingMs = exportDeadline - Date.now()") && app.includes("fetchWithAbortDeadline"), "Obsidian learning-note exports should enforce an aggregate deadline and explicitly settle note fetches instead of multiplying per-note timeouts");
assert(serviceWorker.includes(`const CACHE = "llm-field-notes-v${packageManifest.version}"`), "service worker cache version should track the release version");
assert(serviceWorker.includes("./LICENSE"), "service worker should precache the public license");
assert(serviceWorker.includes("./SECURITY.md") && serviceWorker.includes("./RUNBOOK.md"), "service worker should precache public security and operational guidance");
assert(serviceWorker.includes("./social-card.svg"), "service worker should precache the social share card");
assert(serviceWorker.includes("./CODE_OF_CONDUCT.md"), "service worker should precache community guidance");
assert(serviceWorker.includes("./CONTRIBUTING.md"), "service worker should precache the contribution entry point");
assert(serviceWorker.includes("./curriculum.js") && curriculum.includes("export const notes") && curriculum.includes("export const noteDetails") && serviceWorker.includes("./notes/tokens.md") && serviceWorker.includes("./notes/production.md") && serviceWorker.includes("./notes/knowledge-graphs.md"), "curriculum metadata should be a published shared module and service worker asset");
assert(html.includes('id="connection-status"') && html.includes("remote extraction will resume") && app.includes('querySelector("#connection-status")') && app.includes('addEventListener("online"') && app.includes('addEventListener("offline"') && app.includes("browserIsOffline") && app.includes("clear the endpoint to use local extraction"), "the workbench should disclose offline local-first behavior and fail fast before remote extraction retries");
assert(app.includes("MAX_OPERATION_DIAGNOSTIC_CHARS") && app.includes("boundedOperationDiagnostic") && app.includes("could not load") && app.includes("could not extract"), "browser operation diagnostics should be bounded and normalized before user-visible failure summaries");
assert(app.includes("The undo history recovery could not be downloaded.") && app.includes("The recovery snapshot could not be downloaded.") && app.match(/boundedOperationDiagnostic\(\s*error,\s*"The undo history recovery could not be downloaded\."/s) && app.match(/boundedOperationDiagnostic\(\s*error,\s*"The recovery snapshot could not be downloaded\."/s), "recovery download failures should use the bounded diagnostic normalizer");
assert.equal((app.match(/error instanceof Error\s*\?\s*error\.message/g) || []).length, 1, "browser UI error paths should not bypass the bounded diagnostic normalizer with raw error messages");
assert(app.includes("const commitDocumentDraft =") && app.includes("clearTimeout(draftSaveTimer)") && app.includes("draftSaveTimer = null"), "successful document commits should cancel pending draft-save timers before clearing recovery state");
assert(app.includes('addEventListener("beforeunload"') && app.includes("graphHasContent") && app.includes("storageDurabilityFailure") && app.includes("documentDraftKey") && app.includes("restoreStoredDocumentDraft") && app.includes("saveDocumentDraft") && app.includes("readDocumentDraft") && app.includes("hasUnsavedDocumentDraft") && app.includes("commitDocumentDraft") && app.includes("Recovered an unfinished document draft") && app.includes("queuedFiles") && app.includes('event.returnValue = ""'), "ephemeral graph storage, recoverable document drafts, and in-memory queued files should warn before tab closure");
assert(app.includes('document.addEventListener("freeze", flushBrowserStorage)'), "browser lifecycle freezing should trigger a durable-storage flush");
assert(app.includes("hasPersistentStorage && hasDurableStorage") && app.includes("const fallbackStorage = hasPersistentStorage && !hasDurableStorage") && app.includes("browser storage may be evicted") && app.includes("fallbackStorage || reducedHistory"), "fallback-only browser storage should warn before unload and offer a backup");
assert(html.includes('id="retry-queued-files"') && app.includes("syncRetryQueuedFilesButton") && app.includes("pendingFiles.length === 0") && app.includes("ingestButton.click()") && app.includes('dataset.state = offline ? "offline" : queued ? "queued"'), "retained batch files should expose an explicit retry action with distinct queued/offline status");
assert(html.includes('id="retry-build"') && app.includes("setRetryBuildAvailable") && app.includes('["NETWORK_ERROR", "REMOTE_ERROR", "TIMEOUT"].includes(error?.code)') && app.includes("retryBuildButton?.addEventListener"), "retryable single-document model failures should expose an explicit retry action while preserving the editor draft");
assert(llms.includes("./notes/knowledge-graphs.md") && readme.includes("knowledge-graph note"), "machine-readable and human project maps should expose the knowledge-graph learning loop");
assert(serviceWorker.includes("cache: \"no-cache\"") && serviceWorker.includes("AbortController") && serviceWorker.includes("NETWORK_TIMEOUT_MS") && serviceWorker.includes("fetchPromise") && serviceWorker.includes("timeoutPromise") && serviceWorker.includes("fetchTimedOut") && serviceWorker.includes("CACHE_OPERATION_TIMEOUT_MS") && serviceWorker.includes("withCacheTimeout") && serviceWorker.includes("MAX_RESPONSE_BYTES") && serviceWorker.includes("response.clone().body.getReader") && serviceWorker.includes("reader.cancel") && serviceWorker.includes("bodyReadComplete") && serviceWorker.includes("reader.releaseLock") && serviceWorker.includes("precacheShell") && serviceWorker.includes("new Request(new URL(asset, self.location)") && serviceWorker.includes("if (!response.ok)") && serviceWorker.includes("status !== 200") && serviceWorker.includes("matchShellCache") && serviceWorker.includes("cache.match") && serviceWorker.includes("return null") && serviceWorker.includes("if (networkResponse) return networkResponse"), "service worker should explicitly bound fetch settlement and cache operations for install and runtime shell responses, reject partial successful assets, release completed body readers, and survive cache read failures during offline or transient HTTP fallback");
assert(serviceWorker.includes("function cancelResponseBody") && serviceWorker.includes("const rejectResponse =") && serviceWorker.includes("cancelResponseBody(response)"), "service-worker early response validation failures should cancel unread response bodies");
assert(serviceWorker.includes("networkResponse.status >= 400 && networkResponse.status < 500"), "service worker should preserve online client-error navigations such as the deployed 404 page");
assert(serviceWorker.includes("Network response body length does not match Content-Length") && serviceWorker.includes("body is unavailable for bounded validation") && serviceWorker.includes("response.status >= 400 && response.status < 500"), "service worker should reject truncated or unreadable cacheable shell responses while preserving online client errors");
assert(serviceWorker.includes("isHtmlShellPath") && serviceWorker.includes("ArrayBuffer.isView(result.value)") && serviceWorker.includes("HTML response received for a non-HTML shell asset") && serviceWorker.includes("Non-HTML response received for an HTML shell asset"), "service worker should validate actual response byte views and reject representation mismatches before caching shell assets");
const installHandler = serviceWorker.match(/self\.addEventListener\("install"[\s\S]*?\n\}\);/)?.[0] || "";
assert(!installHandler.includes("skipWaiting") && serviceWorker.includes("SKIP_WAITING"), "service-worker updates should not take over an active tab without explicit approval");
assert(serviceWorker.includes("isShellRequest") && serviceWorker.includes("isCacheableShellRequest") && serviceWorker.includes("SHELL_PATHS") && serviceWorker.includes("CACHE_PREFIX") && serviceWorker.includes("startsWith(CACHE_PREFIX)"), "service worker should restrict interception, cache writes, and cache cleanup to this app");
assert(app.includes("let serviceWorkerManager = null") && app.includes("serviceWorkerManager = navigator.serviceWorker") && app.includes("Service-worker support is optional") && app.includes('typeof serviceWorkerManager?.register === "function"') && app.includes('typeof serviceWorkerManager?.addEventListener === "function"') && app.includes('serviceWorkerManager.register("./sw.js", { updateViaCache: "none" })') && app.includes("registration.update()") && app.includes("reload-update") && app.includes("SKIP_WAITING") && app.includes("SERVICE_WORKER_UPDATE_TIMEOUT_MS") && app.includes("removeEventListener(\"controllerchange\"") && app.includes("The update did not activate yet"), "the app should safely handle restricted service-worker getters and apply updates without stale HTTP cache entries, leaked listeners, or a permanently stuck update control");
assert(app.includes("try {\n              worker.postMessage({ type: \"SKIP_WAITING\" });") && app.includes("A terminated or restricted first-install worker"), "first-install service-worker activation failures must not surface as workbench errors");
assert.equal(graphSchema.properties.schema.const, "llm-field-notes/graph@1");
assert.equal(graphSchema.properties.revisions.maxItems, 20, "graph schema should bound retained revisions");
assert.equal(graphSchema.properties.learning.properties.examples.maxItems, 500, "graph schema should bound reusable learning memory");
assert.equal(graphSchema.$defs.node.properties.lastReviewedAt.maxLength, 128, "concept review timestamps should be bounded");
assert.equal(graphSchema.$defs.edge.properties.lastReviewedAt.maxLength, 128, "relation review timestamps should be bounded");
assert.equal(graphSchema.$defs.learningExample.properties.lastReviewedAt.maxLength, 128, "learning review timestamps should be bounded");
assert.equal(feedbackSchema.properties.examples.items.properties.lastReviewedAt.maxLength, 128, "feedback review timestamps should be bounded");
assert.equal(graphSchema.properties.integrity.properties.ambiguousSourceIds.maxItems, 100, "graph schema should bound provenance ambiguity diagnostics");
assert.equal(graphSchema.properties.integrity.properties.ambiguousEdgeIds.maxItems, 100, "graph schema should bound edge identity ambiguity diagnostics");
assert.equal(graphSchema.properties.integrity.properties.conflictingNodeIds.maxItems, 100, "graph schema should bound contradictory concept diagnostics");
assert.equal(graphSchema.properties.integrity.properties.conflictingEdgeIds.maxItems, 100, "graph schema should bound contradictory relation diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.documents.maximum, 4294967295, "graph schema should preserve bounded document truncation diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.learningExamples.maximum, 4294967295, "graph schema should preserve bounded learning truncation diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.documentText.maximum, 4294967295, "graph schema should preserve clipped document-text diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.documentTitle.maximum, 4294967295, "graph schema should preserve clipped document-title diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.evidenceText.maximum, 4294967295, "graph schema should preserve clipped evidence-text diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.evidenceItems.maximum, 4294967295, "graph schema should preserve omitted evidence-item diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.sourceReferences.maximum, 4294967295, "graph schema should preserve omitted provenance-reference diagnostics");
assert.equal(graphSchema.properties.integrity.properties.truncated.properties.aliases.maximum, 4294967295, "graph schema should preserve clipped alias diagnostics");
assert.equal(graphSchema.properties.integrity.properties.dropped.properties.edges.maximum, 4294967295, "graph schema should preserve bounded malformed-edge diagnostics");
assert.equal(graphSchema.properties.version.maximum, 9007199254740991, "graph schema should bound versions to safe integers");
assert(diffSchema.properties.integrity.properties.conflictingNodeIds && diffSchema.properties.integrity.properties.conflictingEdgeIds, "diff schema should preserve contradictory review diagnostics");
assert(graphCore.includes("MAX_FEEDBACK_FINGERPRINT_EXAMPLES") && graphCore.includes("asBoundedArray(value, MAX_FEEDBACK_FINGERPRINT_EXAMPLES)"), "feedback fingerprint canonicalization should bound imported examples");
assert(graphCore.includes("parseJsonWithUniqueKeys")
  && server.includes("parseJsonWithUniqueKeys")
  && app.includes("parseJsonWithUniqueKeys")
  && graphStore.includes("parseJsonWithUniqueKeys")
  && projection.includes("parseJsonWithUniqueKeys")
  && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("parseJsonWithUniqueKeys"), "JSON request, browser, persistence, projection, and provider boundaries should reject duplicate object keys");
assert(graphCore.includes("MAX_JSON_DEPTH") && graphCore.includes("depth > MAX_JSON_DEPTH"), "JSON parsing should bound nesting depth before recursive validation");
assert(graphCore.includes("MAX_JSON_KEY_DIAGNOSTIC_CHARS") && graphCore.includes("diagnosticKey"), "JSON parser diagnostics should bound duplicate-key text before returning API errors");
assert(fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("parseJsonWithUniqueKeys") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("Pending durable writes") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("PENDING_WRITE_MARKER_PREFIX") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("readPendingWriteMarkers") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("MAX_PENDING_WRITES") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("entries.some"), "durable pending-write metadata should validate its complete bounded entry contract, cap per-key generations, and isolate them before hydration");
assert(server.includes("SERVER_DRAINING") && server.includes("activeExtractorDrainRejectors") && server.includes("serverDrainPromise") && server.includes('"retry-after": "5"') && server.includes('error: "Server is draining."'), "graceful shutdown should settle stubborn in-flight extraction and return a retryable draining response");
assert(app.includes("MAX_SEARCH_QUERY_CHARS") && app.includes("boundedSearchQuery") && html.includes('id="graph-search" class="graph-search" type="search" maxlength="256"') && html.includes('id="search" type="search" maxlength="256"'), "browser search surfaces should cap query work before repeated graph or note scans");
assert(app.includes("async function readBrowserFileBytes") && app.includes("file.slice(0, byteLimit + 1)") && app.includes("parseObsidianVault(vaultBytes)"), "browser file imports should slice before reading so oversized vaults cannot allocate unbounded input buffers");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("compactFeedbackHint") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_FEEDBACK_LABEL_CHARS"), "remote extractor requests should compact feedback to the declared request contract");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_REQUEST_BYTES") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("byteLength(requestBody)") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("Promise.race([response.arrayBuffer(), abortPromise])"), "remote extractor requests and non-streaming response reads should enforce bounded byte and timeout contracts before or during network I/O");
assert(graphCore.includes("boundedHistoryForFingerprint") && graphCore.includes("history: boundedHistoryForFingerprint(history)"), "backup fingerprints should bind the newest bounded history window");
assert(graphCore.includes("MAX_BACKUP_HISTORY") && graphCore.includes("Backup history cannot contain more than"), "backup construction should fail closed at the schema-aligned history ceiling");
assert(graphCore.includes("oldestTimestamp") && graphCore.includes("canonicalEdgesByKey"), "duplicate graph records should collapse with deterministic metadata and relation representatives");
assert(graphCore.includes("retainNewestDocuments") && graphCore.includes("compareDocumentRetention") && graphCore.includes("makeDocumentRetentionCandidate") && graphCore.includes("boundedDocumentTimestamp") && graphCore.includes("heap.length < MAX_GRAPH_DOCUMENTS"), "oversized document imports should retain the newest source window with bounded intermediate memory and precomputed identity inputs");
assert(graphCore.includes("const originalGraph = normalizeGraph(graph)") && graphCore.includes("hitVersionLimit") && graphCore.includes("limited: \"version\""), "feedback dataset imports should roll back atomically at the revision ceiling");
assert(graphCore.includes("const incomingIsCanonical") && graphCore.includes("const labels = [...new Set([existing.label, node.label"), "duplicate extraction concepts should use the same deterministic canonicalization rule as graph imports");
assert(evaluation.includes("aliases.slice(0, MAX_ALIASES)") && evaluation.includes("MAX_ID_CHARS") && evaluation.includes("validateEvaluationReport"), "evaluation matching and promotion reports should enforce bounded runtime contracts");
assert(evaluation.includes("assertRoundedRatio") && evaluation.includes("is inconsistent with its counts"), "evaluation validation should reconcile reported ratios with their bounded counts before promotion");
assert(evaluation.includes("MAX_TIMESTAMP_CHARS") && evaluation.includes("parseTimestamp(value.evaluatedAt)"), "evaluation validation should enforce bounded machine-readable provenance timestamps");
assert(evaluation.includes("DATE_TIME_PATTERN"), "evaluation validation should require full date-time provenance rather than date-only strings");
assert(evaluation.includes("assertKnownKeys") && evaluation.includes("is not allowed"), "evaluation validation should enforce the closed published report schema");
assert(evaluation.includes("feedback exceeds") && evaluation.includes("value.length > MAX_EVALUATION_EXAMPLES"), "evaluation should fail closed on oversized reviewed datasets instead of silently truncating benchmark inputs");
assert(evaluation.includes("MAX_GRAPH_NODES") && evaluation.includes("MAX_GRAPH_EDGES") && evaluation.includes("extraction exceeds"), "evaluation should fail closed on oversized candidate graph output instead of silently truncating it");
assert(evaluation.includes("assertReviewableExamples") && evaluation.includes("not a reviewed concept or relation"), "evaluation should fail closed on malformed or unreviewed benchmark examples instead of silently filtering them");
assert(projection.includes("MAX_DIRECT_FEEDBACK_ITEMS") && projection.includes("boundFeedbackMutation") && projection.includes("boundedFeedbackAliases") && projection.includes("rawFeedback"), "direct Obsidian feedback mutation should normalize raw collections and fields before mutation");
assert(projection.includes("const originalGraph = normalizeGraph(graph)") && projection.includes("if (!advanceGraphVersion(graph))"), "Obsidian feedback should fail closed if its final atomic revision cannot advance");
assert(jsonLdProjection.includes("MAX_JSONLD_CANONICAL_DEPTH") && jsonLdProjection.includes("depth > MAX_JSONLD_CANONICAL_DEPTH") && jsonLdProjection.includes("catch"), "JSON-LD verification should bound nesting and fail closed");
assert.equal(vaultManifestSchema.properties.format.const, "llm-field-notes/vault@1", "vault manifests should have a versioned schema contract");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("MAX_VAULT_MANIFEST_CHARS"), "vault manifest parsing should have a dedicated pre-parse size bound");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("normalizeFeedbackTimestamp"), "direct Obsidian feedback should share the parsed-note timestamp validation boundary");
assert(projection.includes("if (incoming === null) return false"), "Obsidian projection imports must not erase trusted review chronology with an empty timestamp");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("OBSIDIAN_FRONTMATTER_FIELDS") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("OBSIDIAN_FIELDS_BY_TYPE") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("MAX_NODE_MENTIONS"), "Obsidian feedback should enforce a closed type-specific frontmatter contract");
assert.equal(vaultManifestSchema.properties.graphFingerprint.pattern, "^fnv64-[0-9a-f]{16}-[0-9]+$", "vault manifests should bind exports to graph fingerprints");
assert.equal(vaultManifestSchema.properties.graphFingerprint.maxLength, 128, "vault manifest fingerprints should be bounded");
assert.equal(vaultManifestSchema.properties.appVersion.maxLength, 64, "vault manifests should retain bounded producer-version metadata");
assert.equal(vaultManifestSchema.properties.learning.properties.unavailable.maxItems, 1000, "vault manifests should bound unavailable learning-note diagnostics");
assert.equal(vaultManifestSchema.properties.learning.properties.complete.type, "boolean", "vault manifests should disclose learning-projection completeness");
assert(vaultManifestSchema.additionalProperties === false && vaultManifestSchema.properties.learning.additionalProperties === false && projection.includes("validManifestKeys") && projection.includes("hasOnlyManifestKeys") && projection.includes("hasOnlyLearningKeys"), "vault manifest imports should enforce the schema's closed manifest and learning-key contracts");
assert.equal(jsonLdSchema.properties.format.const, "llm-field-notes/jsonld@1", "JSON-LD projections should declare a versioned contract");
assert.equal(jsonLdSchema.properties.fingerprint.pattern, "^fnv64-[0-9a-f]{16}-[0-9]+$", "JSON-LD projections should bind to graph fingerprints");
assert.equal(jsonLdSchema.properties.appVersion.maxLength, 64, "JSON-LD projections should retain bounded producer-version metadata");
assert.equal(diffSchema.properties.fromFingerprint.pattern, "^fnv64-[0-9a-f]{16}-[0-9]+$", "diff schema should bind the source graph fingerprint");
assert.equal(diffSchema.properties.toFingerprint.pattern, "^fnv64-[0-9a-f]{16}-[0-9]+$", "diff schema should bind the target graph fingerprint");
assert.equal(jsonLdSchema.properties.integrity.$ref, "#/$defs/integrity", "JSON-LD projections should expose graph integrity diagnostics");
assert.equal(jsonLdSchema.$defs.integrityIds.maxItems, 100, "JSON-LD integrity identifiers should remain bounded");
assert.equal(jsonLdSchema.$defs.diagnosticCounts.properties.sourceReferences.maximum, 4294967295, "JSON-LD integrity counts should preserve bounded provenance diagnostics");
assert.equal(jsonLdSchema.$defs.diagnosticCounts.properties.documentTitle.maximum, 4294967295, "JSON-LD integrity counts should preserve clipped document-title diagnostics");
assert.equal(jsonLdSchema.$defs.evidence.maxItems, 8, "JSON-LD evidence arrays should preserve the retained evidence-record bound");
assert.equal(diffSchema.$defs.item.properties.evidenceCount.maximum, 8, "diff evidence counts should preserve the retained evidence-record bound");
assert.equal(healthSchema.properties.reviewQueue.items.properties.evidence.maximum, 8, "health review evidence counts should preserve the retained evidence-record bound");
assert.equal(jsonLdSchema.properties["@graph"].items.$ref, "#/$defs/member", "JSON-LD schemas should validate typed graph members");
assert.equal(jsonLdSchema.properties["@graph"].maxItems, 16520, "JSON-LD projections should reserve bounded members for revision history");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.const === "schema:CreativeWork")), "JSON-LD schemas should describe source members");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.const === "lfn:Relation")), "JSON-LD schemas should describe relation members");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.contains?.const === "lfn:Concept")), "JSON-LD schemas should describe concept members");
assert(jsonLdSchema.$defs.member.oneOf.filter((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.const === "lfn:Relation" || part.properties?.["@type"]?.contains?.const === "lfn:Concept")).every((variant) => variant.allOf?.some((part) => part.required?.includes("lfn:sources"))), "JSON-LD concept and relation members should preserve explicit provenance references");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.required?.includes("lfn:addedAt"))), "JSON-LD source members should preserve creation chronology");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.required?.includes("lfn:createdAt") && part.required?.includes("lfn:updatedAt"))), "JSON-LD concept members should preserve update chronology");
assert.equal(jsonLdSchema.properties.learningExampleCount.maximum, 500, "JSON-LD roots should bound reusable learning memory");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.const === "lfn:LearningExample")), "JSON-LD schemas should describe reusable learning members");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.const === "lfn:Revision")), "JSON-LD schemas should describe revision audit members");
assert(fs.readFileSync(new URL("../jsonld-projection.js", import.meta.url), "utf8").includes('"sourceConcept": { "@id": "lfn:sourceConcept", "@type": "@id" }'), "JSON-LD context should declare learning endpoint references");
assert(jsonLdSchema.$defs.member.oneOf.some((variant) => variant.allOf?.some((part) => part.properties?.["lfn:sourceLabel"] && part.properties?.["lfn:targetLabel"])), "JSON-LD learning relations should preserve endpoint labels");
const learningMember = jsonLdSchema.$defs.member.oneOf.find((variant) => variant.allOf?.some((part) => part.properties?.["@type"]?.const === "lfn:LearningExample"));
assert(learningMember?.allOf?.some((part) => part.oneOf?.some((branch) => branch.properties?.["lfn:kind"]?.const === "concept" && branch.required?.includes("lfn:concept"))), "JSON-LD learning concepts should require concept references");
assert(learningMember?.allOf?.some((part) => part.oneOf?.some((branch) => branch.properties?.["lfn:kind"]?.const === "relation" && branch.required?.includes("lfn:sourceLabel") && branch.required?.includes("lfn:targetLabel"))), "JSON-LD learning relations should require endpoint references and labels");
assert.equal(jsonLdSchema.$defs.memberBase.properties["@id"].maxLength, 2000, "JSON-LD member identifiers should allow bounded escaped graph IDs");
assert.equal(jsonLdSchema.$defs.sourceRef.items.maxLength, 2000, "JSON-LD provenance references should match bounded escaped graph IDs");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("MAX_GRAPH_VERSION") && fs.readFileSync(new URL("../app.js", import.meta.url), "utf8").includes("advanceGraphVersion"), "graph mutations should guard safe-integer revision overflow");
assert(graphCore.includes("existing.lastReviewedAt = newestTimestamp(existing.lastReviewedAt, node.lastReviewedAt)") && graphCore.includes("lastReviewedAt: newestTimestamp(existing.lastReviewedAt, edge.lastReviewedAt)") && graphCore.includes("existing.lastReviewedAt = newestTimestamp(existing.lastReviewedAt, nextEdge.lastReviewedAt)"), "duplicate graph items and merged relations should preserve the newest review timestamps");
assert(graphCore.includes("invalidatedNodeIds") && graphCore.includes("invalidatedEdgeIds") && graphCore.includes("lastReviewedAt: null"), "source mutations should invalidate review dates for affected graph items");
assert(graphCore.includes("collisionDigest") && graphCore.includes("appendIdSuffix(collisionBase"), "conflicting source IDs should use deterministic collision repair instead of random IDs");
assert(graphCore.includes("const ordered = edges.map") && graphCore.includes("const used = new Set()") && graphCore.includes("relationSemanticKey(left.edge.source, left.edge.target, left.edge.label)"), "duplicate relation-ID suffixes should be assigned by stable global semantic ordering");
assert(graphCore.includes("!ambiguousEdgeIds.has(example.id)") && graphCore.includes("canonicalEdgesByKey.get(relationSemanticKey(example.source, example.target, example.label))"), "ambiguous relation learning references should resolve only through semantic identity");
assert(graphCore.includes("const normalizeAliases") && graphCore.includes("existing.aliases || []") && graphCore.includes(".sort(lexicalCompare)"), "alias collections should be canonicalized before graph fingerprints and merges");
assert(graphCore.includes("const INTEGRITY_DIAGNOSTIC_KEYS") && graphCore.includes("diffIntegrityCounts") && graphCore.includes("integrity.truncated.changed") && graphCore.includes("integrity.dropped.changed"), "graph diffs should disclose bounded truncation and malformed-entry diagnostic changes");
assert(graphCore.includes("const normalizeSourceIds") && graphCore.includes(".sort(lexicalCompare)") && graphCore.includes("MAX_SOURCE_REFERENCES"), "provenance source-reference collections should be canonicalized before graph fingerprints");
assert(fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("isValidBroadcastValue(value)") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("isValidBroadcastValue(cursor.value)") && fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8").includes("Storage value exceeds the safety limit."), "browser storage hydration and writes should enforce bounded keys and values");
assert(graphCore.includes("canonicalizeEvidenceForFingerprint") && graphCore.includes("sliceTextAtCodePointBoundary") && graphCore.includes("evidence: canonicalizeEvidenceForFingerprint"), "evidence record ordering and bounded human text should be canonicalized safely without changing runtime display order");
assert(projection.includes("sliceTextAtCodePointBoundary") && jsonLdProjection.includes("sliceTextAtCodePointBoundary") && evaluation.includes("sliceTextAtCodePointBoundary"), "projection, JSON-LD, and evaluation boundaries should reuse code-point-safe text limits");
assert(graphCore.includes("export function canonicalizeGraphForExport") && graphCore.includes("documents: [...graph.documents].sort") && graphCore.includes("nodes: [...graph.nodes].sort") && graphCore.includes("edges: [...graph.edges].sort"), "external graph projections should use one canonical top-level collection ordering");
assert(graphCore.includes("export function compareGraphFreshness") && graphCore.includes("candidate.committedAt") && graphCore.includes("const { committedAt, ...fingerprintGraph }") && graphCore.includes("Undo and restore intentionally move back") && graphStore.includes("const commitTimestamp") && graphStore.includes("normalized.committedAt") && app.includes("compareGraphFreshness(incomingGraph, lastRenderedGraphState.graph)") && app.includes("Ignored a stale graph update"), "cross-tab graph synchronization should account for committed undo/restore rollbacks without changing graph identity");
assert(graphCore.includes("const byText = new Map()") && graphCore.includes("existing.sources = mergeSourceIds(existing.sources, item.sources)"), "duplicate evidence records should merge provenance before consuming the bounded evidence budget");
assert(graphCore.includes("MAX_EVIDENCE_INPUT_ITEMS") && graphCore.includes("asBoundedArray(value, MAX_EVIDENCE_INPUT_ITEMS)"), "evidence normalization should use a separate bounded input scan budget");
assert(graphCore.includes("MAX_EVIDENCE_RECORDS") && graphCore.includes("slice(0, MAX_EVIDENCE_RECORDS)") && !graphCore.includes("slice(0, 8)"), "evidence normalization and merging should share the published evidence-record bound");
assert(graphCore.includes("ambiguousSourceIds: [...ambiguousSourceIds].sort(lexicalCompare)") && graphCore.includes("ambiguousEdgeIds: [...ambiguousEdgeIds].sort(lexicalCompare)"), "integrity diagnostic collections should use stable export ordering");
assert(graphCore.includes("setRemappedLearning") && graphCore.includes("preferLearningExample(existing, example)"), "concept merges should preserve the newest review timestamp for collapsed learning examples");
assert(graphCore.includes("existing.updatedAt = newestTimestamp(existing.updatedAt, node.updatedAt)"), "duplicate concept normalization should preserve the newest update timestamp");
assert(graphCore.includes("preferLearningExample") && graphCore.includes("incomingTime > existingTime") && graphCore.includes("learningExampleTieBreak") && graphCore.includes("Number.isNaN(existingTime)"), "all learning-memory write paths should resolve duplicate decisions by review freshness and stable untimestamped tie breaks");
assert(graphCore.includes("normalizedExamples.sort") && graphCore.includes("leftTime - rightTime"), "feedback imports should apply learning examples in stable recency order before bounded retention");
assert(graphCore.includes("contradictory reviewed decision") && graphCore.includes("selected values by review freshness or deterministic tie-breaking"), "feedback conflict decisions should remain auditable in revision history");
assert(graphCore.includes("function sameDocumentContent") && !graphCore.includes("document.fingerprint && document.fingerprint === extraction.source.fingerprint"), "source identity checks should compare canonical content instead of trusting custom fingerprints");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("valid.length > historyCapacity") && fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("restored.graph || !restored.history"), "over-capacity undo history and partial rollback inputs should be preserved for recovery before degraded persistence fallbacks");
assert(graphCore.includes("function preferImportedDecision") && graphCore.includes("const preferredDecision = preferImportedDecision"), "feedback application should let a resolved timestamp-free import correct an equally undated graph decision");
assert(graphCore.includes("`${left.id}\\u0000${slugify(left.label)}`") && graphCore.includes("`${relationSemanticKey(left.source, left.target, left.label)}\\u0000${left.id}`"), "live extractor guidance should use canonical concept and relation ordering");
assert.equal(graphSchema.properties.redacted.type, "boolean", "graph schema should preserve redaction markers");
assert.equal(graphSchema.properties.graphFingerprint.pattern, "^fnv64-[0-9a-f]{16}-[0-9]+$", "graph JSON exports should support deterministic integrity fingerprints");
assert.equal(graphSchema.properties.appVersion.maxLength, 64, "graph JSON exports should retain bounded producer-version metadata");
assert.equal(graphSchema.$defs.node.properties.aliases.maxItems, 20, "graph schema should bound aliases");
assert(graphSchema.$defs.evidence.required.includes("sources"), "evidence provenance is part of the graph contract");
assert.equal(graphSchema.$defs.evidence.properties.text.maxLength, 12000, "graph schema should bound evidence text");
assert.equal(graphSchema.$defs.evidence.properties.sources.maxItems, 200, "graph schema should bound evidence provenance references");
assert.equal(graphSchema.$defs.evidence.properties.sources.items.maxLength, 200, "graph schema should bound evidence provenance identity length");
assert.equal(graphSchema.$defs.document.properties.id.maxLength, 200, "graph schema should bound document identity length");
assert.equal(graphSchema.$defs.node.properties.id.maxLength, 200, "graph schema should bound concept identity length");
assert.equal(graphSchema.$defs.edge.properties.id.maxLength, 200, "graph schema should bound relation identity length");
assert.equal(graphSchema.$defs.edge.properties.source.maxLength, 200, "graph schema should bound relation endpoints");
assert.equal(graphSchema.$defs.edge.properties.target.maxLength, 200, "graph schema should bound relation endpoints");
assert.equal(graphSchema.$defs.revision.properties.reason.maxLength, 200, "graph schema should bound revision reasons");
assert.deepEqual(graphSchema.$defs.revision.properties.operation.enum, ["unknown", "migration", "ingest", "rebuild", "feedback", "learning", "manual", "projection", "source-removal"], "revision operations should be a bounded audit vocabulary");
assert.deepEqual(graphSchema.$defs.revision.properties.extractor.enum, ["unknown", "local", "remote"], "revision extractor provenance should be a bounded privacy-safe vocabulary");
assert.equal(graphSchema.$defs.revision.properties.nodes.maximum, 5000, "graph schema should bound revision node counts");
assert.equal(graphSchema.$defs.revision.properties.edges.maximum, 10000, "graph schema should bound revision edge counts");
assert.equal(graphSchema.$defs.node.properties.aliases.items.maxLength, 120, "graph concept aliases should be bounded");
assert.equal(graphSchema.$defs.learningExample.properties.aliases.items.maxLength, 120, "learning aliases should be bounded");
assert.equal(graphSchema.$defs.node.properties.mentions.maximum, 1000000, "graph mention counts should be bounded");
assert.equal(graphSchema.$defs.node.properties.feedback.maximum, 1000000, "graph feedback counts should be bounded");
assert.equal(graphSchema.$defs.edge.properties.feedback.minimum, -1000000, "graph feedback counts should have a lower bound");
assert.equal(graphSchema.properties.updatedAt.maxLength, 128, "graph timestamps should be bounded");
assert.equal(graphSchema.$defs.document.properties.addedAt.maxLength, 128, "document timestamps should be bounded");
assert.equal(graphSchema.$defs.document.properties.uri.maxLength, 2048, "source URIs should be bounded");
assert.match(graphSchema.$defs.document.properties.uri.pattern, /https\?/i, "graph URI schema should allow web schemes");
assert.equal(graphSchema.$defs.revision.properties.timestamp.maxLength, 128, "revision timestamps should be bounded");
const feedbackExampleSchema = feedbackSchema.properties.examples.items;
assert.equal(feedbackExampleSchema.properties.id.maxLength, 200, "feedback identity fields should match graph identity bounds");
assert.deepEqual(feedbackExampleSchema.properties.status.enum, ["accepted", "rejected"], "feedback exports should contain reviewed decisions only");
assert.equal(feedbackExampleSchema.properties.sources.maxItems, 200, "feedback provenance references should be bounded");
assert.equal(feedbackExampleSchema.properties.sources.items.maxLength, 200, "feedback provenance identity length should be bounded");
assert.equal(feedbackExampleSchema.properties.evidence.maxItems, 8, "feedback evidence should be bounded");
assert.equal(requestSchema.properties.feedback.items.$ref, "#/$defs/feedbackHint", "remote feedback should use the bounded hint contract");
assert.equal(requestSchema.properties.document.properties.uri.maxLength, 2048, "remote source URIs should be bounded");
assert.equal(requestSchema.properties.document.properties.uri.pattern, graphSchema.$defs.document.properties.uri.pattern, "remote URI schema should match graph URI safety");
assert.equal(diffSchema.$defs.item.properties.uri.pattern, graphSchema.$defs.document.properties.uri.pattern, "diff URI schema should match graph URI safety");
assert.equal(requestSchema.$defs.feedbackHint.properties.id.maxLength, 200, "remote feedback hint IDs should be bounded");
assert.deepEqual(requestSchema.$defs.feedbackHint.properties.status.enum, ["accepted", "rejected"], "remote feedback hints should contain reviewed decisions only");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("mergeSourceIds"), "graph merges should use bounded provenance merging");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("value.examples.slice(-MAX_FEEDBACK_FINGERPRINT_EXAMPLES)") && graphCore.includes("retainNewestLearningExamples"), "learning normalization should retain the newest bounded corrections");
assert.equal(feedbackSchema.properties.format.const, "llm-field-notes/feedback@1");
assert.equal(backupSchema.properties.format.const, "llm-field-notes/backup@1");
assert.equal(backupSchema.properties.appVersion.maxLength, 64, "backups should retain bounded producer-version metadata");
assert.match(backupSchema.properties.graphFingerprint.pattern, /^\^fnv64-/);
assert(!backupSchema.required.includes("graphFingerprint"), "backup fingerprints should remain optional for legacy backup compatibility");
assert(graphCore.includes("buildBackupEnvelope") && graphCore.includes("validateBackupEnvelope") && graphCore.includes("fingerprint does not match its graph and history") && app.includes("validateBackupEnvelope(imported, { label: \"That backup\" })"), "backups should centralize bounded producer metadata and verify content identity before restore");
assert.equal(evaluationComparisonSchema.properties.format.const, "llm-field-notes/evaluation-comparison@1");
assert.equal(evaluationComparisonSchema.properties.feedbackTrust.properties.maxUntrustedFeedback.maximum, 15000, "evaluation comparisons should bound recorded feedback trust thresholds");
assert.equal(evaluationComparisonSchema.properties.metrics.maxItems, 12, "evaluation comparisons should allow every supported metric");
assert.equal(evaluationComparisonSchema.properties.regressions.maxItems, 12, "evaluation comparisons should allow every supported regression");
assert.equal(evaluationSchema.properties.feedback.properties.datasetFingerprint.pattern, "^fnv1a-[0-9a-f]{8}(?:[0-9a-f]{8})?$");
for (const freshnessField of ["freshExamples", "staleExamples", "undatedExamples", "untrustedExamples"]) {
  assert.equal(evaluationSchema.properties.feedback.properties[freshnessField].maximum, 15000, `evaluation schema should bound ${freshnessField}`);
}
assert.equal(evaluationSchema.$defs.acceptedMetric.properties.evidenceCoverage.maximum, 1, "evaluation reports should expose bounded evidence coverage");
assert.equal(evaluationSchema.$defs.acceptedMetric.properties.reviewedPrecision.maximum, 1, "evaluation reports should expose bounded reviewed precision");
assert.equal(feedbackSchema.properties.datasetFingerprint.pattern, "^fnv1a-[0-9a-f]{8}(?:[0-9a-f]{8})?$");
assert.equal(feedbackSchema.properties.truncatedExamples.maximum, 15500, "feedback exports should disclose bounded omission counts");
assert(!feedbackSchema.required.includes("datasetFingerprint"), "feedback fingerprints should remain optional for legacy dataset compatibility");
assert(app.includes("matchesFeedbackFingerprint"), "feedback imports should accept verified legacy fingerprints during migration");
assert(app.includes("too many feedback items to apply safely"), "the browser should explain bounded Obsidian feedback rejections");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("Try it in 60 seconds") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("npm run serve:pages"), "the README should expose a verified first-run path");
assert.equal(healthSchema.properties.format.const, "llm-field-notes/health@1");
assert(!healthSchema.required.includes("graphFingerprint") && healthSchema.properties.graphFingerprint.pattern.startsWith("^fnv64-"), "health reports should add a backward-compatible deterministic graph fingerprint");
assert.equal(healthSchema.properties.appVersion.maxLength, 64, "health reports should identify the producing build within a bounded field");
assert.equal(healthSchema.properties.health.properties.reviewCandidates.maximum, 15000, "health schemas should bound actionable review candidates");
assert.equal(healthSchema.properties.health.properties.reviewQueueTruncated.type, "boolean", "health schemas should disclose capped review queues");
assert.equal(healthSchema.properties.health.properties.staleReviewCandidates.maximum, 15000, "health schemas should bound stale review candidates");
assert.equal(healthSchema.properties.health.properties.staleLearningExamples.maximum, 500, "health schemas should bound stale learning examples");
assert.equal(healthSchema.properties.health.properties.freshSourceReviewCoverage.maximum, 100, "health schemas should bound fresh source coverage");
assert.equal(healthSchema.properties.health.properties.orphanedSourceReferences.maximum, 27000000, "health schemas should account for direct and evidence provenance references");
assert.equal(healthSchema.properties.health.properties.ambiguousSourceReferences.maximum, 27000000, "health schemas should account for direct and evidence ambiguity references");
assert.equal(healthSchema.properties.health.properties.rejectedEdges.maximum, 10000, "health schemas should bound rejected relation diagnostics");
assert.equal(healthSchema.properties.health.properties.ambiguousLabels.maximum, 5000, "health schemas should bound ambiguous label diagnostics");
assert.equal(healthSchema.properties.health.properties.reviewedSources.maximum, 1000, "health schemas should bound reviewed source diagnostics");
assert.equal(healthSchema.properties.health.properties.newEvidenceReviewCandidates.maximum, 15000, "health schemas should expose bounded new-evidence review pressure");
assert.equal(healthSchema.properties.health.properties.unanchoredEvidenceRecords.maximum, 120000, "health schemas should bound unanchored evidence diagnostics");
assert.equal(healthSchema.properties.health.properties.evidenceGroundingCheckedRecords.maximum, 2000, "health schemas should bound evidence grounding work");
assert.equal(healthSchema.properties.health.properties.evidenceGroundingComparisons.maximum, 250000, "health schemas should bound evidence grounding comparisons");
assert.equal(healthSchema.properties.health.properties.evidenceGroundingSourceScanChars.maximum, 50000000, "health schemas should bound evidence grounding source scanning");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxUnanchoredEvidence.maximum, 120000, "health schemas should bound unanchored evidence gates");
assert.equal(healthSchema.properties.health.properties.feedbackContextRetained.maximum, 500, "health schemas should bound retained extractor guidance");
assert.equal(healthSchema.properties.health.properties.feedbackContextAvailable.maximum, 15500, "health schemas should bound the total live and reusable guidance inventory");
assert.equal(healthSchema.properties.health.properties.feedbackContextTruncated.type, "boolean", "health schemas should disclose truncated extractor guidance");
assert.equal(healthSchema.properties.health.properties.feedbackContextExcluded.maximum, 15500, "health schemas should disclose withheld extractor guidance");
assert(graphCore.includes("export const HEALTH_COUNT_LIMITS") && graphCore.includes("export const HEALTH_PERCENTAGES") && graphCore.includes("export const HEALTH_BOOLEANS") && graphCore.includes("export const HEALTH_OBJECTS") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("Object.entries(HEALTH_COUNT_LIMITS)") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("HEALTH_PERCENTAGES") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("HEALTH_BOOLEANS") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("HEALTH_OBJECTS"), "health runtime bounds and types should be exported and checked exhaustively");
assert(graphCore.includes("export const MAX_SOURCE_REFERENCE_DIAGNOSTICS") && graphCore.includes("orphanedSourceReferences: MAX_SOURCE_REFERENCE_DIAGNOSTICS") && graphCore.includes("ambiguousSourceReferences: MAX_SOURCE_REFERENCE_DIAGNOSTICS"), "provenance diagnostic bounds should be derived from graph item, evidence, and source-reference limits");
assert(fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("parseJsonWithUniqueKeys") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("MAX_PRODUCER_VERSION_CHARS"), "schema contract checks should share duplicate-key parsing and producer-version bound drift checks");
assert(graphCore.includes("assertHealthInvariant") && graphCore.includes("sourceQuality totals cannot exceed documents") && graphCore.includes("supportedNodes plus unsupportedNodes must equal activeNodes"), "health validation should enforce cross-field diagnostic consistency");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("MAX_EVALUATION_ID_CHARS") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("MAX_SOURCE_REFERENCES") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("assertReviewableExamples") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("bounded evidence contract"), "evaluation input should enforce bounded reviewed-example fields before matching or fingerprinting");
assert(app.includes("reservedCurrentKeys") && app.includes("const learningBudget = Math.max(0, MAX_FEEDBACK_EXAMPLES - reservedCurrentKeys.size)"), "feedback exports should reserve bounded capacity for current reviewed decisions before detached memory");
assert(app.includes("orderedLearningExamples") && app.includes("rightTime - leftTime"), "feedback exports should prioritize reusable learning examples by review freshness");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("All runtime health-count limits are exported"), "schema documentation should describe exhaustive health-count contract parity");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("Health percentage and boolean fields are exported"), "schema documentation should describe exhaustive health type parity");
assert.equal(healthSchema.properties.reviewQueue.maxItems, 15000, "health schemas should bound exported review candidates");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("MAX_REVIEW_QUEUE_ITEMS") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("reviewQueueTruncated"), "review queue limits and truncation diagnostics should use named shared domain contracts");
assert.equal(healthSchema.properties.reviewQueue.items.additionalProperties, false, "health review queue items should have a closed privacy-safe shape");
assert.equal(healthSchema.properties.health.properties.truncated.type, "boolean", "health schemas should disclose import truncation");
assert.equal(healthSchema.properties.health.properties.truncatedItems.maximum, 9007199254740991, "health schemas should bound total omitted import items");
assert.equal(healthSchema.properties.health.properties.truncatedEvidenceItems.maximum, 4294967295, "health schemas should expose omitted evidence-item diagnostics");
assert.equal(healthSchema.properties.health.properties.truncatedSourceReferences.maximum, 4294967295, "health schemas should expose omitted provenance-reference diagnostics");
assert.equal(healthSchema.properties.health.properties.truncatedAliases.maximum, 110000, "health schemas should expose clipped alias diagnostics");
assert.equal(healthSchema.properties.health.properties.droppedItems.maximum, 9007199254740991, "health schemas should bound total dropped import items");
assert.equal(healthSchema.properties.gate.properties.violations.maxItems, 12, "health schemas should allow every bounded health threshold violation");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxReviewCandidates.maximum, 15000, "health schemas should bound review-candidate gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxReviewQueueTruncated.maximum, 1, "health schemas should bound review-queue truncation gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxEvidenceGroundingTruncated.maximum, 1, "health schemas should bound evidence-grounding sampling gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxFeedbackContextTruncated.maximum, 1, "health schemas should bound guidance-context truncation gates");
assert(graphCore.includes("maxReviewQueueTruncated: 1"), "health runtime gates should bound review-queue truncation thresholds");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxStaleReviewCandidates.maximum, 15000, "health schemas should bound stale-review gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxStaleLearningExamples.maximum, 500, "health schemas should bound stale-learning gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxWithheldGuidance.maximum, 15500, "health schemas should bound withheld-guidance gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxConflictingItems.maximum, 200, "health schemas should bound contradictory-record gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxTruncatedItems.maximum, 9007199254740991, "health schemas should bound import truncation gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxDroppedItems.maximum, 9007199254740991, "health schemas should bound malformed-import gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxUnsupportedNodes.maximum, 5000, "health schemas should bound unsupported-concept gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.maxUnsupportedEdges.maximum, 10000, "health schemas should bound unsupported-relation gates");
assert.equal(healthSchema.properties.gate.properties.thresholds.properties.minFreshSourceReview.maximum, 100, "health schemas should bound fresh source-review gates");
assert.equal(packageManifest.scripts.health, "node experiments/inspect-graph.mjs", "graph health diagnostics should have an automation shortcut");
assert(packageManifest.scripts["health:sample"]?.includes("examples/sample-graph.json")
  && packageManifest.scripts["health:sample"]?.includes("--min-provenance 100")
  && packageManifest.scripts["health:sample"]?.includes("--max-evidence-grounding-truncated 0")
  && packageManifest.scripts["health:sample"]?.includes("--max-feedback-context-truncated 0"), "the published sample graph should have a reusable structural health promotion gate");
assert.equal(packageManifest.scripts["project:jsonld"], "node experiments/project-jsonld.mjs", "JSON-LD projection should have an automation shortcut");
assert.equal(packageManifest.scripts["verify:jsonld"], "node experiments/verify-jsonld.mjs", "JSON-LD verification should have an automation shortcut");
assert.equal(packageManifest.scripts["verify:graph"], "node experiments/verify-graph.mjs", "graph verification should have an automation shortcut");
assert.equal(packageManifest.scripts["verify:backup"], "node experiments/verify-backup.mjs", "backup verification should have an automation shortcut");
assert.equal(packageManifest.scripts["verify:share"], "node scripts/verify-share.mjs", "share verification should have an automation shortcut");
assert.equal(packageManifest.scripts["verify:diff"], "node experiments/verify-diff.mjs", "diff verification should have an automation shortcut");
assert(fs.existsSync(new URL("../experiments/project-jsonld.mjs", import.meta.url)), "JSON-LD projection CLI should remain deliverable");
assert(fs.existsSync(new URL("../experiments/verify-jsonld.mjs", import.meta.url)), "JSON-LD verification CLI should remain deliverable");
assert(fs.existsSync(new URL("../experiments/verify-graph.mjs", import.meta.url)), "graph verification CLI should remain deliverable");
assert(fs.existsSync(new URL("../experiments/verify-backup.mjs", import.meta.url)), "backup verification CLI should remain deliverable");
assert(fs.existsSync(new URL("../experiments/verify-diff.mjs", import.meta.url)), "diff verification CLI should remain deliverable");
assert(fs.existsSync(new URL("../experiments/inspect-graph.mjs", import.meta.url)), "graph health CLI should remain deliverable");
assert(fs.existsSync(new URL("../scripts/check-security-txt.mjs", import.meta.url)) && packageManifest.scripts["security:check"] === "node scripts/check-security-txt.mjs", "security disclosure metadata should have a release-gated freshness check");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("buildHealthReport(graph") && fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("report.gate = runGate(report.health"), "graph health CLI should reuse one diagnostic pass for its report and gate");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes('readGraphInput(inputPath, { label: "Graph health input" })'), "graph health CLI should use the shared fingerprinted graph input boundary");
assert(fs.readFileSync(new URL("../experiments/diff-graphs.mjs", import.meta.url), "utf8").includes('readGraphInput(beforePath, { label: "Graph diff input" })') && fs.readFileSync(new URL("../experiments/diff-graphs.mjs", import.meta.url), "utf8").includes('readGraphInput(afterPath, { label: "Graph diff input" })'), "graph diff CLI should use the shared fingerprinted graph input boundary");
assert(fs.readFileSync(new URL("../experiments/graph-input.mjs", import.meta.url), "utf8").includes("export function validateProducerVersion") && fs.readFileSync(new URL("../experiments/verify-graph.mjs", import.meta.url), "utf8").includes("history.forEach((snapshot) => validateProducerVersion") && fs.readFileSync(new URL("../tests/experiment-smoke.mjs", import.meta.url), "utf8").includes("malformedHistoryProducerPath"), "all graph input verifiers should reject malformed producer metadata before normalization");
assert(fs.readFileSync(new URL("../experiments/verify-diff.mjs", import.meta.url), "utf8").includes("diffGraphs(before, after)") && fs.readFileSync(new URL("../experiments/verify-diff.mjs", import.meta.url), "utf8").includes("trustedPastTimestamp") && fs.readFileSync(new URL("../experiments/verify-diff.mjs", import.meta.url), "utf8").includes("MAX_GRAPH_INPUT_BYTES") && !fs.readFileSync(new URL("../experiments/verify-diff.mjs", import.meta.url), "utf8").includes("localeCompare"), "diff verification CLI should recompute bounded, chronology-safe, locale-independent diffs from shared graph inputs");
assert(evaluation.includes("parseTimestamp(value.evaluatedAt)") && evaluation.includes("Date.now()"), "evaluation reports should reject future-dated evaluation metadata");
assert(graphCore.includes("validateBackupEnvelope") && fs.readFileSync(new URL("../experiments/graph-input.mjs", import.meta.url), "utf8").includes("validateBackupEnvelope(value") && fs.readFileSync(new URL("../experiments/verify-graph.mjs", import.meta.url), "utf8").includes("validateBackupEnvelope(value"), "graph input and verification boundaries should share strict backup-envelope validation before fingerprint normalization");
assert(fs.readFileSync(new URL("../experiments/verify-graph.mjs", import.meta.url), "utf8").includes("complete: integrity.truncatedItems === 0 && integrity.droppedItems === 0") && fs.readFileSync(new URL("../experiments/verify-graph.mjs", import.meta.url), "utf8").includes("historyIntegrity"), "graph verification should distinguish fingerprint validity from completeness and include backup-history import loss");
assert(fs.readFileSync(new URL("../experiments/evaluate-feedback.mjs", import.meta.url), "utf8").includes("matchesGraphFingerprint(extractionValue, extractionValue.graphFingerprint)"), "evaluation CLI should verify fingerprinted graph inputs, including wrapped extraction payloads");
assert(fs.readFileSync(new URL("../experiments/evaluate-feedback.mjs", import.meta.url), "utf8").includes("truncatedExamples") && fs.readFileSync(new URL("../experiments/evaluate-feedback.mjs", import.meta.url), "utf8").includes("partial export"), "evaluation CLI should reject partial feedback exports before scoring");
assert(fs.readFileSync(new URL("../experiments/evaluate-feedback.mjs", import.meta.url), "utf8").includes("--max-untrusted-feedback"), "evaluation CLI should expose an opt-in untrusted-feedback gate");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("input.nodes.slice(0, MAX_GRAPH_NODES)") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("input.edges.slice(0, MAX_GRAPH_EDGES)"), "provider normalization should bound returned node and edge collections before copying them");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("health.ambiguousLabels"), "graph health ambiguity gates should include duplicate concept labels");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("npm run health -- graph.json") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-review-queue-truncated") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-evidence-grounding-truncated") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-feedback-context-truncated") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-stale-review-candidates") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-stale-learning-examples") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-truncated-items") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-dropped-items") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-conflicting-items") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--min-fresh-source-review") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("--max-unsupported-nodes"), "the main README should document graph health automation, review-queue truncation, grounding sampling, guidance truncation, stale-learning, contradiction, truncation, and dropped-entry gates");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("only stale reusable learning memory") && fs.readFileSync(new URL("../ARCHITECTURE.md", import.meta.url), "utf8").includes("explicit, undoable cleanup action"), "public documentation should explain stale-learning cleanup semantics");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("stale reusable-learning counts") && fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("--max-stale-learning-examples") && fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("--max-withheld-guidance") && fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("withheld pending"), "the schema guide should document stale-learning, withheld-guidance, truncation, and dropped-entry health diagnostics");
assert(fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-review-queue-truncated") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-stale-review-candidates") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-stale-learning-examples") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-truncated-items") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-dropped-items") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-conflicting-items") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--min-fresh-source-review") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("--max-unsupported-nodes"), "the experiment guide should document stale-learning, contradiction, truncation, and dropped-entry gates");
assert(fs.readFileSync(new URL("../experiments/compare-evaluations.mjs", import.meta.url), "utf8").includes("validateEvaluationReport"), "evaluation promotion should validate reviewed benchmarks before comparison");
assert(fs.readFileSync(new URL("../experiments/compare-evaluations.mjs", import.meta.url), "utf8").includes("contradictory reviewed decisions"), "evaluation promotion should reject conflicted reviewed benchmarks");
assert(fs.readFileSync(new URL("../experiments/compare-evaluations.mjs", import.meta.url), "utf8").includes("--max-untrusted-feedback"), "evaluation promotion should expose an opt-in untrusted-feedback gate");
assert(fs.readFileSync(new URL("../experiments/compare-evaluations.mjs", import.meta.url), "utf8").includes("pathToFileURL(process.argv[1])"), "evaluation promotion helpers should be import-safe for programmatic consumers");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("evaluation-comparison.schema.json"), "the public project map should document the evaluation promotion contract");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("evaluation.schema.json") && fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("evaluation-comparison.schema.json"), "the schema guide should document evaluation contracts");
assert(fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes('readSchema("learning-loop.schema.json")') && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("learning-loop guidance delta"), "runtime/schema contract checks should cover the learning-loop artifact bounds");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("learning-loop.schema.json"), "the schema guide should document the learning-loop artifact contract");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("closed companion contracts") && fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("fail closed"), "schema guidance should distinguish extensible graph fields from closed companion contracts");
assert(fs.readFileSync(new URL("../schema/README.md", import.meta.url), "utf8").includes("graphFingerprint"), "the schema guide should document backup integrity fingerprints");
assert.equal(diffSchema.properties.format.const, "llm-field-notes/diff@1");
assert.equal(diffSchema.properties.graphSchema.const, "llm-field-notes/graph@1");
assert.equal(diffSchema.$defs.collection.properties.changed.maxItems, 10000, "graph diffs should remain bounded");
assert(diffSchema.properties.learning && diffSchema.properties.integrity, "graph diffs should include learning and integrity changes");
assert(diffSchema.properties.redaction, "graph diffs should include redaction state changes");
assert.equal(diffSchema.$defs.diagnosticCounts.properties.documentText.maximum, 4294967295, "diff schemas should preserve clipped document-text diagnostics");
assert.equal(diffSchema.$defs.diagnosticCounts.properties.documentTitle.maximum, 4294967295, "diff schemas should preserve clipped document-title diagnostics");
assert.equal(diffSchema.$defs.diagnosticCounts.properties.evidenceText.maximum, 4294967295, "diff schemas should preserve clipped evidence-text diagnostics");
assert.equal(diffSchema.$defs.diagnosticCounts.properties.evidenceItems.maximum, 4294967295, "diff schemas should preserve omitted evidence-item diagnostics");
assert.equal(diffSchema.$defs.diagnosticCounts.properties.sourceReferences.maximum, 4294967295, "diff schemas should preserve omitted provenance-reference diagnostics");
assert.equal(requestSchema.properties.operation.const, "extract-graph");
assert.equal(backupSchema.properties.graph.$ref, "graph.schema.json", "backup schema should resolve graph schema locally");
assert.equal(backupSchema.$id, "backup.schema.json", "backup schema identifiers should remain offline-resolvable");
assert(html.includes("Content-Security-Policy") && html.includes("sha256-efybT6wtRaWZshwoqZ3HWV1NybxKXj9GmB5ie+Pw5iM=") && server.includes("sha256-efybT6wtRaWZshwoqZ3HWV1NybxKXj9GmB5ie+Pw5iM="), "the strict CSP should narrowly authorize structured discovery metadata");
assert(!html.includes("fonts.googleapis.com") && !fs.readFileSync(new URL("../styles.css", import.meta.url), "utf8").includes("fonts.googleapis.com"), "the local-first app should not require third-party font requests");
assert(server.includes("style-src 'self'; font-src 'self'"), "server CSP should keep styles and fonts same-origin");
assert(robots.includes("User-agent: *") && robots.includes("Allow: /"), "static deployments should expose an intentional indexing policy");
assert(html.includes('<meta name="robots" content="index,follow" />'), "the public wiki should declare its indexing intent");
assert(html.includes('<link rel="canonical" href="./" />'), "the public wiki should declare a canonical entry point");
assert(html.includes('rel="alternate" type="application/atom+xml" href="feed.xml"'), "the public wiki should advertise its optional Atom feed");
assert(html.includes('<noscript class="noscript-map">') && html.includes("Tokens are the interface") && html.includes("Ship the boring parts"), "the learning map should remain crawlable and usable without JavaScript");
assert(app.includes('class="note-source"') && app.includes("./notes/"), "interactive learning notes should expose durable Markdown sources");
assert(server.includes("learningNoteAssets") && server.includes('extname(entry.name) === ".md"') && server.includes('".md": "text/markdown; charset=utf-8"'), "Markdown learning notes should be safely deliverable");
assert(server.includes('const fixedPublicAssets = FIXED_PUBLIC_ASSETS;') && publicAssets.includes("export const PUBLIC_ASSETS") && publicAssets.includes("export const OFFLINE_SHELL_ASSETS"), "static serving and readiness should share one public asset allowlist");
assert(fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("contain duplicate entries") && fs.readFileSync(new URL("../scripts/check-release.mjs", import.meta.url), "utf8").includes("unpublishedSitemapAssets"), "release validation should reject duplicate or unpublished deployment asset entries");
for (const noteFile of fs.readdirSync(notesDirectory).filter((file) => file.endsWith(".md"))) {
  assert(serviceWorker.includes(`"./notes/${noteFile}"`), `service worker shell is missing learning note: ${noteFile}`);
  if (noteFile !== "README.md") {
    const noteId = noteFile.slice(0, -3);
    assert(curriculum.includes(`id: "${noteId}"`), `shared learning catalog is missing note: ${noteFile}`);
    assert(notesIndex.includes(`(${noteFile})`), `notes index is missing note link: ${noteFile}`);
  }
}
assert(publicAssets.includes('"CONTRIBUTING.md"') && html.includes('href="CONTRIBUTING.md"'), "the public contribution link should target an allowlisted asset");
assert(publicAssets.includes('"ARTIFACTS.md"') && serviceWorker.includes("./ARTIFACTS.md"), "the public artifact index should be allowlisted and available offline");
assert(artifactHtml.includes('href="./#sample"') && artifactHtml.includes("Try the workbench"), "the artifact gallery should offer a direct one-click path into the sample workbench");
assert(publicAssets.includes('"ARTIFACT_SUBMISSION.md"') && serviceWorker.includes("./ARTIFACT_SUBMISSION.md"), "the public artifact submission guide should be allowlisted and available offline");
const { PUBLIC_ASSETS: publicAssetList } = await import("../scripts/public-assets.mjs");
const generatedPublicAssets = new Set(["sample-graph.html"]);
for (const documentName of ["README.md", "CONTRIBUTING.md", "ARTIFACTS.md", "ARTIFACT_SUBMISSION.md", "ARCHITECTURE.md", "SECURITY.md", "llms.txt"]) {
  const documentText = fs.readFileSync(new URL(`../${documentName}`, import.meta.url), "utf8");
  for (const match of documentText.matchAll(/\[[^\]]+\]\(([^)]+)\)/g)) {
    const target = match[1].split("#", 1)[0].split("?", 1)[0];
    if (!target || /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i.test(target)) continue;
    const normalizedTarget = target.replace(/^\.\/+/, "") || "index.html";
    assert(publicAssetList.includes(normalizedTarget) || generatedPublicAssets.has(normalizedTarget), `${documentName} links to an unpublished public asset: ${match[1]}`);
  }
}
const experimentAssets = fs.readdirSync(new URL("../experiments/", import.meta.url))
  .filter((file) => file.endsWith(".mjs"))
  .sort()
  .map((file) => `experiments/${file}`);
assert.equal(artifactStructuredData.mainEntity.numberOfItems, experimentAssets.length + 5, "artifact structured metadata should declare the complete experiment, verifier, and starter-graph inventory");
assert.equal(new Set(artifactItems.map((item) => item.url)).size, artifactItems.length, "artifact structured metadata should not duplicate artifact URLs");
assert.deepEqual(artifactItems.map((item) => item.position), Array.from({ length: experimentAssets.length + 5 }, (_, index) => index + 1), "artifact structured metadata should use contiguous item positions");
for (const artifactAsset of experimentAssets) {
  assert(publicAssetList.includes(artifactAsset), `experiment source should be publicly allowlisted: ${artifactAsset}`);
  assert(publicAssets.includes(`"${artifactAsset}"`) && serviceWorker.includes(`"./${artifactAsset}"`), `public experiment source should be allowlisted and available offline: ${artifactAsset}`);
  assert(dockerignore.includes(`!${artifactAsset}`), `Docker context should retain the published experiment source: ${artifactAsset}`);
  assert(artifactHtml.includes(`href="${artifactAsset}"`), `artifact gallery should expose every published experiment: ${artifactAsset}`);
  assert(artifactItems.some((item) => item.url === artifactAsset), `artifact structured metadata should expose every published experiment: ${artifactAsset}`);
}
assert(publicAssets.includes('"ARCHITECTURE.md"') && fs.readFileSync(new URL("../ARCHITECTURE.md", import.meta.url), "utf8").includes("# LLM Field Notes architecture"), "the architecture guide should be public and served");
const changelog = fs.readFileSync(new URL("../CHANGELOG.md", import.meta.url), "utf8");
assert(publicAssets.includes('"CHANGELOG.md"') && changelog.includes("# Changelog") && changelog.includes("## [Unreleased]") && changelog.includes("type-specific, fail-closed Obsidian frontmatter") && changelog.includes("crash-consistent graph-store write ordering") && changelog.includes("static DOM and ARIA contract checks"), "the release changelog should be public and record current production hardening");
assert(html.includes('href="ARCHITECTURE.md"'), "the architecture guide should be discoverable from the public wiki");
assert(html.includes('href="CHANGELOG.md"'), "the release changelog should be discoverable from the public wiki");
assert(publicAssets.includes('"README.md"') && publicAssets.includes('"SECURITY.md"') && publicAssets.includes('"RUNBOOK.md"') && readme.includes("[production runbook](RUNBOOK.md)"), "public deployment and operational guidance should be allowlisted and linked");
assert(runbook.includes("npm run health:sample") && runbook.includes("16–4,096 characters") && runbook.includes("control characters") && runbook.includes("30-day publication-evidence"), "the production runbook should require the same graph-quality and bearer-secret gates as CI and explain retained Pages deployment evidence");
assert(publicAssets.includes('"CODE_OF_CONDUCT.md"') && conduct.includes("Expected behavior") && fs.readFileSync(new URL("../CONTRIBUTING.md", import.meta.url), "utf8").includes("CODE_OF_CONDUCT.md"), "community participation guidance should be public and linked");
assert(publicAssets.includes('"experiments/README.md"') && serviceWorker.includes("./experiments/README.md") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("tiny-bpe.mjs") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("tiny-training.mjs") && fs.readFileSync(new URL("../experiments/README.md", import.meta.url), "utf8").includes("tiny-transformer.mjs"), "runnable experiment documentation should be deliverable and offline-safe");
assert(publicAssets.includes('"schema/vault-manifest.schema.json"') && serviceWorker.includes("./schema/vault-manifest.schema.json"), "the Obsidian projection contract should remain available offline");
assert(publicAssets.includes('"schema/encrypted-backup.schema.json"') && serviceWorker.includes("./schema/encrypted-backup.schema.json") && fs.readFileSync(new URL("../schema/encrypted-backup.schema.json", import.meta.url), "utf8").includes('"llm-field-notes/encrypted-backup@1"'), "the encrypted backup contract should remain publicly available and cached offline");
const securityTxt = fs.readFileSync(new URL("../.well-known/security.txt", import.meta.url), "utf8");
assert(publicAssets.includes('".well-known/security.txt"') && serviceWorker.includes("./.well-known/security.txt") && securityTxt.includes("Contact: https://github.com/humblemat810/llm-wiki/security/advisories/new") && securityTxt.includes("Expires: 2027-07-15T00:00:00.000Z"), "machine-readable security disclosure metadata should remain published and cached offline");
for (const match of html.matchAll(/(?:href|src)="([^"]+)"/g)) {
  const reference = match[1];
  if (!reference || reference.startsWith("#") || reference.startsWith("http:") || reference.startsWith("https:") || reference.startsWith("data:")) continue;
  const asset = reference.split("#", 1)[0].split("?", 1)[0].replace(/^\.\/+/, "") || "index.html";
  const dynamicallyAllowlistedNote = asset.startsWith("notes/") && asset.endsWith(".md") && server.includes("learningNoteAssets");
  const dynamicallyAllowlistedNotePage = asset.startsWith("notes/") && asset.endsWith(".html") && server.includes("notePageMatch");
  const dynamicallyAllowlistedFeed = asset === "feed.xml" && server.includes('requestPath === "/feed.xml"');
  assert(publicAssets.includes(`"${asset}"`) || dynamicallyAllowlistedNote || dynamicallyAllowlistedNotePage || dynamicallyAllowlistedFeed, `public shell reference is not in the static allowlist: ${asset}`);
}
const shellAssets = readServiceWorkerShellAssets(serviceWorker);
for (const shellAsset of shellAssets) {
  const asset = shellAsset.replace(/^\.\/+/, "") || "index.html";
  const dynamicallyAllowlistedNote = asset.startsWith("notes/") && asset.endsWith(".md") && server.includes("learningNoteAssets");
  const dynamicallyAllowlistedNotePage = asset.startsWith("notes/") && asset.endsWith(".html") && server.includes("notePageMatch");
  assert(publicAssets.includes(`"${asset}"`) || dynamicallyAllowlistedNote || dynamicallyAllowlistedNotePage, `service-worker asset is not in the static allowlist: ${asset}`);
}
assert(server.includes("permissions-policy") && server.includes("cross-origin-opener-policy"), "server should restrict browser capabilities and cross-origin isolation");
assert(server.includes("strict-transport-security") && server.includes('origin.startsWith("https://")'), "HTTPS public origins should enable conditional HSTS without affecting local HTTP development");
assert(server.includes("x-frame-options"), "server should provide legacy clickjacking protection");
assert(server.includes("EXTRACTOR_AUTH_TOKEN") && server.includes("METRICS_AUTH_TOKEN") && server.includes("timingSafeEqual") && server.includes("www-authenticate") && server.includes("authentication_failures_total") && server.includes('"same-site"'), "reference APIs should support observable constant-time bearer guards and reject sibling-origin Fetch Metadata");
assert(server.includes("compactFeedbackHint") && security.includes("compacts reviewed feedback at its trust"), "reference extraction should strip untrusted feedback fields before provider calls");
assert(server.includes("safeDiagnosticCode") && server.includes("sanitizeLogEntry") && server.includes("LOG_ENTRY_KEYS") && server.includes("Object.entries(entry).slice(0, 24)") && server.includes("const safeHost = sanitizeLogEntry({ host }).host") && server.includes("EXTRACTOR_FAILURE"), "provider diagnostic codes, structured log fields, and lifecycle host output should be bounded and allowlisted before logging");
assert(server.includes("responsesByStatus") && server.includes("llm_field_notes_http_responses_total") && server.includes("httpRequestsInFlight") && server.includes("llm_field_notes_http_duration_ms_bucket") && server.includes("llm_field_notes_extraction_failures_by_code_total") && server.includes("EXTRACTION_FAILURE_CODES"), "metrics should expose privacy-safe HTTP status, latency, in-flight pressure, and fixed-cardinality extraction-failure telemetry");
assert(server.includes("parseTrustedProxyHops") && server.includes("llm_field_notes_trusted_proxy_hops") && readme.includes("TRUST_PROXY_HOPS") && security.includes("invalid `TRUST_PROXY_HOPS`"), "proxy trust configuration should be bounded, observable, and fail closed when explicitly invalid");
assert(graphCore.includes("retainNewestLearningExamples") && graphCore.includes("trustedReviewTime") && readme.includes("review freshness"), "reusable learning memory should retain the newest trusted review decisions instead of relying only on insertion order");
assert(server.includes('response.setHeader("x-request-id", requestId)') && server.includes("const requestId = randomUUID();"), "all server responses should expose bounded request IDs for operational correlation");
assert(server.includes("export function canWriteResponse") && server.includes("!canWriteResponse(response)"), "all server response helpers should share the terminal response-state guard");
assert(fs.readFileSync(new URL("../SECURITY.md", import.meta.url), "utf8").includes("EXTRACTOR_AUTH_TOKEN"), "security guidance should document the optional extraction bearer guard");
assert(server.includes("requireExtractorAuth") && server.includes("requireMetricsAuth") && server.includes("AUTH_NOT_CONFIGURED") && server.includes("loopbackHost"), "non-loopback server startup should fail extraction and metrics closed without configured authentication");
assert(server.includes("EXTRACTOR_REQUEST_KEYS") && server.includes("EXTRACTOR_DOCUMENT_KEYS") && server.includes("EXTRACTOR_FEEDBACK_KEYS") && server.includes("hasOnlyKeys"), "the extraction server should enforce the closed extractor-request schema instead of silently discarding unknown fields");
assert(server.includes("Invalid extraction request body.") && server.includes("REQUEST_LENGTH_MISMATCH") && security.includes("bounded allowlist of safe diagnostics"), "extraction body failures should expose only bounded client diagnostics while retaining length-specific handling");
assert(server.includes("MIN_AUTH_TOKEN_CHARS") && server.includes("isUsableAuthToken") && server.includes("MAX_AUTH_TOKEN_CHARS") && security.includes("shorter than 16"), "bearer configuration should reject weak or malformed secrets before readiness");
assert(server.includes("requireSecurePublicOrigin") && server.includes("securePublicOriginConfigured") && server.includes("requireBuildRevision") && server.includes("A trusted source build revision is not configured.") && security.includes("fail readiness without a trusted HTTPS"), "non-loopback server readiness should require a trusted HTTPS public origin and traceable source revision");
assert(security.includes("Production launch checklist") && security.includes("Obsidian export/import review"), "security guidance should include an actionable production launch checklist");
assert(security.includes("--max-review-queue-truncated 0") && security.includes("--max-evidence-grounding-truncated 0") && security.includes("--max-feedback-context-truncated 0"), "security guidance should require complete review, grounding, and guidance health gates for production promotion");
assert(readme.includes("production launch checklist") && readme.includes("normalizeExtractionForDocument"), "the main README should link deployment guidance and describe the provenance-preserving normalization boundary");
assert(fs.readFileSync(new URL("../CONTRIBUTING.md", import.meta.url), "utf8").includes("normalizeExtractionForDocument"), "contributor guidance should distinguish generic and provenance-preserving extraction normalization");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes('credentials: "same-origin"'), "browser extraction should preserve same-origin gateway sessions without exposing tokens");
assert(server.includes("function sendEmpty"), "server should apply security headers to empty error responses");
assert(server.includes('"x-robots-tag": "noindex, nofollow"'), "operational JSON and metrics responses should opt out of crawler indexing");
assert(server.includes("function sendPlainText") && server.includes("function sendNotModified") && server.includes('["GET", "HEAD"].includes(request.method)') && server.includes("request.method === \"HEAD\""), "HTTP endpoints should preserve correct MIME, conditional responses, and HEAD parity");
assert(server.includes('"cache-control": "no-store"') && server.includes("NO_CACHE_STATIC_ASSETS") && server.includes('resolvedFilePath.endsWith("sw.js")') && server.includes('const cacheControl = "no-cache"'), "error, release-sensitive shell, service-worker, and generated discovery responses should use safe cache policies");
assert(server.includes('resolvedFilePath.endsWith("version.json")'), "release metadata should use a revalidating cache policy");
assert(server.includes('NO_CACHE_STATIC_ASSETS.has(relative)') && server.includes('"share.html"') && server.includes('relative === "artifacts.html"') && server.includes('resolvedFilePath.endsWith("sw.js")'), "origin-transformed and recipient shell pages should revalidate instead of serving stale deployment metadata");
assert(server.includes("realpath") && server.includes("resolvedFilePath") && server.includes("lstat") && server.includes("must not be a symbolic link"), "runtime static serving and readiness should reject symlinked assets and symlink escapes");
assert(server.includes("O_NOFOLLOW") && server.includes("READ_ONLY_NOFOLLOW_FLAGS"), "bounded runtime file readers should refuse symlink paths with platform support");
assert(server.includes("MAX_STATIC_ASSET_BYTES") && server.includes("metadata.size"), "static serving and readiness should bound public asset sizes");
assert(server.includes("PUBLIC_ORIGIN") && server.includes("sitemap.xml") && server.includes("xmlEscape") && server.includes("renderRepositoryAwareSecurityTxt") && server.includes("checkContinue") && server.includes("writeContinue") && server.includes("parseConfiguredBoundedInteger"), "production deployments should have opt-in crawler projections, fork-aware security metadata, early Expect: 100-continue body rejection, and fail-closed numeric configuration");
assert(publicOrigin.includes("export function normalizePublicOrigin") && publicOrigin.includes("export function requirePublicOrigin") && publicOrigin.includes("requireSecure") && publicOrigin.includes("loopback") && server.includes("requirePublicOrigin") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("requireSecure: true") && fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8").includes("requireSecure: true"), "Node and Pages deployments should share one fail-closed public-origin policy, with HTTPS enforced for non-loopback static publication");
assert(server.includes("feed.xml") && server.includes("application/atom+xml"), "production deployments should have an opt-in Atom feed projection");
assert(server.includes('learningNoteAssets.filter((asset) => asset !== "notes/README.md")'), "runtime crawler projections should match the Pages learning-note set");
assert(server.includes("MAX_LEARNING_NOTE_ASSETS") && server.includes("MAX_PUBLIC_ASSET_BYTES") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("MAX_LEARNING_NOTE_ASSETS") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("MAX_PUBLIC_ASSET_BYTES"), "runtime and Pages public collections should have shared count and aggregate bounds");
assert(server.includes("renderOriginAwareIndex") && server.includes("social-card.png") && server.includes("feed.xml"), "configured public origins should produce absolute raster sharing metadata");
assert(server.includes("deriveLearningNoteTitle") && server.includes("readBoundedUtf8(resolvedFile, MAX_NOTE_SUMMARY_CHARS)") && server.includes("sliceTextAtCodePointBoundary") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("sliceTextAtCodePointBoundary") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("MAX_DOCUMENT_TITLE_CHARS") && server.includes("realpathSync") && server.includes("startsWith(`${resolvedRoot}/`)") && server.includes("readSync") && server.includes('".canvas": "application/json; charset=utf-8"'), "Node and Pages note metadata should share static-root containment, code-point-safe truncation, shared title limits, bounded reads, and native Canvas MIME parity");
assert(server.includes("createHash") && server.includes("staticEtagCache") && server.includes("getStaticEtag") && server.includes("metadata.mtimeMs") && server.includes("metadata.ctimeMs") && server.includes("if-none-match") && server.includes("matchesEtag") && server.includes("304"), "static assets should support bounded metadata-keyed HTTP-compliant conditional ETag responses");
assert(server.includes("extractionClientAborts") && server.includes("llm_field_notes_extraction_client_aborts_total") && server.includes("status: 499") && server.includes("REQUEST_ABORTED"), "client-aborted extraction requests should remain visible through privacy-safe metrics and correlated logs");
assert(server.includes("parseDeclaredContentLength") && server.includes("REQUEST_LENGTH_MISMATCH") && server.includes("Content-Length header is invalid"), "HTTP request bodies should validate declared byte lengths before JSON interpretation");
assert(server.includes('"content-length": responseContent.byteLength'), "static responses should expose byte-accurate content lengths");
assert(server.includes('".txt": "text/plain; charset=utf-8"') && server.includes('".mjs": "text/javascript; charset=utf-8"'), "plain-text and published module assets should use explicit MIME types");
assert(server.includes('["ENOENT", "ELOOP", "ENOTDIR", "SYMLINK_ASSET", "ASSET_ESCAPE"]'), "static serving should distinguish missing, escaped, and symlinked assets from server failures");
assert(server.includes('const BODY_DISCARD_STARTED = Symbol("bodyDiscardStarted")') && server.includes('if (request.method !== "POST") discardRequestBody(request, response)') && server.includes("request[BODY_DISCARD_STARTED] = true"), "the server should centrally drain non-POST request bodies and make route-specific drains idempotent");
assert(app.includes('data-learning-action="rebuild"') && app.includes('learningAction === "rebuild"') && app.includes("apply learning to saved sources"), "reviewed learning should expose a direct guarded action to rebuild existing saved sources");
assert(app.includes("data-review-source") && app.includes("markSourceReviewed") && app.includes("alreadyReviewed") && app.includes("mark reviewed today (UTC)"), "source review candidates should expose a direct idempotent atomic UTC-day review action");
assert(app.includes("reviewedTimestamp > Date.now()") && app.includes("Review dates cannot be in the future."), "manual source review edits should reject future-dated timestamps");
assert(app.includes("maximumReviewDate") && app.includes('type="date" value="${escapeHtml(reviewedDate)}" max="${maximumReviewDate}"'), "source review date inputs should prevent future dates in the picker");
assert(html.includes('id="download-redacted-html"') && app.includes("buildRedactedHtmlProjection") && app.includes("buildRedactedGraphVisual") && app.includes("Content-Security-Policy") && app.includes("source text, evidence quotes, and source URIs were removed"), "public graph sharing should offer a bounded script-free redacted HTML projection with a visual graph");
assert(styles.includes(".sample-graph-page .button-light"), "public sample call-to-action buttons should remain readable on the dark sample page");
assert(html.includes("Graph.canvas"), "the projection panel should explain that Obsidian exports include the native Graph.canvas viewer");
assert(html.includes('id="share-redacted-html"') && app.includes('document.querySelector("#share-redacted-html")') && app.includes("function tryShareFile") && app.includes("navigator.canShare") && app.includes("File sharing is unavailable; HTML snapshot downloaded instead."), "redacted HTML should support native file sharing with a bounded download fallback");
assert(html.includes('id="download-redacted-html"') && html.includes('id="share-redacted-html"') && html.match(/id="download-redacted-html"[^>]+aria-describedby="projection-privacy-note"/)?.[0] && html.match(/id="share-redacted-html"[^>]+aria-describedby="projection-privacy-note"/)?.[0], "redacted HTML actions should expose their privacy explanation to assistive technology");
for (const id of ["copy-redacted-markdown", "share-redacted-markdown", "download-redacted-markdown", "download-redacted-html", "share-redacted-html", "download-redacted-canvas", "download-redacted-vault", "download-redacted-jsonld", "download-redacted"]) {
  assert(new RegExp(`id="${id}"[^>]+aria-describedby="projection-privacy-note"`).test(html), `${id} should expose the projection privacy explanation to assistive technology`);
}
assert(testRunner.includes('readdirSync("tests")') && app.includes("const activeObjectUrls = new Set()") && app.includes("revokeAllObjectUrls()") && app.includes("window.addEventListener(\"pagehide\""), "share and download projections should have dedicated privacy and object-URL lifecycle regressions");
assert(html.includes('id="share-wiki"'), "the public wiki should have a share action");
assert(html.includes('class="hero-art" role="img" aria-label="A visual representation of tokens becoming a useful product"'), "decorative hero art should expose its accessible image description");
assert(html.includes('name="twitter:card" content="summary_large_image"'), "social preview metadata should request a large share card");
assert(html.includes('property="og:url" content="./"') && html.includes('property="og:image" content="social-card.png"') && html.includes('property="og:image:type" content="image/png"') && html.includes('property="og:image:width" content="1200"') && html.includes('property="og:image:height" content="630"') && html.includes('name="twitter:image"') && html.includes('name="twitter:image:alt"'), "social previews should declare their URL and production-sized raster share-card metadata");
assert(fs.readFileSync(new URL("../social-card.svg", import.meta.url), "utf8").includes("Turn documents") && fs.readFileSync(new URL("../social-card.svg", import.meta.url), "utf8").includes("living map"), "the share card should carry a readable product promise");
const structuredDataMatch = html.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/);
assert(structuredDataMatch, "the public wiki should expose structured discovery metadata");
const structuredData = JSON.parse(structuredDataMatch[1]);
assert.equal(structuredData["@type"], "WebApplication");
assert.equal(structuredData.isAccessibleForFree, true);
assert(structuredData.featureList.includes("Human feedback and iterative representation improvement"), "structured metadata should describe the self-improvement loop");
assert(app.includes("function openNote") && app.includes("#note=") && app.includes("openNoteFromLocation"), "learning notes should support shareable deep links");
assert(app.includes('class="open-note" href="#note='), "learning notes should expose real permalink anchors");
assert(app.includes("data-copy-note") && app.includes("Note link copied."), "learning notes should expose direct copy-link actions");
assert(app.includes('href="./notes/${encodeURIComponent(note.id)}.html"') && app.includes('href="./notes/${encodeURIComponent(note.id)}.md"'), "learning note cards should expose both the canonical viewer and raw Markdown projection");
assert(app.includes("escapeHtml(note.title)") && app.includes("escapeHtml(note.description)") && app.includes("escapeHtml(item[1])"), "curriculum data should remain escaped at innerHTML rendering boundaries");
assert(app.includes("escapeHtml(node.status)") && app.includes("escapeHtml(edge.status)") && app.includes("escapeHtml(quality)"), "enum-derived graph labels should remain escaped at innerHTML rendering boundaries");
assert(app.indexOf("const escapeHtml") < app.indexOf("renderNotes();\nrenderPath();"), "the HTML escaping helper should initialize before the first curriculum render");
assert(app.includes("const noteHash = location.hash.match") && app.includes("notes/${encodeURIComponent(sharedNote.id)}.html") && app.includes("shareUrl.username = \"\"") && app.includes("shareUrl.password = \"\"") && app.includes("shareUrl.search = \"\"") && app.includes("shareUrl.hash = \"workbench\""), "sharing should use crawler-readable note pages and strip credentials and query data from public links");
assert(notePage.includes("og:type") && notePage.includes("application/ld+json") && notePage.includes("application/atom+xml") && notePage.includes("\"@type\": \"Article\"") && notePage.includes("buildLearningNotePage") && notePage.includes("renderLearningNoteMarkdown") && notePage.includes("https?:\\/\\/") && !notePage.includes("style="), "note landing pages should carry Article metadata, feed discovery, safe rendered Markdown, and safe external links without inline styles");
assert(notePage.includes("MAX_NOTE_SUMMARY_CHARS") && server.includes("readBoundedUtf8") && server.includes("readSync") && fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8").includes("MAX_NOTE_SUMMARY_CHARS"), "runtime and Pages note summaries should share a bounded summary window with bounded runtime reads");
assert(server.includes("learningNotePageCsp") && server.includes("script-src 'none'") && server.includes("connect-src 'none'"), "Node note landing pages should enforce a strict response CSP");
assert(server.includes("function sendNotModified(response, etag, cacheControl, extraHeaders = {})"), "conditional responses should support projection-specific security headers");
assert(html.includes("notes/tokens.html") && !html.includes("Enable JavaScript for progress tracking and interactive note dialogs. The core curriculum is available here without scripts:"), "no-script curriculum links should point to canonical note landing pages");
assert(html.includes('aria-labelledby="dialog-title"') && html.includes('aria-describedby="dialog-question"'), "learning note dialogs should expose their accessible name and description");
assert(html.includes('id="progress-status"') && html.includes('aria-live="polite"'), "learning progress failures should have a visible status surface");
assert(app.includes("lastNoteTrigger") && app.includes('document.querySelector("#dialog-close").focus()') && app.includes("lastNoteTrigger.focus()"), "note dialogs should restore focus to their opener");
assert(html.includes('id="ingest-document"') && html.includes('aria-keyshortcuts="Control+Enter Meta+Enter"') && html.includes("Ctrl/Cmd + Enter") && app.includes("event.ctrlKey || event.metaKey") && app.includes("if (!ingestInFlight) ingestButton.click()"), "document ingestion should expose a guarded and discoverable Ctrl/Cmd+Enter shortcut");
assert(app.includes("normalizeGraph"), "state normalization must remain wired");
assert(app.includes("backupCheckpointKey") && app.includes("recordBackupCheckpoint") && app.includes("backupCheckpointStale") && app.includes("graphHasRecoverableState") && app.includes("Undo history is not included in a full backup checkpoint") && app.includes("data-storage-action=\"backup\""), "populated or undo-recoverable workspaces should disclose and clear a specific stale full-backup checkpoint reminder");
assert(app.includes("event.key === backupCheckpointKey") && app.includes("lastBackupFingerprint = typeof event.newValue === \"string\""), "backup checkpoint reminders should synchronize across browser tabs");
assert(app.includes("currentBackupFingerprintCache") && app.includes("backupFingerprintCacheKey"), "backup reminder fingerprinting should avoid repeating full graph serialization on unrelated workbench renders");
assert(app.includes("appStorage.setItem(backupCheckpointKey, fingerprint);\n    lastBackupFingerprint = fingerprint;"), "backup checkpoint reminders should acknowledge a checkpoint only after its local persistence call succeeds");
assert(app.includes("appStorage.setItem(backupCheckpointKey, fingerprint)") && !app.includes("appStorage.setItem(backupCheckpointKey, JSON.stringify(graph)"), "backup checkpoint persistence should retain only a fingerprint, never graph payloads");
assert(app.includes('document.querySelector("#try-sample")') && app.includes("loadSampleButton.click()") && app.includes("ingestButton.click()") && app.includes("Add the sample document to this existing workspace?") && app.includes("Replace the current unbuilt document with the sample?") && app.includes("bypassSampleDraftConfirmation") && app.includes("workbenchBusy()") && fs.existsSync(new URL("../tests/onboarding-smoke.mjs", import.meta.url)), "the public onboarding path should offer a guarded, executable one-click local sample walkthrough");
assert(app.includes('location.hash === "#sample"') && app.includes('history.replaceState({}, "", "#workbench")'), "the installable app sample shortcut should launch the same onboarding walkthrough");
assert(app.includes("RELEASE_METADATA_TIMEOUT_MS") && app.includes("releaseController.signal") && app.includes("releaseFetch") && app.includes("releaseDeadline") && app.includes("Promise.race([releaseFetch, releaseDeadline])"), "release metadata fetch should explicitly settle by a deadline so startup cannot hang indefinitely");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes('normalizeId(inputSource.id) || `doc-${fingerprintText(sourceText)}`'), "extraction source IDs should be deterministic at the normalization boundary");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes('asText(node?.id).trim()'), "extraction concept IDs should repair explicit empty values");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("asDateTime(doc.addedAt"), "graph timestamps should be normalized at the graph boundary");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("normalizeSourceUri(doc.uri)"), "source URIs should be normalized at the graph boundary");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("providerNodeLabels") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("ambiguousProviderNodeIds") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes('status: "inferred"') && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("normalizeExtractionForDocument"), "remote extraction should derive identity from submitted content and keep provider output inference-only");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("SAFE_SOURCE_URI_SCHEMES"), "source URI schemes should be allowlisted");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("parsed.username") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("new RegExp(`^${scheme}://`"), "HTTP source URIs should reject ambiguous forms and embedded credentials");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("if (clean.length > MAX_SOURCE_URI_CHARS) return null"), "canonical source URI normalization should reject oversized values instead of truncating provenance");
assert(graphCore.includes("MAX_EXTRACTION_UNITS") && graphCore.includes("punctuatedUnits.length") && graphCore.includes("supplementalLineUnits") && graphCore.includes("splitPunctuated") && graphCore.includes("isMarkdownUnit"), "heuristic extraction should supplement prose with bounded Markdown units without merging separate list items");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("incomingSourceId") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("acceptedNodeIds"), "source replacement should retain provenance identity for accepted knowledge");
assert(app.includes("normalizeSourceUri(rawUri)") && app.includes("parseTimestamp(reviewedDate)") && app.includes("Use a valid review date."), "source metadata edits should validate dates and normalize URIs before persistence");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("DEFAULT_GRAPH_TIMESTAMP"), "malformed graph timestamp repairs should be deterministic");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("normalizeId(edge.id) ||"), "imported relation IDs should be repaired");
assert(app.includes("downloadFile"), "projection download must remain wired");
assert(app.includes('document.querySelector("#copy-markdown")') && app.includes("Markdown projection copied to the clipboard"), "the current Markdown projection should be directly shareable");
assert(app.includes("Learning ledger") && app.includes("accepted: ${accepted.length}") && app.includes("rejected: ${rejected.length}"), "Markdown projections should preserve the learning ledger");
assert(app.includes("`fingerprint: ${graphFingerprint}`") && app.includes("graphFingerprint = fingerprintBackup(graph)"), "Markdown projections should expose a deterministic graph fingerprint");
assert(app.includes("## Graph health") && app.includes("Active-item provenance coverage"), "Markdown projections should expose privacy-safe graph health diagnostics");
assert(graphCore.includes("function buildReviewQueue(graph, limit = 20, now = Date.now(), unanchoredByItem = new Map())") && graphCore.includes("buildEvidenceGrounding(graph)") && graphCore.includes("inspectGraph(graph, { now: inspectionTime, includeReviewQueue: true })") && graphCore.includes("reviewCandidates.map(reviewQueueExportItem)") && graphCore.includes("HEALTH_BOOLEANS") && graphCore.includes("must be boolean") && graphCore.includes("export function validateHealthReport") && graphCore.includes("gate cannot pass with violations"), "health reports should use one bounded inspection pass and a runtime contract validator");
assert(app.includes("Import truncation:") && app.includes("restore the original export before editing"), "browser and Markdown projections should warn when normalization omitted imported items");
assert(app.includes("Malformed import entries:") && app.includes("inspect the original export"), "browser and Markdown projections should warn when normalization dropped malformed items");
assert(app.includes("health = null") && app.includes("graphHealth = health || inspectGraph(graph)") && app.includes("buildMarkdown(graph, { maxEvidenceChars: MAX_MARKDOWN_PREVIEW_EVIDENCE_CHARS, graphFingerprint: projectionFingerprint, health })"), "the workbench should reuse one health snapshot when rendering the live Markdown preview");
assert(app.includes("const inspectionTime = Date.now()") && app.includes("inspectGraph(graph, { now: inspectionTime, includeReviewQueue: true })") && app.includes("const reviews = derivedCacheHit ? workbenchDerivedCache.reviews : health.reviewQueue"), "the live workbench should use one bounded inspection pass for health and review candidates");
assert(app.includes("WORKBENCH_DERIVED_CACHE_TTL_MS") && app.includes("workbenchDerivedCache.expiresAt > inspectionTime") && app.includes("expiresAt = inspectionTime + WORKBENCH_DERIVED_CACHE_TTL_MS"), "time-sensitive workbench diagnostics should expire from the graph-derived cache");
assert(app.includes("recordHistory: false") && app.includes("expectedFingerprint: fingerprintBackup(incomingGraph)") && app.includes("A newer graph update won while stale state was being repaired"), "cross-tab stale graph repair should use optimistic version and fingerprint preconditions");
assert(app.includes("const graphFingerprint = fingerprintBackup(graph)") && app.includes("buildMarkdown(projectedGraph, { graphFingerprint })") && app.includes("graphFingerprint,") && app.includes("graphVersion: projectedGraph.version"), "vault projections should reuse one graph fingerprint across their Markdown and manifest outputs");
assert(app.includes("function buildVaultFiles(graph, { appVersion = \"unknown\" } = {})") && app.includes("function buildCanvasProjection(graph)") && app.includes("assertGraphTextExportBudget([graph]);") && app.includes("assertGraphTextExportBudget([history.at(-1), graph])") && !app.slice(app.indexOf("function buildCanvasProjection(graph)"), app.indexOf("\nasync function buildLearningVaultFiles")).includes("buildVaultFiles("), "large Markdown, vault, feedback, and diff projections should preflight source/evidence bytes while direct Canvas export remains independent of full-vault serialization");
assert(app.includes('.forEach((edge) => relations.set(edge.id, allocate("Relations", edge.id, "relation")))') && app.includes("paths.relations.get(edge.id)"), "relation projection paths should be keyed by stable IDs rather than object identity");
assert(app.includes("MAX_ZIP_FILES") && app.includes("files.length > MAX_ZIP_FILES") && app.includes("typeof file.content !== \"string\"") && app.includes("archiveLimit"), "the Obsidian ZIP writer should bound file count, validate file records, and sanitize archive-limit configuration");
assert(app.includes("### Reusable memory") && app.includes("learningLines"), "Markdown projections should expose reusable learning memory");
assert(app.includes("Learning/review-ledger.md") && app.includes("type: learning-ledger") && app.includes("const learningDecisionExamples = buildFeedbackDataset(projectedGraph).examples"), "Obsidian projections should expose a complete reusable-review ledger derived from both live decisions and detached learning memory");
assert(app.includes("Revision history") && app.includes("graph.revisions.map((revision)"), "Markdown projections should preserve bounded revision history");
assert(app.includes("```mermaid") && app.includes("mermaidNodes") && app.includes(".slice(0, 300)"), "Markdown projections should include a bounded Mermaid graph view");
assert(app.includes("Visual graph omits") && app.includes("concept and relation indexes above remain complete"), "Markdown projections should disclose bounded Mermaid omissions without implying the indexes are incomplete");
assert(app.includes("safeMarkdownLabel") && app.includes("quoteMarkdown"), "Markdown projections should escape link delimiters and multiline evidence");
assert(app.includes("safeMarkdownUri") && app.includes("https?:\\/\\/"), "Markdown projections should render only safe HTTP source URI links");
assert(app.includes("normalizeSourceUri(uri)") && app.includes("normalized = normalizeSourceUri(uri)"), "browser projections should reuse the canonical source URI validator");
assert(app.includes("Source URI: ${safeMarkdownUri(doc.uri)}"), "Obsidian source notes should expose safe source URI links");
assert(app.includes("MAX_MARKDOWN_PREVIEW_EVIDENCE_CHARS") && app.includes("Evidence preview truncated"), "live Markdown previews should bound rendered evidence");
assert(app.includes("if (evidenceTruncated) break"), "Markdown evidence rendering should stop after its output budget is exhausted");
assert(app.includes("MAX_MARKDOWN_EXPORT_EVIDENCE_CHARS"), "full Markdown exports should bound evidence accumulation");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("conflicts"), "evaluation reports should disclose contradictory feedback");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("stableMatchKey") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("candidateIndexes.length") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("candidateOwners") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("queueIndex++"), "evaluation matching should prioritize constrained one-to-one matches deterministically");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("MAX_EVALUATION_MATCH_COMPARISONS") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("comparison safety limit"), "evaluation matching should fail closed before oversized pairwise work exhausts resources");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("hasDetailedIdentity") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("forwardEndpointMatches") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("reverseEndpointMatches") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("example.sourceLabel"), "evaluation should validate portable relation labels and endpoint pairs even when relation IDs differ");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("export const relationSemanticKey") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("knownEdges = new Map(graph.edges.map((edge) => [relationSemanticKey"), "graph relation identity should use one shared semantic key");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("node.status !== \"rejected\"") && fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("edge.status !== \"rejected\""), "evaluation accepted recall should exclude explicitly rejected candidates");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("evidence.sources?.includes(sourceId)"), "evaluation evidence coverage should require source-anchored evidence");
assert(fs.readFileSync(new URL("../evaluation.js", import.meta.url), "utf8").includes("reviewedPrecision") && fs.readFileSync(new URL("../experiments/compare-evaluations.mjs", import.meta.url), "utf8").includes("reviewedPrecision"), "evaluation promotion should expose reviewed candidate precision");
assert(graphCore.includes('new Intl.Segmenter("und"'), "multilingual extraction should use an explicit locale-neutral segmenter");
assert(graphCore.includes("const stableLabelId") && graphCore.includes("canonicalLabelKey(label)"), "long concept labels should receive collision-resistant generated identities");
assert(app.includes("MAX_RENDERED_GRAPH_NODES") && app.includes("MAX_RENDERED_GRAPH_EDGES") && app.includes("view capped:") && app.includes("not shown") && app.includes("refine search to inspect more"), "large graphs should bound DOM rendering and quantify omitted matches");
assert(app.includes("manualNodes = graph.nodes") && app.includes("searchIndex.nodeText(node).includes(query)"), "manual relation selectors should honor graph search filters");
assert(app.includes('else selectSource(candidate.id)') && app.includes('graphSearchQuery = "";') && app.includes('document.querySelector("#graph-search").value = "";') && app.indexOf('document.querySelector(".mini-button[data-view=\'list\']").click();') < app.indexOf('if (candidate.kind === "node") selectGraphNode(candidate.id);'), "review-next should reveal and scroll candidates before selecting them");
assert(app.includes("zipStore"), "Obsidian vault export must remain wired");
assert(app.includes("const compare = (left, right)") && app.includes("[...graph.nodes]") && app.includes("sort((left, right) => compare(left.id, right.id))"), "Obsidian projection path allocation should be independent of graph collection order");
assert(app.includes("const projectedGraph =") && app.includes("JSON.stringify(buildGraphExport(projectedGraph"), "Obsidian vault content should use canonical graph collection ordering");
assert(app.includes("graph = canonicalizeGraphForExport(graph)") && app.includes("function buildMarkdown"), "direct Markdown projections should canonicalize graph collection ordering");
assert(app.includes("buildLearningVaultFiles") && app.includes("Learning/${note.id}.md"), "Obsidian vaults should include the versioned learning notes");
assert(app.includes('name: "README.md"') && app.includes("Open [[_index]]") && app.includes("[[Graph.canvas]]") && app.includes("Source of truth"), "Obsidian vaults should include orientation, Canvas viewing, and round-trip guidance");
assert(app.includes("vault-manifest.json") && app.includes("const graphFingerprint = fingerprintBackup(graph)") && app.includes("graphFingerprint,") && app.includes("appVersion: normalizedAppVersion") && app.includes("stampLearningVaultStatus") && app.includes("unavailableNotes"), "Obsidian vaults should include a graph-bound manifest with bounded producer metadata and learning-projection completeness");
assert(app.includes("const generatedAt = graph.updatedAt || DEFAULT_GRAPH_TIMESTAMP"), "vault manifests should use graph revision time or the deterministic graph timestamp for reproducible exports");
assert(html.includes('id="download-jsonld"') && html.includes('id="download-redacted-jsonld"') && app.includes("buildJsonLd(graph, { appVersion: releaseInfo.version })") && jsonLdProjection.includes("export function buildJsonLd(graph, { appVersion } = {})") && app.includes("application/ld+json"), "the workbench should offer full and redacted JSON-LD projections");
assert(html.includes('id="projection-privacy-note"') && html.includes("Full exports include source text") && html.match(/id="download-diff"[^>]+aria-describedby="projection-privacy-note"/)?.[0] && (html.match(/aria-describedby="projection-privacy-note"/g) || []).length >= 7, "source-bearing export controls should expose a persistent accessible privacy warning");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("Embedded graph JSON metadata does not match its vault manifest") && app.includes("vault.graphError"), "vault imports should verify embedded graph JSON metadata against its manifest before applying feedback");
assert(app.includes("LEARNING_NOTE_TIMEOUT_MS") && app.includes("MAX_LEARNING_NOTE_CHARS") && app.includes("MAX_LEARNING_NOTES_CHARS") && app.includes("readBoundedTextResponse") && app.includes("getReader") && app.includes("reader.cancel") && app.includes("const cancelReader =") && app.includes("abortReader") && app.includes("Promise.race([response.arrayBuffer(), abortPromise])") && app.includes("const readChunk =") && app.includes("pendingReadSettled") && app.includes("signal?.addEventListener") && app.includes("!Number.isFinite(declaredLength)") && app.includes("cannot validate response encoding safely") && app.includes("isReadableSameOriginResponse(response)") && !app.includes("return response.text()") && app.includes("request timed out"), "learning-note vault fetches should be bounded in memory, fail closed on encoding, reject cross-origin responses, and cancel stalled readers");
assert(app.includes("declaredResponseBytes") && app.includes("const normalized = typeof header === \"string\" ? header.trim() : \"\""), "browser response size checks should not coerce missing content-length headers to zero");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("/^\\d+$/.test(normalizedDeclaredHeader)") && app.includes("/^\\d+$/.test(normalized)"), "response size guards should accept only decimal content-length values");
assert(curriculum.includes("node experiments/tiny-training.mjs"), "the interactive training note should point to the runnable training artifact");
assert(app.includes("archive exceeds the 50 MB safety limit") && app.includes("download-vault"), "Obsidian vault exports should enforce their import size limit");
assert(app.includes("estimatedArchiveBytes") && app.includes("addVaultFile") && app.includes("buildLearningVaultFiles({ estimatedArchiveBytes"), "vault projection should preflight cumulative archive size while generating files");
assert(app.includes("VAULT_TOO_LARGE") && app.includes("if (error?.code === \"VAULT_TOO_LARGE\") throw error"), "vault size failures should not be downgraded to missing-note warnings");
assert(app.includes("archiveLength = offset + centralDirectory.length + end.length"), "vault exports should check size before final byte concatenation");
assert(app.includes("nextOffset + nextCentralLength + 22 > archiveLimit"), "vault exports should preflight size before retaining all archive parts");
assert(app.includes("estimatedArchiveBytes += 30 + nameBytes.length + contentBytes.length + 46 + nameBytes.length"), "vault exports should preflight encoded file sizes before ZIP allocation");
assert(app.includes("MAX_EXPORT_BYTES") && app.includes("This export exceeds the 50 MB safety limit"), "text projections should enforce an export size limit");
assert(app.includes("function downloadBytes(filename, bytes, type)") && app.includes("ArrayBuffer.isView(bytes)") && app.includes("Number.isSafeInteger(byteView.byteLength)") && app.includes("byteView.byteLength > MAX_EXPORT_BYTES") && app.includes("function revokeObjectUrlLater") && app.includes("URL.revokeObjectURL(url)"), "binary projections should require actual byte data, enforce the export size limit, and clean up object URLs even when download triggers fail");
assert(app.includes("buildFeedbackDataset"), "feedback dataset export must remain wired");
assert(app.includes("truncatedExamples") && app.includes("omitted at the export limit") && app.includes("partial dataset anyway"), "feedback export/import should disclose and confirm bounded omission");
assert(app.includes("dataset fingerprint does not match its examples"), "feedback imports must verify dataset identity before applying learning decisions");
assert(app.includes("aliases: node.aliases"), "feedback export should preserve concept aliases");
assert(app.includes("applyFeedbackDataset") && app.includes("Feedback dataset imported") && app.includes("contradictory decision") && app.includes("already up to date") && app.includes("revision limit. Export a backup before importing more feedback"), "feedback conflicts, idempotent imports, and revision limits should be surfaced accurately");
assert(app.includes("freshness or deterministic tie-breaking selected a value") && readme.includes("newest") && readme.includes("timestamp wins") && readme.includes("stable deterministic"), "contradictory-feedback resolution rules should be explicit to operators");
assert(!app.includes("asArray(imported.history).slice(-HISTORY_LIMIT"), "backup history should be bounded after malformed entries are filtered");
assert(app.includes("const restoredHistoryCount = graphStore.readHistory().length"), "backup restore status should report actually retained undo history");
assert(app.includes("unmatched or invalid example"), "feedback imports should surface skipped examples");
assert(app.includes('document.querySelector("#document-file").value = ""'), "successful imports should reset the file input");
assert(app.includes("MAX_ACTIVE_FEEDBACK_CONCEPTS") || fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("MAX_ACTIVE_FEEDBACK_CONCEPTS"), "adaptive extraction should bound active feedback hints");
assert(server.includes("/api/extract-graph"), "reference extraction endpoint must remain wired");
assert(dockerfile.includes("HEALTHCHECK") && dockerfile.includes("readyz") && dockerfile.includes("parseConfiguredBoundedInteger") && dockerfile.includes("setting.valid"), "container health checks should reuse the validated server port parser and fail closed on invalid runtime configuration");
assert(dockerfile.includes("USER node"), "container should not run as root");
assert(dockerfile.includes('CMD ["node", "server.mjs"]'), "container should exec Node directly for reliable signal handling");
assert(/FROM node:22-alpine@sha256:[0-9a-f]{64}/.test(dockerfile) && dockerfile.includes(`ARG APP_VERSION=${packageManifest.version}`) && dockerfile.includes("ARG VCS_REF=unknown") && dockerfile.includes("ARG PUBLIC_REPOSITORY_URL=https://github.com/humblemat810/llm-wiki") && dockerfile.includes("ARG PUBLIC_DOCUMENTATION_URL=https://github.com/humblemat810/llm-wiki/blob/main/RUNBOOK.md") && dockerfile.includes('org.opencontainers.image.version="$APP_VERSION"') && dockerfile.includes('org.opencontainers.image.revision="$VCS_REF"') && dockerfile.includes('org.opencontainers.image.source="$PUBLIC_REPOSITORY_URL"') && dockerfile.includes('org.opencontainers.image.documentation="$PUBLIC_DOCUMENTATION_URL"') && dockerfile.includes("ENV BUILD_REVISION=$VCS_REF") && dockerfile.includes("ENV PUBLIC_REPOSITORY_URL=$PUBLIC_REPOSITORY_URL") && dockerfile.includes("STOPSIGNAL SIGTERM"), "container should use the maintained Node 22 line, remain digest-pinned, carry synchronized OCI release, revision, fork-aware source metadata into runtime, and preserve graceful SIGTERM shutdown");
assert(dockerignore.includes(".codex") && dockerignore.includes(".env") && dockerignore.includes("tests") && dockerignore.includes("benchmarks") && dockerignore.includes("llm-field-notes-*.zip") && dockerignore.includes("*.pem") && dockerignore.includes("*.sqlite") && dockerignore.includes("sbom.spdx.json") && dockerignore.includes("backups") && dockerignore.includes("!experiments/verify-backup.mjs"), "container context should exclude agent artifacts, secrets, development tests, benchmarks, private keys, databases, generated release evidence, local exports, and retain the public backup verifier");
assert(dockerfile.includes("COPY --chown=node:node . ."), "container application files should be owned by the non-root runtime user");
assert(server.includes("process.env.HOST"), "server host should be configurable for containers");
assert(server.includes("/livez") && server.includes("/readyz") && server.includes("Static app shell is unavailable") && server.includes("beginDrain"), "server should distinguish process liveness from app readiness and expose one drain primitive");
assert(server.includes("createRequire") && server.includes("APP_VERSION") && server.includes("BUILD_REVISION") && server.includes("llm_field_notes_build_info") && server.includes("llm_field_notes_build_revision_info"), "operational endpoints should expose the deployed application version and sanitized source revision");
assert(server.includes("EXTRACTOR_MODES") && server.includes("llm_field_notes_extractor_mode") && server.includes("extractorMode"), "operational metrics should expose a bounded active extraction lane");
const providerAdapter = fs.readFileSync(new URL("../provider-adapter.js", import.meta.url), "utf8");
assert(providerAdapter.includes("x-request-id") && providerAdapter.includes("REQUEST_ID_PATTERN"), "provider requests should carry only validated server request IDs for incident correlation");
assert(server.includes("createConfiguredProviderExtractor")
  && providerAdapter.includes("EXTRACTOR_PROVIDER_URL")
  && providerAdapter.includes("response_format")
  && providerAdapter.includes('redirect: "error"')
  && providerAdapter.includes('"cache-control": "no-store"')
  && providerAdapter.includes("EXTRACTOR_PROVIDER_INCLUDE_SOURCE_URI")
  && providerAdapter.includes("includeSourceUri && uri")
  && providerAdapter.includes("parseJsonWithUniqueKeys")
  && fs.existsSync(new URL("../tests/provider-adapter-smoke.mjs", import.meta.url))
  && security.includes("disable automatic HTTP redirects"), "the reference server should provide a credential-isolated, structured-output model adapter with redirect protection and regression coverage");
assert(server.includes("revision: BUILD_REVISION") && server.includes("rawBuildRevision.toLowerCase()"), "health and readiness responses should expose a canonical sanitized source revision");
assert(server.includes("server.isDraining") && server.includes("Server is draining"), "readiness should fail while the server drains");
assert(server.includes("DEFAULT_READINESS_TIMEOUT_MS") && server.includes("MAX_READINESS_TIMEOUT_MS") && server.includes("Readiness check timed out.") && server.includes("Promise.race"), "readiness validation should have its own bounded fail-closed deadline");
assert(server.includes("readinessTimeouts") && server.includes("READINESS_TIMEOUT") && server.includes("llm_field_notes_readiness_timeouts_total"), "readiness timeout failures should expose bounded structured and Prometheus diagnostics");
assert(server.includes("readinessFailures") && server.includes("READINESS_FAILED") && server.includes("llm_field_notes_readiness_failures_total"), "completed readiness failures should expose bounded structured and Prometheus diagnostics");
assert(server.includes("REQUEST_TIMEOUT_MS") && server.includes("HEADERS_TIMEOUT_MS") && server.includes("keepAliveTimeout"), "HTTP transport timeouts should be explicit");
assert(server.includes("REQUEST_ABORTED") && server.includes('request.once("aborted"'), "request-body reads should stop when clients disconnect");
assert(server.includes("export function canWriteResponse") && server.includes("response?.headersSent") && server.includes("!canWriteResponse(response)"), "responses should avoid writes after client disconnects or duplicate header emission");
assert(server.includes("SIGTERM") && server.includes("draining active requests"), "server should drain requests during shutdown");
assert(server.includes("EXTRACTOR_RATE_LIMIT") && server.includes('".canvas": "application/json; charset=utf-8"'), "server rate limit should be configurable and native Canvas downloads should use a JSON content type");
assert(server.includes("content-length") && server.includes("request.resume()") && server.includes("readBody(request, response)"), "server should reject declared oversized request bodies early");
assert(server.includes("size > MAX_BODY_BYTES") && server.includes("statusCode: 413") && server.includes("discardRequestBody(request, response)"), "server should reject chunked oversized request bodies while streaming");
assert(server.includes("discardRequestBody") && server.includes("discardError") && server.includes("discardedBytes > MAX_BODY_BYTES") && server.includes("response.once(\"finish\""), "rejected request bodies should preserve bounded keep-alive draining and terminate oversized uploads after the response");
assert(server.includes("requestContentType !== \"application/json\""), "server should validate the exact JSON media type");
assert(server.includes("durationMs") && server.includes("route: \"extract-graph\""), "server should support structured extraction logs");
assert(server.includes('event: "server-ready"') && server.includes("version: APP_VERSION") && server.includes("actualPort"), "standalone startup logs should identify the serving release and port");
assert(server.includes('event: "server-draining"') && server.includes('event: "server-stopped"') && server.includes('event: "server-stop-timeout"') && server.includes("drained: idle"), "standalone lifecycle logs should identify graceful and forced shutdown outcomes");
assert(server.includes("const safeLog") && server.includes("result?.catch?.(() => {})"), "logger failures should be isolated from request handling");
assert(server.includes("parseConfiguredBoundedInteger") && server.includes("EXTRACTOR_RATE_LIMIT") && server.includes("EXTRACTOR_TIMEOUT_MS") && server.includes("EXTRACTOR_CONCURRENCY"), "explicit numeric server settings should fail closed through one bounded parser");
assert(readme.includes("explicit malformed or out-of-range values fail standalone startup closed") && !readme.includes("Invalid environment or embedded-server values fall back to the 60/minute"), "operator documentation should describe fail-closed numeric configuration");
assert(publicOrigin.includes("allowLoopbackHttp") && server.includes("requireSecurePublicOrigin"), "public origin security should make loopback HTTP an explicit deployment-mode opt-in");
const deploymentConfig = fs.readFileSync(new URL("../scripts/check-deployment-config.mjs", import.meta.url), "utf8");
assert(deploymentConfig.includes("publicOriginIsLoopback") && server.includes("publicOriginIsExternal") && server.includes("productionMode"), "production-mode detection should account for externally exposed loopback-bound servers");
assert(rebuildAdapter.includes("preserveSourceContent") && rebuildAdapter.includes("replacement changed saved source content") && graphCore.includes("preserveSourceContent"), "automatic rebuilds should preserve authoritative saved source content while allowing inferred representation changes");
assert(rebuildAdapter.includes("const replacementInput = normalizeGraph(graph)") && rebuildAdapter.includes("extension points"), "automatic rebuilds should isolate extension replacement implementations from the committed graph");
assert(rebuildAdapter.includes("abortableExtract(extract, source, normalizeGraph(graph), signal)"), "automatic rebuilds should isolate extractor graph context from the committed graph");
assert(packageManifest.scripts["deployment:check"] === "node scripts/check-deployment-config.mjs"
  && deploymentConfig.includes("checkDeploymentConfig")
  && deploymentConfig.includes("non-loopback-production")
  && deploymentConfig.includes("EXTRACTOR_AUTH_TOKEN")
  && deploymentConfig.includes("METRICS_AUTH_TOKEN")
  && productionCheck.includes('"run", "deployment:check"'), "the canonical gate should include a fail-closed pre-traffic deployment configuration check");
assert(server.includes("invalidSetting[0]") && server.includes("must be an integer from") && server.includes("process.env.HOST.trim()"), "invalid port configuration should fail safe while preserving host normalization");
assert(server.includes('server.once("error"') && server.includes("failed to listen"), "server startup errors should be handled explicitly");
assert(server.includes("route: \"static\"") && server.includes("safeLog"), "unexpected static failures should use safe structured logging");
assert(server.includes("EXTRACTOR_TIMEOUT_MS") && server.includes("AbortController") && server.includes("clientAbortPromise") && server.includes("504"), "provider extraction should be bounded and cancellable even when clients disconnect before a provider settles");
assert(server.includes("const REQUEST_TIMEOUT_MS = 30000") && readme.includes("30-second receive window") && security.includes("separate 30-second receive timeout"), "request receive timeouts should remain shorter than provider execution timeouts and be documented at the public security boundary");
assert(server.includes('request.once("aborted"') && server.includes('response.once("close"'), "provider work should stop when clients disconnect");
assert(server.includes("!response.writableEnded"), "provider responses should guard disconnected sockets");
assert(server.includes("abortActiveExtractors"), "graceful shutdown should abort active provider work");
assert(server.includes("waitForIdle") && server.includes("Promise.all([closePromise"), "graceful shutdown should wait for active provider promises before clean exit");
assert(server.includes("closeIdleConnections"), "graceful shutdown should close idle keep-alive connections");
assert(server.includes("hasAllowedRequestOrigin") && server.includes("ORIGIN_REJECTED") && server.includes("sec-fetch-site"), "browser extractor requests should enforce a same-origin boundary");
assert(server.includes('const requestId = randomUUID();') && server.includes('safeLog({ requestId, status, route: "static"') && server.includes('uncaughtExceptionMonitor') && server.includes('event: "uncaught-exception"') && server.includes('process.on("unhandledRejection"') && server.includes('event: "unhandled-rejection"') && server.includes('shutdown("unhandled-rejection")'), "runtime application failures should retain response-correlated request IDs without logging request content, and fatal process faults should emit structured lifecycle events before bounded shutdown");
assert(browserSmoke.includes("applying reviewed learning to saved sources")
  && browserSmoke.includes("button[data-learning-action='rebuild']")
  && browserSmoke.includes("source(?:s)? rebuilt")
  && browserSmoke.includes("learning:\\s*1 accepted")
  && browserSmoke.includes("drilling full backup restore")
  && browserSmoke.includes("#download-backup")
  && browserSmoke.includes("llm-field-notes/backup@1")
  && browserSmoke.includes("Full backup restored"), "browser release smoke should exercise reviewed-learning rebuild and full backup restore through the user-facing persistence paths");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_RESPONSE_BYTES"), "remote extractor responses should be bounded before parsing");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("const abortReader") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("const cancelReader =") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("const readChunk =") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("pendingReadSettled") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("reader.cancel") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("readBoundedResponse(response, controller.signal)"), "remote response streaming should stop on cancellation, size failure, or provider timeout");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("const timeoutPromise = new Promise") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("externalAbortPromise") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("response = await Promise.race") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("Promise.resolve(cancellation).catch") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("cancelResponseBody(response)") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("if (!response.ok)"), "remote extractor timeouts, caller cancellation, terminal response cleanup, and retry cleanup should settle even when an underlying fetch or body cancellation ignores abort signals");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_DOCUMENT_TITLE_CHARS") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("rawTitle"), "remote extraction should reject oversized document titles instead of truncating provenance");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("TextEncoder"), "remote response limits should be measured in encoded bytes");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("if (!Number.isFinite(declaredLength))"), "non-streaming remote responses should require a declared size bound");
assert(server.includes('import { MAX_FEEDBACK_CHARS, MAX_REQUEST_BYTES, MAX_RESPONSE_BYTES } from "./extractor-adapter.js"'), "server and browser extraction limits should share one adapter contract");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("responseMediaType !== \"application/json\""), "remote extractor responses should enforce the exact JSON media type");
assert(server.includes("MAX_RESPONSE_BYTES") && server.includes("EXTRACTOR_RESPONSE_TOO_LARGE") && server.includes("Buffer.byteLength(JSON.stringify(responsePayload))"), "server extractor responses should use the shared output bound");
assert(server.includes("invalidDocumentUri") && server.includes("rawDocumentUri") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("Source URI is invalid or exceeds the 2,048 character limit."), "network extraction boundaries should reject invalid source URI metadata instead of dropping it");
assert(server.includes("MAX_FEEDBACK_EXAMPLES") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_FEEDBACK_EXAMPLES"), "feedback example limits should share the graph contract");
assert(server.includes("MAX_REQUEST_BYTES") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("export const MAX_REQUEST_BYTES"), "server and adapter should share the serialized request byte limit");
assert(server.includes("MAX_FEEDBACK_LABEL_CHARS") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("MAX_FEEDBACK_LABEL_CHARS"), "server and adapter should share the feedback label limit");
assert(graphCore.includes("MAX_CONCEPT_LABEL_CHARS") && graphCore.includes("MAX_CONCEPT_LABEL_CHARS = 120"), "graph normalization should centralize the concept-label bound");
assert(projection.includes("MAX_CONCEPT_LABEL_CHARS"), "Obsidian alias parsing should share the graph concept-label bound");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("FEEDBACK_TOO_LARGE") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("Reviewed feedback exceeds"), "remote extraction should reject oversized reviewed guidance instead of silently sending a partial context");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("hasImportIntegrityLoss") && fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("integrity?.dropped") && fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes('lastWriteMode = "integrity"'), "graph persistence should reject mutations that preserve imported truncation or dropped-entry loss until a clean restore is written");
assert(graphCore.includes("graph.integrity?.dropped") && graphCore.includes("importIntegrityLimit") && graphCore.includes("limited: importLimit"), "all graph-domain mutations should reject dropped-entry imports before attempting persistence");
assert(graphCore.includes("slugify(edge.label) !== hintLabel"), "rejected relation guidance should not suppress differently labeled relations between the same endpoints");
assert(graphCore.includes("slugify(hint.label) === slugify(resolvedLabel)") && graphCore.includes("slugify(hint.label) === slugify(label)"), "local extraction should apply rejected relation guidance to the matching semantic label only");
assert(graphCore.includes("trustedReviewTime = (value, now = Date.now())") && graphCore.includes("trustedReviewTime(example.lastReviewedAt, now)"), "learning guidance ordering should use the same explicit inspection timestamp as freshness filtering");
assert(graphStore.includes("captureRecoverySnapshot") && app.includes("captureRecoverySnapshot(text)") && app.includes("hasImportIntegrityLoss(importedGraph)"), "lossy graph imports should preserve the original JSON payload for recovery before normalized persistence");
assert(app.includes("graphWriteFailureMessage") && app.includes("This graph came from an incomplete import"), "the workbench should explain integrity-blocked writes and direct users to restore the original export");
assert(app.includes("const incompleteImportMessage") && app.includes("mutationLimitMessage(result"), "domain integrity limits should produce actionable restore guidance across UI mutation handlers");
assert(app.includes('const result = removeSource(currentGraph, removeSourceButton.dataset.removeSource);') && app.includes('mutationLimitMessage(result,\n        result.ambiguous'), "source removal should surface restore guidance when an incomplete import blocks the mutation");
assert(server.includes("normalizeExtractionForDocument") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("normalizeExtractionForDocument"), "browser and server extraction should preserve submitted document provenance metadata");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("retryBackoffDelay") && fs.readFileSync(new URL("../tests/extractor-smoke.mjs", import.meta.url), "utf8").includes("later retries should use bounded exponential backoff"), "remote extraction retries should use bounded exponential backoff when Retry-After is absent");
assert(graphCore.includes("EXTRACTION_EVIDENCE_TOO_LARGE") && graphCore.includes("EXTRACTION_SOURCES_TOO_LARGE") && graphCore.includes("EXTRACTION_EVIDENCE_TEXT_TOO_LARGE") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("EXTRACTION_EVIDENCE_SOURCES_TOO_LARGE"), "provider extraction should reject oversized nested evidence and provenance instead of silently clipping it");
assert(app.includes("renderInspector"), "evidence inspector must remain wired");
assert(app.includes("mergeConcepts") && app.includes("data-merge-node"), "the workbench should expose undoable concept merging");
assert(app.includes('class="row-inspect" data-select-node') && app.includes('class="row-inspect" data-select-edge') && app.includes('aria-current="${selected ? "true" : "false"}"') && app.includes('document.querySelectorAll(".relation-row")') && app.includes('selectedRow.scrollIntoView({ block: "nearest" })'), "graph list inspection should use native keyboard controls and preserve selected state for assistive technology");
assert(app.includes('aria-label="Confirm relation') && app.includes('aria-label="Dismiss relation'), "relation feedback controls should identify their relation context");
assert(app.includes('document.querySelector("#node-list").addEventListener("keydown"') && app.includes('document.querySelector("#relation-list").addEventListener("keydown"') && fs.readFileSync(new URL("../styles.css", import.meta.url), "utf8").includes(".row-inspect:focus-visible"), "graph list controls should remain keyboard navigable with visible focus");
assert(app.includes("if (event.detail === 0) document.querySelector(\"#inspector-panel\").focus()"), "keyboard inspection should move focus to the updated inspector without stealing pointer focus");
assert(app.includes('document.querySelector("#graph-canvas").addEventListener("keydown"') && app.match(/document\.querySelector\("#inspector-panel"\)\.focus\(\);/g)?.length >= 3, "graph keyboard activation and review navigation should leave focus in the inspector");
assert(app.includes("event.target.value = \"\";") && app.includes("That file could not be loaded."), "failed file selection should be reset so the same file can be retried");
assert(app.includes("A single text/Markdown file is an editor seed") && app.includes("the title and text fields") && app.includes("authoritative document") && app.includes("if (files.length === 1)") && app.includes("files loaded. Build the batch when ready."), "single-file imports should build from the edited editor draft while multi-file selections remain queued");
assert(app.includes("Your editor draft was preserved; retry the build or clear the model endpoint"), "remote extraction failures should disclose that the editable draft remains safe to retry");
assert(contributing.includes("Resource not accessible by integration") && contributing.includes("pull_request_target") && contributing.includes("write-all"), "contributor guidance should distinguish forked CodeQL telemetry warnings from failed scans without recommending unsafe permission broadening");
assert(html.includes('id="graph-canvas"') && html.includes('id="graph-canvas" viewBox="0 0 720 430" role="group"') && html.includes('data-view="graph" aria-pressed="true" aria-controls="graph-canvas-wrap node-list relation-list source-list"') && app.includes('focusable="true" role="button" aria-current="${selected ? "true" : "false"}"'), "interactive graph nodes should remain exposed to assistive technology with selected state and view toggles should identify their controlled regions");
assert(app.includes("workbench.addEventListener(\"click\"") && app.includes("stopImmediatePropagation()") && app.includes("sourceReplacementInFlight") && app.includes("syncWorkbenchBusy"), "in-flight graph builds and source replacements should lock competing mutations and expose busy state");
assert(app.includes("MAX_RELEASE_METADATA_CHARS") && app.includes("readBoundedTextResponse(response, MAX_RELEASE_METADATA_CHARS") && app.includes("isReadableSameOriginResponse(response)") && app.includes("Release metadata crossed the app origin boundary.") && app.includes("parseTimestamp(value.date)") && app.includes("Release metadata is oversized.") && app.includes("Release metadata is invalid."), "startup release metadata should be bounded, same-origin, and shape-validated");
assert(app.includes("data-select-source"), "source inspection links must remain wired");
assert(app.includes("data-inspector-feedback") && app.includes("REVIEW DECISION"), "the inspector should expose direct feedback actions for the active-learning loop");
assert(app.includes("function persistFeedbackDecision") && app.includes('persistFeedbackDecision("node"') && app.includes('persistFeedbackDecision("edge"'), "all feedback surfaces should share one conflict-safe persistence path");
assert(app.includes("document-uri") && app.includes("inspector-source-uri"), "source URI entry and editing must remain wired");
assert(app.includes('document.querySelector("#document-uri").value = "";'), "loading a new sample or file should not retain stale source URI provenance");
assert(app.includes("const rawSourceUri") && app.includes("Use a valid source URI under") && !app.includes("document.querySelector(\"#document-uri\").value.trim().slice(0, MAX_SOURCE_URI_CHARS)"), "browser source URI inputs should reject oversized values instead of truncating provenance");
assert(app.includes("const rawTitle") && app.includes("MAX_DOCUMENT_TITLE_CHARS"), "browser ingestion should reject oversized titles instead of silently truncating local provenance");
assert(app.includes("cleanupFilePicker") && app.includes("handleFilePickerFocus") && app.includes('window.addEventListener("focus", handleFilePickerFocus'), "source replacement file pickers should remove temporary inputs when the native dialog is canceled");
assert(app.includes("previewLimit"), "source previews must remain bounded");
assert(app.includes("pendingFiles"), "batch ingestion state must remain wired");
assert(app.includes("MAX_IMPORT_BYTES") && app.includes("file.size"), "browser imports should reject oversized files before reading them");
assert(app.includes("MAX_GRAPH_DOCUMENT_CHARS") && app.includes("document-text"), "graph ingestion should expose the aggregate source-text bound");
assert(app.includes("MAX_BATCH_FILES") && app.includes("MAX_BATCH_CHARS"), "batch imports should bound file count and aggregate text size");
assert(app.includes("const fileTitle") && app.includes("title over ${MAX_DOCUMENT_TITLE_CHARS} characters"), "batch ingestion should reject over-limit filename-derived titles consistently");
assert(app.includes("Replacement document titles must be no longer than ${MAX_DOCUMENT_TITLE_CHARS} characters.") && app.includes("{ title: fileTitle, text }"), "source replacement should validate filename-derived titles consistently across local and remote extraction");
assert(app.includes("[...graph.nodes].sort") && app.includes("[...graph.edges].sort"), "feedback exports should canonicalize live graph item ordering before applying the bounded dataset limit");
assert(app.includes("declaredFeedbackBytes") && app.includes("feedbackChars"), "feedback-note batches should enforce aggregate bounds before and during reads");
assert(app.includes("MAX_ZIP_BYTES"), "browser vault imports should reject oversized ZIPs before reading them");
assert(app.includes("renderWorkbenchUnsafe"), "workbench error boundary must remain wired");
assert(app.includes("data-edit-source"), "source metadata editing must remain wired");
assert(app.includes("conflicting label") && app.includes("That concept label already exists") && app.includes("item.aliases"), "label and alias collisions should be surfaced instead of creating ambiguous concepts");
assert(app.includes("That relation already exists for these concepts") && app.includes("slugify(item.label) === slugify(nextLabel)"), "relation label collisions should be surfaced instead of silently merging semantic edges");
assert(app.includes("inspector-source-quality") && app.includes("inspector-source-reviewed"), "source quality and review-date editing must remain wired");
assert(app.includes("data-remove-source"), "source removal must remain wired");
assert(app.includes("replaceSource") && app.includes("data-replace-source"), "source replacement must remain wired");
assert(app.includes("diffGraphs") && html.includes('id="download-diff"'), "revision diff export must remain wired");
assert(app.includes("revision-diff-preview") && app.includes("LAST DIFF"), "the workbench should show a bounded inline latest revision diff");
assert(app.includes("const integrityChanges") && app.includes("integrity: ${kind}.${key}"), "the live revision diff should surface truncation and malformed-entry diagnostic changes");
assert(app.includes("redactGraph") && html.includes('id="download-redacted"'), "privacy-preserving graph export must remain wired");
assert(app.includes("That graph's fingerprint does not match its contents") && graphCore.includes("matchesGraphFingerprint") && app.includes("buildGraphExport(graph, { appVersion: releaseInfo.version })"), "graph JSON imports and exports should verify deterministic integrity fingerprints");
assert(graphCore.includes("buildBackupEnvelope") && graphCore.includes("validateBackupEnvelope") && app.includes("buildBackupEnvelope(graphStore.read(), graphStore.readHistory()") && app.includes("validateBackupEnvelope(imported, { label: \"That backup\" })") && app.includes("canonicalizeGraphForExport(graphStore.read())"), "direct graph and backup exports should use canonical top-level collection ordering and strict backup-envelope imports");
assert(app.includes('name: "graph.json", content: JSON.stringify(buildGraphExport(projectedGraph'), "Obsidian vault graph JSON should preserve its manifest integrity fingerprint and producer metadata");
assert(app.includes('name: "Graph.canvas"') && app.includes("canvasNodes") && app.includes("canvasEdges"), "Obsidian vaults should include a native Canvas projection alongside authoritative graph JSON");
assert(app.includes("buildGraphExport(graph, { appVersion: releaseInfo.version })"), "direct graph JSON exports should retain producer provenance");
assert(app.includes('name: "graph.jsonld", content: JSON.stringify(buildJsonLd(projectedGraph, { appVersion: normalizedAppVersion })'), "Obsidian vaults should include the JSON-LD projection");
assert(graphCore.includes("graph.learning.examples = graph.learning.examples.map(redactEvidence)") && graphCore.includes("Array.isArray(item.evidence)"), "redaction must scrub reusable-learning evidence as well as graph-item evidence");
assert(graphCore.includes("if (graph.redacted === true)") && graphCore.includes('title: "Redacted source"') && graphCore.includes('text: "",') && graphCore.includes("uri: null"), "redacted markers should enforce the privacy boundary during graph normalization, including source titles");
assert(html.includes('id="download-health"') && app.includes("buildHealthReport(graph") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("export function buildHealthReport") && app.includes("appVersion:"), "privacy-safe graph health export must remain wired and content-bound");
assert(html.includes('id="download-redacted-vault"') && app.includes("llm-field-notes-redacted-vault.zip"), "privacy-preserving Obsidian vault export must remain wired");
assert(html.includes('id="download-canvas"') && html.includes('id="download-redacted-canvas"') && app.includes('downloadFile("Graph.canvas"') && app.includes('downloadFile("Graph-redacted.canvas"') && app.includes("buildCanvasProjection") && app.includes("standalone: true") && app.includes("projection-provenance") && app.includes("const canvasNodeIds = new Map(projectedNodes.map((node, index) => [node.id, `concept-${index}`]))") && app.includes("Graph fingerprint:"), "the workbench should offer validated full and redacted self-contained native Obsidian Canvas projections with traceable provenance");
assert(app.includes("Redacted projection") && app.includes("redacted: ${graph.redacted === true}"), "redacted Markdown projections should disclose their privacy state");
assert(app.includes("health.redacted") && app.includes("redacted source content"), "graph health should retain redaction visibility");
assert(app.includes("buildCompactFeedbackDataset") && html.includes('id="download-compact-feedback"'), "compact feedback export must remain wired");
assert(app.includes("datasetFingerprint: fingerprintFeedbackExamples(examples)"), "compact feedback exports must recompute identity after removing source material");
assert(fs.readFileSync(new URL("../ARCHITECTURE.md", import.meta.url), "utf8").includes("explicit privacy levels"), "architecture should document projection privacy levels");
assert(html.includes('title="Includes evidence and source references"') && html.includes('title="Removes evidence and source references"'), "feedback export privacy boundaries should be visible before download");
assert(app.includes("This graph is redacted"), "redacted graph imports should disclose unavailable source content");
assert(fs.existsSync(new URL("../experiments/diff-graphs.mjs", import.meta.url)), "the graph diff CLI should remain deliverable");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("Replacing an updated source file"), "source replacement should be documented for users");
assert(app.includes("commitManualGraph") && app.includes("relationSemanticKey(source, target, label)") && app.includes("relationSemanticKey(edge.source, edge.target, edge.label)"), "manual graph editing must reuse the shared endpoint-order-insensitive relation identity");
assert(graphCore.includes('kind === "edge" && graph.integrity.ambiguousEdgeIds.includes(normalizedId)') && graphCore.includes('normalizedExample.kind === "relation"') && graphCore.includes("ambiguousEdgeIds.includes(normalizedExample.id)") && projection.includes('graph.integrity.ambiguousEdgeIds.includes(feedback.id)'), "direct, bulk, and Obsidian relation feedback should fail closed for ambiguous imported edge IDs");
assert(graphCore.includes("const ambiguousEdgeIds = new Set(graph.integrity.ambiguousEdgeIds)") && graphCore.includes("!ambiguousEdgeIds.has(edge.id)"), "review queues and extractor guidance should omit ambiguous imported relation IDs");
assert(graphCore.includes("!ambiguousSourceIds.has(document.id)") && graphCore.includes("document.quality === \"unknown\" || isStaleSourceReview(document)"), "review queues should omit ambiguous imported source IDs while retaining ordinary source review candidates");
assert(app.includes("inspectGraph"), "graph health diagnostics must remain wired");
assert(app.includes("health.ambiguousLabels") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("ambiguousLabels"), "ambiguous concept labels should be visible in graph health");
assert(app.includes("MAX_SEARCH_TEXT_CHARS") && app.includes("MAX_SEARCH_EVIDENCE_CHARS"), "graph search indexing should bound evidence text per item");
assert(app.includes("health.ambiguousSourceIds") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("ambiguousSourceIds"), "ambiguous source IDs should be visible in graph health");
assert(app.includes("health.ambiguousSourceReferences") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("ambiguousSourceReferences"), "ambiguous provenance references should be visible in graph health");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("provenanceItems") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("supportedNodes.length + supportedEdges.length"), "provenance coverage should include active graph items, not only evidence records");
assert(app.includes("sourceReviewCoverage"), "graph health should expose source review coverage");
assert(app.includes("sourceQualitySummary"), "graph health should expose source quality distribution");
assert(!fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("Add a source-quality label"), "completed source metadata work should not remain in the roadmap");
assert(app.includes("reviewQueue") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("unresolved provenance"), "active-learning review queue should prioritize and explain provenance gaps");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("REVIEW_STALE_DAYS") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("stale"), "active-learning review queue should revisit stale human decisions");
assert(app.includes("Review next · ${candidate.reason}"), "review queue reasons should be visible when navigating to the next item");
assert(app.includes("data-review-action"), "review-next navigation must remain wired");
assert(app.includes("applyFeedback"), "feedback behavior must remain centralized in the graph core");
assert(app.includes("feedback} feedback"), "feedback state should be visible in the workbench");
assert(app.includes("node.lastReviewedAt") && app.includes("edge.lastReviewedAt") && app.includes("const reviewedAt = example.lastReviewedAt") && app.includes("last_reviewed: ${node.lastReviewedAt || \"\"}"), "human review timestamps should remain visible and exportable");
assert(html.includes('id="manual-edge-label"') && html.includes('aria-label="Relation label"'), "manual relation labels should have an accessible name");
assert([...html.matchAll(/<button\b[^>]*>/g)].every((match) => /\stype="button"/.test(match[0]) || /\stype="submit"/.test(match[0])), "static buttons should declare their form behavior explicitly");
assert(app.includes("node.status = \"accepted\"") && app.includes("edge.status = \"accepted\""), "human label edits should enter the reusable accepted-feedback loop");
assert(app.includes("syncLearningRelationLabels(graph).graph") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("syncLearningRelationLabels"), "concept corrections should keep relation learning endpoint labels synchronized");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("normalizeAliases(example.aliases)"), "feedback aliases should be bounded and deduplicated before normalization");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("const nodeById = new Map(graph.nodes.map"), "feedback imports should index graph items before applying examples");
assert(app.includes("buildExtractorFeedback") && app.includes("includeStale: false") && !app.includes("buildFeedbackDataset(graph).examples"), "extraction requests should use compact fresh reviewed feedback");
assert(graphCore.includes("includeGuidanceItem") && graphCore.includes("isStaleGuidanceItem") && graphCore.includes("const isStaleGuidanceItem = (item, now = Date.now()) => isStaleLearningExample(item, now)"), "stale or undated reusable memory and live decisions should remain auditable but be excluded from new extraction guidance");
assert(graphCore.includes("feedbackContextStatsForGraph(graph, { includeStale: false, now })"), "health guidance counts should match the fresh extractor context timestamp");
assert(app.includes("feedbackContextExcluded") && app.includes("withheld pending review"), "health and Markdown projections should disclose guidance withheld pending review");
assert(app.includes("const guidanceContext = [") && app.includes('].filter(Boolean).join("; ")'), "Markdown guidance diagnostics should use a stable readable summary");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("thresholdMaximums") && fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("maximum === undefined || number <= maximum"), "health CLI thresholds should enforce published maxima");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("HEALTH_GATE_LIMITS"), "health CLI threshold maxima should reuse runtime health gate limits");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("--max-review-queue-truncated") && fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("health.reviewQueueTruncated"), "health CLI should gate incomplete review queues");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("--max-evidence-grounding-truncated") && fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("health.evidenceGroundingTruncated"), "health CLI should gate sampled evidence grounding");
assert(fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("--max-feedback-context-truncated") && fs.readFileSync(new URL("../experiments/inspect-graph.mjs", import.meta.url), "utf8").includes("health.feedbackContextTruncated"), "health CLI should gate incomplete extractor guidance");
assert(graphCore.includes("export const HEALTH_GATE_LIMITS") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("Object.entries(HEALTH_GATE_LIMITS)") && fs.readFileSync(new URL("../scripts/check-contracts.mjs", import.meta.url), "utf8").includes("health ${key} gate"), "health gate runtime/schema parity should be exhaustive in the shared contract audit");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes('node.status === "accepted" || node.status === "rejected"'), "extractor feedback should exclude neutral inferred items");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("ambiguousConceptKeys"), "adaptive extraction should reject ambiguous feedback keys");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("portableFeedbackKey") && fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("relationSemanticKey("), "unmatched feedback should canonicalize portable relation identities before entering learning memory");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("const feedback = []"), "extractor feedback should balance concepts and relations under the shared cap");
assert(app.includes("applyObsidianFeedback"), "Obsidian feedback import must remain wired");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes('fields.type === "source"'), "source projection metadata should remain importable");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("manifestError") && app.includes("vault.manifest.graphFingerprint"), "vault imports should surface invalid or stale manifest identity");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("projectionMetadataError") && app.includes("feedback.graphFingerprint"), "individual Obsidian notes should surface invalid or stale projection identity");
assert(projection.includes("Embedded graph JSON is missing; JSON-LD cannot be verified"), "vault imports should disclose unanchored JSON-LD projections");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("feedback.source === item.source") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("feedback.target === item.target") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("fields.source.trim()"), "relation projection imports should preserve and validate both endpoints");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("feedback.source === item.target") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("feedback.target === item.source"), "relation projection imports should preserve endpoint-order-insensitive identity");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("feedback.fingerprint !== source.fingerprint") && app.includes('`fingerprint: ${JSON.stringify(doc.fingerprint)}`'), "source projection imports should bind metadata edits to the source fingerprint");
assert(app.includes("staleVault || unverifiedVault") && app.includes("staleProjection || unverifiedProjection") && app.includes("window.confirm"), "stale or unverifiable Obsidian projections should require explicit confirmation before applying edits");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("duplicateField"), "Obsidian frontmatter should reject duplicate keys");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("Object.hasOwn(fields, \"status\")") && fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("hasReviewDate"), "Obsidian frontmatter should reject invalid mutation fields");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("MAX_FEEDBACK_NOTE_CHARS"), "Obsidian feedback notes should have an explicit parser bound");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("mismatched central and local filenames"), "ZIP imports should validate central and local filename agreement");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes("overlapping its central directory"), "ZIP imports should reject file data overlapping the central directory");
assert(fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8").includes('name.includes("\\\\")'), "ZIP imports should reject Windows-style unsafe paths");
assert(app.includes("parseObsidianVault"), "ZIP vault feedback import must remain wired");
assert(app.includes("download-redacted-markdown") && app.includes("redactGraph(graphStore.read())"), "the workbench should offer a privacy-safe Markdown projection");
assert(app.includes("function assertGraphTextExportBudget") && app.includes("assertGraphTextExportBudget([backup.graph, ...backup.history])") && app.includes("assertGraphTextExportBudget([graph])"), "large graph exports should preflight source and evidence bytes before serialization");
assert(app.includes("looksLikeObsidianFeedback") && app.includes("Obsidian-shaped notes must be valid"), "malformed Obsidian-shaped notes should fail closed instead of becoming source documents");
const projectionAdapter = fs.readFileSync(new URL("../projection-adapter.js", import.meta.url), "utf8");
assert(projectionAdapter.includes("looksLikeObsidianFeedback") && projectionAdapter.includes("fields.last_reviewed.length > MAX_TIMESTAMP_CHARS") && projectionAdapter.includes("typeof fields.last_reviewed !== \"string\""), "all Obsidian note types should reject malformed review dates and expose shape detection");
assert(projectionAdapter.includes("normalizedUri") && projectionAdapter.includes("rawUri !== undefined") && projectionAdapter.includes("!normalizedUri"), "direct source feedback should reject unsafe URI values instead of silently clearing provenance");
assert(projectionAdapter.includes(".filter(Boolean).sort()") && projectionAdapter.includes("const existingAliases"), "Obsidian alias feedback should canonicalize set-like aliases without dropping established aliases");
assert(app.includes("Select an Obsidian vault ZIP by itself"), "mixed ZIP selections must be rejected");
assert(app.includes("invalidFeedbackFiles") && app.includes("no feedback was applied"), "malformed ZIP feedback notes should fail closed instead of being silently skipped");
assert(app.includes("truncatedDocumentTitle") && app.includes("truncatedDocumentText") && app.includes("truncatedEvidenceText") && app.includes("document title") && app.includes("document text clipped"), "the live health strip should explain document-metadata and text-level import truncation");
assert(app.includes("conflictingItems") && app.includes("contradictory review statuses"), "the live health strip should disclose contradictory duplicate review records");
assert(app.includes("paths.relations.get(edge.id)"), "relation projections must have collision-safe stable editable notes");
assert(app.includes("broken source reference"), "broken provenance must be visible in the health strip");
assert(app.includes("readRecovery"), "corrupt graph recovery must be wired");
assert(app.includes("data-recovery-action"), "recovery actions must be visible in the workbench");
assert(app.includes('data-recovery-action="download"') && app.includes('if (action === "history-download")') && app.includes("downloadFile(\"llm-field-notes-recovery.json\""), "graph recovery actions should keep their canonical download contract");
assert(app.includes("readHistoryRecovery") && app.includes("history-download") && app.includes("history-dismiss"), "malformed undo history recovery must be visible and downloadable");
assert(app.includes("The undo history recovery could not be downloaded.") && app.includes("The recovery snapshot could not be downloaded."), "recovery downloads should report browser or export failures in the workbench status");
assert(app.includes("hasRecoverySuppression") && app.includes("snapshot exceeded the safety limit") && app.includes('backupButton.dataset.storageAction = "backup"'), "suppressed recovery snapshots should be disclosed with a backup action");
assert(app.includes('data-storage-action="backup"') && app.includes("exportBackupSnapshot"), "durable storage warnings should provide a direct validated backup action");
assert(app.includes("requestPersistentStorage") && app.includes("typeof storageManager?.persist !== \"function\"") && app.includes("typeof storageManager.persisted === \"function\"") && app.includes("PERSISTENCE_REQUEST_TIMEOUT_MS") && app.includes("Persistent storage request timed out."), "local graph durability requests should verify callable browser APIs and remain bounded");
assert(app.includes("persistenceRequestInFlight") && app.includes("persistenceRequested = await Promise.race") && app.includes("Some privacy-restricted browsers expose a throwing storage getter"), "browser persistence requests should distinguish successful grants from retryable denials, prevent overlapping attempts, and tolerate restricted storage getters");
assert(app.includes('typeof navigator.share === "function"'), "share action should verify the native share capability before invoking it");
assert(app.includes("showProgressStorageError") && app.includes("progress may not persist") && app.includes("pathProgressKey") && app.includes("event.key === progressKey"), "learning progress should disclose storage failures and synchronize across tabs");
assert(app.includes("const graphWriteSuccessMessage") && app.includes("Undo history was reduced; export a backup before leaving this tab."), "successful degraded graph writes should disclose reduced undo history immediately");
assert(app.includes("const reducedHistory") && app.includes("const ephemeralStorage = !hasPersistentStorage") && app.includes("const fallbackStorage = hasPersistentStorage && !hasDurableStorage") && app.includes("backupCheckpointStale") && app.includes('data-storage-action="backup"') && app.includes("ephemeralStorage || fallbackStorage || reducedHistory || storageDurabilityFailure || backupCheckpointStale"), "ephemeral and degraded storage, plus stale backup checkpoints, should provide one direct backup action");
assert(app.includes("The graph build failed; your saved graph was preserved.") && app.includes("Rebuild failed; your saved graph was preserved."), "unexpected build and rebuild failures should surface without leaving unhandled event-handler promises");
assert(html.includes('id="rebuild-sources"') && app.includes("async function rebuildSavedSources") && app.includes("Successful replacements will be saved together"), "the workbench should offer a bounded, atomic rebuild of saved sources using current feedback");
assert(app.includes('rebuildSourcesButton.setAttribute("aria-busy", "true")') && app.includes("Rebuilding source ${sourceIndex + 1} of ${total}"), "saved-source rebuilds should expose accessible progress and busy state");
assert(html.includes('id="rebuild-sources" type="button" aria-busy="false"'), "saved-source rebuild should declare its idle busy state in the initial markup");
assert(app.includes("if (initialGraph.redacted)") && app.includes("restore a full backup or original sources before rebuilding"), "saved-source rebuilds must fail closed when source text was redacted");
assert(app.includes("const incompleteImport") && app.includes("Restore the original export before rebuilding saved sources"), "saved-source rebuilds must reject incomplete imports before sending any source to an extractor");
assert(app.includes("initialGraph.integrity.ambiguousSourceIds") && app.includes("Repair the original export before rebuilding saved sources"), "saved-source rebuilds must reject ambiguous source identities before extraction");
assert(app.includes("const rebuildFeedback = buildExtractorFeedback(initialGraph, { includeStale: false })") && app.includes("feedback: rebuildFeedback"), "saved-source rebuilds should snapshot fresh reviewed guidance before source replacement can mark reviews stale");
assert(app.includes("rebuildDetails.reduce") && app.includes("pruningSummary") && fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("rebuildDetails"), "saved-source rebuilds should disclose bounded pruning outcomes instead of reporting only a source count");
assert(fs.readFileSync(new URL("../rebuild-adapter.js", import.meta.url), "utf8").includes("abortableExtract") && fs.readFileSync(new URL("../tests/rebuild-smoke.mjs", import.meta.url), "utf8").includes("hangingExtraction"), "saved-source rebuild cancellation should return promptly when an injected extractor ignores its abort signal");
assert(app.includes("createRemoteExtractor"), "optional remote extraction must remain wired");
assert(app.includes("runRemoteExtraction"), "remote extraction cancellation must remain wired");
assert(app.includes("const absoluteExtractorEndpoint = (endpoint) => new URL(validateExtractorEndpoint(endpoint), location.href).toString()") && app.includes("createRemoteExtractor({ endpoint: absoluteExtractorEndpoint(endpoint) })"), "documented relative same-origin extractor paths must be resolved to absolute URLs before entering the provider adapter");
assert(app.includes("const flushBrowserStorage") && app.includes('window.addEventListener("pagehide", () =>') && app.includes("revokeAllObjectUrls()") && app.includes('document.visibilityState === "hidden"'), "browser storage should flush pending durable writes and browser downloads should clean up when a tab is hidden or unloaded");
assert(app.includes("ingestInFlight") && app.includes("activeBuildController") && app.includes('setAttribute("aria-busy", "true")') && app.includes("documentFileInput.disabled = true") && app.includes("files.slice(fileIndex)") && app.includes("retryableBatchFiles") && app.includes('["NETWORK_ERROR", "REMOTE_ERROR", "TIMEOUT"].includes(error?.code)'), "graph builds should lock competing UI actions and preserve canceled or transiently failed batch files");
assert(app.includes("transient failure${retryableBatchFiles.length === 1 ? \"\" : \"s\"} kept for retry"), "batch status should disclose transient files retained for retry");
assert(app.includes("endpointUrl.origin !== location.origin"), "browser extraction must respect same-origin CSP");
assert(app.includes("endpointUrl.username") && app.includes("endpointUrl.search") && app.includes("endpointUrl.hash") && fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("embedded credentials"), "extractor endpoints should reject credentials, query strings, and fragments");
assert(app.indexOf("const endpoint = extractorEndpointInput.value.trim();") < app.indexOf("if (pendingFiles.length)"), "endpoint validation should not block feedback imports");
assert(app.includes("updatePrivacyNote"), "remote extraction data boundary must be disclosed dynamically");
assert(app.includes("Documents, optional source URI, and bounded reviewed feedback"), "remote privacy copy must include optional source URI metadata");
assert(html.includes("No document extraction call while the extractor endpoint is blank"), "privacy copy should distinguish static app requests from document extraction");
assert(app.includes("Saved with reduced undo history"), "degraded storage writes must be visible");
assert(app.includes("That feedback could not be saved"), "feedback persistence failures must be surfaced");
assert(app.includes("MAX_GRAPH_REVISIONS") && app.includes("graph.revisions = graph.revisions.slice(0, MAX_GRAPH_REVISIONS)"), "feedback revisions must remain bounded");
assert(app.includes("expectedVersion") && app.includes("changed in another tab"), "batch writes should detect cross-tab graph conflicts");
assert(app.includes("backup was not restored") && app.includes("import was not written"), "async imports should detect cross-tab graph conflicts");
assert(app.includes("manual change was not written") && app.includes("That feedback was not written") && app.includes("extraction was running"), "all graph mutations should surface cross-tab conflicts");
assert(app.includes("graphStore.clear(graphStoreOptions") && app.includes("graphStore.undo(graphStoreOptions") && app.includes("forget-local-data") && app.includes("browserStorage.storage.clear()") && app.includes("durable deletion could not be verified"), "undo, graph clear, and explicit full local-data purge should remain available with cross-tab conflict protection and honest durability messaging");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes('lastWriteMode = "none"'), "no-op undo should not inherit stale write status");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes('lastWriteMode = "failed"'), "failed undo should report a failed write status");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("historyCapacity"), "history capacity should be sanitized before slicing");
assert(app.includes("browserStorage.subscribe") && app.includes("event.external") && app.includes("Graph updated in another tab") && app.includes("divergent graph with the same revision") && app.includes("renderedFingerprint") && !app.includes('window.addEventListener("storage"'), "the storage adapter should own external graph synchronization, stale-update repair, and same-revision conflict detection");
assert(app.includes("sourceTitles") && app.includes("node.evidence.map"), "graph search should include source and evidence text");
assert(app.includes("health.reviewedItems"), "the workbench should expose accumulated feedback decisions");
assert(app.includes("health.acceptedItems") && app.includes("health.rejectedItems"), "the workbench should expose accepted and rejected learning outcomes");
assert(app.includes("health.staleReviewCandidates"), "the workbench should expose stale review pressure");
assert(app.includes("health.staleLearningExamples"), "the workbench should expose stale reusable learning pressure");
assert(app.includes("health.freshSourceReviewCoverage"), "the workbench should expose fresh source coverage");
assert(app.includes("health.evidenceGroundingCoverage") && app.includes("health.unanchoredEvidenceRecords"), "the workbench should expose non-destructive evidence grounding diagnostics");
assert(app.includes('["conflictingNodeIds", "conflictingEdgeIds"]') && app.includes("Contradictory duplicate review records"), "Markdown and revision-diff projections should disclose contradictory duplicate review records");
assert(app.includes("learningExamples") && app.includes("reusable learning example"), "the workbench should expose reusable learning memory");
assert(app.includes("clearLearningMemory") && app.includes("clearStaleLearningMemory") && app.includes('data-learning-action="clear"') && app.includes('data-learning-action="clear-stale"') && app.includes("Forget reusable learning memory") && app.includes("Fresh learning memory will remain"), "the workbench should allow all or stale reusable learning memory to be cleared safely");
assert(app.includes("rememberLearningItem") && app.includes("graph.nodes[graph.nodes.length - 1]") && app.includes("graph.edges[graph.edges.length - 1]"), "manual and in-app edits should feed reusable learning memory");
assert(app.includes("createGraphSearchIndex") && app.includes("documentTitleMap"), "graph search should reuse a per-render document title index");
assert(app.includes("nodeTextCache") && app.includes("edgeTextCache"), "graph search should cache per-render searchable text");
const storageAdapter = fs.readFileSync(new URL("../storage-adapter.js", import.meta.url), "utf8");
assert(app.includes("getBrowserStorage") && storageAdapter.includes("createMemoryStorage"), "the workbench should survive unavailable browser storage");
assert(app.includes("Browser storage is unavailable"), "storage fallback limitations should be disclosed");
assert(app.includes("await browserStorage.ready") && app.includes("hasDurableStorage") && app.includes("pagehide") && storageAdapter.includes("flush()"), "the workbench should hydrate and flush durable storage across page lifecycle boundaries");
assert(storageAdapter.includes("instanceId") && storageAdapter.includes("randomUUID") && storageAdapter.includes("getRandomValues") && storageAdapter.includes("pendingSequence"), "durable write markers should distinguish storage instances before acknowledging commits");
assert(storageAdapter.includes("STORAGE_MESSAGE_FORMAT") && storageAdapter.includes("event.data?.format !== STORAGE_MESSAGE_FORMAT") && storageAdapter.includes("isStaleExternalGraphValue") && storageAdapter.includes("key === PENDING_WRITES_KEY") && storageAdapter.includes("channel?.addEventListener(\"message\""), "durable BroadcastChannel synchronization should use a versioned envelope, reject stale graph values, and isolate the internal pending-write marker");
assert(storageAdapter.includes("MAX_BROADCAST_VALUE_CHARS") && storageAdapter.includes("MAX_BROADCAST_VALUE_BYTES") && storageAdapter.includes("isValidStorageValue") && storageAdapter.includes("utf8Encoder.encode(value).byteLength"), "cross-tab storage synchronization should accept only bounded UTF-8 string payloads");
assert(storageAdapter.includes("MAX_STORAGE_ENTRIES") && storageAdapter.includes("MAX_STORAGE_TOTAL_BYTES") && storageAdapter.includes("STORAGE_LIMIT"), "IndexedDB hydration should enforce aggregate entry and byte ceilings before materializing application state");
assert(storageAdapter.includes("fitsAggregateStorageLimit") && storageAdapter.includes("Storage namespace exceeds the aggregate safety limit"), "durable writes and cross-tab updates should enforce the same aggregate storage ceiling as hydration");
assert(storageAdapter.includes("fitsLocalStorageAggregate") && storageAdapter.includes("storageFailure"), "localStorage fallback writes and external updates should enforce aggregate storage limits and expose degraded state");
assert(storageAdapter.includes('type: "status"') && storageAdapter.includes("notifyStatus"), "localStorage fallback degradation should notify live status subscribers");
assert(storageAdapter.includes("MAX_STORAGE_KEY_CHARS") && storageAdapter.includes("isValidStorageKey") && storageAdapter.match(/isValidStorageKey/g)?.length >= 4, "all storage synchronization and hydration paths should bound namespaced key lengths");
assert(storageAdapter.includes("keys.forEach((key) => storage.removeItem(key))") && !storageAdapter.includes("clear: () => localStorage.clear"), "fallback storage clear should preserve unrelated origin-localStorage entries");
assert(storageAdapter.includes("readNamespacedKeys") && storageAdapter.includes("clearDatabaseValues") && storageAdapter.includes("fallbackWrite(PENDING_WRITES_KEY, null)") && storageAdapter.includes("CLEAR_PENDING_KEY") && storageAdapter.includes("event.key === CLEAR_PENDING_KEY") && storageAdapter.includes("hasPendingClear") && storageAdapter.includes("markPendingClear") && storageAdapter.includes("clearRequestedBeforeReady") && storageAdapter.includes("isWriteAfterClear") && storageAdapter.includes("pendingTokenParts"), "durable storage clear should purge the full namespaced local and IndexedDB state, retain a generation-aware reload- and cross-tab-safe retry intent, preserve post-clear writes, and handle malformed remnants and pre-hydration clears");
assert(feedbackSchema.properties.examples.items.properties.aliases.uniqueItems === true
  && feedbackSchema.properties.examples.items.properties.sources.uniqueItems === true
  && diffSchema.$defs.item.properties.aliases.uniqueItems === true
  && diffSchema.$defs.item.properties.sources.uniqueItems === true
  && jsonLdSchema.$defs.sourceRef.uniqueItems === true
  && jsonLdSchema.$defs.member.oneOf.some((member) => JSON.stringify(member).includes('"lfn:aliases"') && JSON.stringify(member).includes('"uniqueItems":true')), "public feedback, diff, and JSON-LD schemas should enforce canonical unique aliases");
assert(app.includes("MAX_PROGRESS_JSON_CHARS") && app.includes("notes.some((note) => note.id === item)") && app.includes("item < path.length"), "learning progress should validate persisted IDs and indices");
assert(app.includes("reconcileBackupCheckpoint") && app.includes("backupCheckpointSyncTimer") && app.includes("setTimeout(reconcileBackupCheckpoint, 1000)"), "backup checkpoint reminders should reconcile durable cross-tab state even when a lifecycle event is missed");
assert(storageAdapter.includes("indexedDB") && storageAdapter.includes("BroadcastChannel") && storageAdapter.includes("dispose()") && storageAdapter.includes("channel?.close") && storageAdapter.includes("database.close") && storageAdapter.includes("assertStorageKey") && storageAdapter.includes("STORAGE_PREFIX") && storageAdapter.includes("Storage keys must use"), "browser storage should support durable hydration, cross-tab synchronization, lifecycle teardown, IndexedDB connection closure, and bounded namespaced write keys");
assert(app.includes("storageDurabilityFailure") && app.includes("Durable storage unavailable"), "durability failures should be disclosed in the workbench");
assert(storageAdapter.includes("notifyStatus") && storageAdapter.includes("storageFailure"), "storage failures should be observable rather than silently downgraded");
assert(storageAdapter.includes("IDB_OPERATION_TIMEOUT_MS") && storageAdapter.includes("Transaction timed out") && storageAdapter.includes("transaction.abort"), "IndexedDB write transactions should be bounded and fall back when they stall");
assert(storageAdapter.includes("hydrationAbandoned") && storageAdapter.includes("closeHydrationDatabase"), "late IndexedDB hydration should close abandoned connections");
assert(fs.existsSync(new URL("../storage-adapter.js", import.meta.url)), "the storage adapter should remain a separately testable module");
assert(fs.existsSync(new URL("../PRODUCTION_STATUS.md", import.meta.url)) && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("PRODUCTION_STATUS.md") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("Settings → Pages") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("PUBLIC_ORIGIN") && (productionStatus.includes("Blocked by external configuration") || productionStatus.includes("Deployed; workflow verifier fix pending rerun")) && productionStatus.includes("PAGES_EXPECTED_REVISION"), "the repository should publish an explicit production boundary, Pages activation procedure, and deployment-origin configuration");
assert(productionStatus.includes("Model-provider privacy boundary") && productionStatus.includes("disable automatic redirects") && productionStatus.includes("intermediary caching"), "the production status matrix should expose the provider privacy boundary and its enforced redirect/cache protections");
assert(productionStatus.includes("exact normalized `PUBLIC_ORIGIN` deployment base"), "the production status matrix should disclose exact deployment-base binding for capacity evidence");
assert(productionStatus.includes("Supported Node compatibility") && productionStatus.includes("full `npm test` suite passed on Node 24"), "the production status should record both supported runtime lanes");
assert(productionStatus.includes("## Closure checklist") && productionStatus.includes("npm run production:check") && productionStatus.includes("do not call the public URL production"), "the production status should define the repository-close gate separately from public launch");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("PUBLIC_DOCUMENTATION_URL") && fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("OCI source and documentation labels"), "container documentation should explain fork-aware OCI metadata arguments");
assert(fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("public experiment sources") && fs.readFileSync(new URL("../ARCHITECTURE.md", import.meta.url), "utf8").includes("unpublished development-only files") && !fs.readFileSync(new URL("../README.md", import.meta.url), "utf8").includes("while development-only experiments and generated SBOM evidence remain outside the image"), "container documentation should distinguish published experiment sources from unpublished development files");
assert(llms.includes("CONTRIBUTING.md#correct-a-graph-representation") && llms.includes("without sharing private source text"), "the machine-readable project map should expose the privacy-safe graph-correction workflow");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("const valid = stored.filter(isGraphRecord)") && fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("trimHistory(valid)"), "backup history should filter malformed snapshots before applying its capacity");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("JSON.stringify(stored) !== JSON.stringify(normalized)"), "repairable persisted graphs should be preserved before normalization");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("HISTORY_RECOVERY_KEY") && fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("captureRaw(historyRecoveryKey"), "malformed persisted undo history should be preserved before filtering");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("expectedFingerprint") && app.includes("expectedFingerprint"), "graph mutations should use content fingerprints in addition to versions for optimistic concurrency");
assert(fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("serializeHistory(history)") && fs.readFileSync(new URL("../graph-store.js", import.meta.url), "utf8").includes("serializeHistory(trimHistory(history))"), "undo and clear should preflight persisted history size");
assert(app.includes("MAX_GRAPH_NODES") && app.includes("MAX_GRAPH_EDGES") && app.includes("MAX_GRAPH_DOCUMENTS"), "manual and extracted graph growth should respect explicit graph caps");
assert(app.includes('existing.status = "accepted";\n    existing.feedback += 1;\n    existing.confidence = .99;'), "reconfirming an existing manual relation should preserve feedback-count learning history");
assert(graphCore.includes("limited: \"feedback-examples\"") && app.includes("MAX_FEEDBACK_FINGERPRINT_EXAMPLES.toLocaleString()"), "feedback imports should reject oversized example collections instead of silently slicing human decisions");
assert(app.includes("failureSummary") && app.includes("failures.slice(0, 3)"), "batch ingestion should surface bounded actionable failure details");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("conflictingSource"), "source-ID collisions should be repaired before graph merge");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("sameContent"), "import normalization should preserve conflicting source records");
assert(app.includes("const nodeById = new Map(graph.nodes.map") && app.includes("relatedByNode"), "large graph rendering and projection should reuse indexed lookups");
assert(fs.readFileSync(new URL("../graph-core.js", import.meta.url), "utf8").includes("knownNodeAliases"), "graph ingestion should index canonical labels and aliases");
assert(app.includes("function buildFeedbackDataset(graph)") && app.includes("const nodeById = new Map(graph.nodes.map((node) => [node.id, node]))"), "feedback export should index relation endpoint labels");
assert(app.includes("vaultExportInFlight") && app.includes("async function withVaultExport") && app.includes('button.setAttribute("aria-busy", "true")'), "expensive Obsidian vault exports should be single-flight and expose their busy state");
assert(app.includes('filter((node) => node.status === "accepted" || node.status === "rejected")'), "feedback exports should exclude neutral inferred items");
assert(app.includes("document.quality") && app.includes("document.lastReviewedAt"), "graph search should include source provenance metadata");
assert(app.includes("getVisibleNodes(graph, searchIndex)"), "graph layout and lists should share one search index");
assert(bugTemplate.includes("name: Bug report") && bugTemplate.includes("id: reproduce"), "bug intake should request reproduction steps");
assert(featureTemplate.includes("name: Feature request") && featureTemplate.includes("id: proposal"), "feature intake should request a concrete proposal");
assert(learningNoteTemplate.includes("name: Learning note") && learningNoteTemplate.includes("short_version") && learningNoteTemplate.includes("failure_modes") && learningNoteTemplate.includes("try_it"), "learning-note intake should preserve the executable curriculum contract");
assert(security.includes("EXTRACTOR_RATE_LIMIT") && security.includes("trusted reverse") && security.includes("proxy"), "server deployment security guidance should remain present");
assert(readme.includes("--read-only --tmpfs /tmp") && security.includes("--read-only --tmpfs /tmp"), "deployment guidance should preserve the read-only container recommendation");
assert(security.includes("extractor endpoint is configured") && security.includes("optional source URI") && security.includes("bounded reviewed feedback") && security.includes("/metrics"), "security guidance should describe remote data and metrics boundaries accurately");
assert(security.includes("redacted Obsidian vault"), "security guidance should describe the redacted vault privacy boundary");
assert(dockerignore.split(/\r?\n/).includes("dist") && dockerignore.split(/\r?\n/).includes(".llm-field-notes-pages-build-*") && dockerignore.split(/\r?\n/).includes("*.previous-*"), "Docker builds should exclude generated Pages artifacts and rollback debris");
assert(gitignore.split(/\r?\n/).includes(".llm-field-notes-pages-build-*") && gitignore.split(/\r?\n/).includes("*.previous-*"), "generated Pages staging and rollback debris should remain outside version-control status");
assert(dockerignore.split(/\r?\n/).includes("experiments/*.mjs") && dockerignore.split(/\r?\n/).includes("scripts/*.mjs") && dockerignore.split(/\r?\n/).includes("scripts/build-pages.mjs") && dockerignore.split(/\r?\n/).includes("scripts/check-release.mjs") && dockerignore.split(/\r?\n/).includes("scripts/check-artifacts.mjs") && dockerignore.split(/\r?\n/).includes("scripts/check-contracts.mjs") && dockerignore.split(/\r?\n/).includes("scripts/verify-pages.mjs") && dockerignore.split(/\r?\n/).includes("!scripts/public-assets.mjs") && dockerignore.split(/\r?\n/).includes("!scripts/note-page.mjs") && dockerignore.split(/\r?\n/).includes("!scripts/public-repository.mjs") && dockerignore.split(/\r?\n/).includes("!scripts/sample-graph-page.mjs") && dockerignore.split(/\r?\n/).includes("!scripts/verify-share.mjs") && dockerignore.split(/\r?\n/).includes("!experiments/tiny-training.mjs"), "Docker builds should exclude release-verification modules and generated evidence while retaining every public runtime asset and script contract");
assert(graphStore.includes("MAX_HISTORY_CAPACITY = MAX_GRAPH_REVISIONS") && graphStore.includes("MAX_PERSISTED_JSON_BYTES") && graphStore.includes("MAX_RECOVERY_JSON_CHARS") && graphStore.includes("Math.min(MAX_HISTORY_CAPACITY") && graphStore.includes("Math.min(MAX_PERSISTED_JSON_CHARS") && graphStore.includes("recoveryJsonLimit") && graphStore.includes("new TextEncoder().encode(value).byteLength"), "graph-store configuration must retain hard persistence, recovery, character, byte, and history upper bounds even when callers provide oversized limits");
assert(graphStore.includes("readRawSnapshot") && graphStore.includes("if (!snapshot)"), "graph mutations should fail closed before changing state when their raw storage snapshot cannot be read");
assert(graphStore.includes("historyNormalizationChanged") && graphStore.includes("captureRaw(historyRecoveryKey, rawHistory)"), "backup restores should preserve raw history when nested normalization drops or truncates entries");
assert(graphStore.includes("const rawGraph = (() =>") && graphStore.includes("captureRaw(recoveryKey, rawGraph)"), "programmatic incomplete graph restores should preserve their raw graph payload for recovery");
assert(graphStore.includes("Commit the primary graph before its undo history") && graphStore.includes("A clear is destructive: record the undo snapshot before removing"), "graph mutations should order persistence operations for crash consistency and destructive recovery");
assert(graphStore.includes("writeAndVerify") && fs.readFileSync(new URL("../tests/smoke.mjs", import.meta.url), "utf8").includes("silentStorage"), "graph mutations should verify storage read-back so silent persistence failures cannot be reported as successful saves");
const workflow = fs.readFileSync(new URL("../.github/workflows/verify.yml", import.meta.url), "utf8");
const pagesWorkflow = fs.readFileSync(new URL("../.github/workflows/pages.yml", import.meta.url), "utf8");
assert(workflow.includes("uses: actions/checkout@") && pagesWorkflow.includes("uses: actions/deploy-pages@"), "deployment workflows should remain explicit and action-backed");
assert(workflow.includes("cancel-in-progress: true") && workflow.includes("timeout-minutes: 20"), "verification CI should cancel superseded runs and bound total execution time");
assert(workflow.includes("npm run smoke:server") && serverSmoke.includes("spawn(process.execPath, [\"server.mjs\"]"), "verification CI should run the reusable standalone Node process smoke so graceful shutdown is observable");
assert(serverSmoke.includes("server-ready") && serverSmoke.includes("server-draining") && serverSmoke.includes("server-stopped"), "the reusable standalone server smoke should assert the lifecycle log contract");
assert(workflow.includes("persist-credentials: false") && pagesBuildWorkflow.includes("persist-credentials: false") && pagesMonitorWorkflow.includes("persist-credentials: false"), "CI checkouts should not persist GitHub tokens into the workspace");
assert(pagesWorkflow.includes("timeout-minutes: 20"), "Pages deployment should bound total execution time");
assert(pagesMonitorWorkflow.includes('PAGES_SMOKE_ATTEMPTS: "30"') && pagesMonitorWorkflow.includes('PAGES_SMOKE_RETRY_DELAY_MS: "2000"') && pagesDeploymentSmoke.includes("parsePagesSmokeConfig") && pagesDeploymentSmoke.includes("must be a positive integer"), "scheduled Pages monitoring should allow bounded CDN propagation retries while rejecting malformed retry settings");
assert(browserMonitorWorkflow.includes("for attempt in 1 2 3") && browserMonitorWorkflow.includes("sleep 20") && browserMonitorWorkflow.includes("npm run browser:smoke"), "scheduled browser monitoring should retry bounded CDN propagation failures");
for (const workflowText of [workflow, pagesWorkflow, pagesMonitorWorkflow, releaseWorkflow]) {
  for (const match of workflowText.matchAll(/^\s*uses:\s*([^\s@]+)@([^\s#]+)/gm)) {
    assert(/^[0-9a-f]{40}$/i.test(match[2]), `GitHub Action reference should be pinned to a commit SHA: ${match[0]}`);
  }
}
assert(workflow.includes('node-version: ["22.x", "24.x"]') && !workflow.includes("18.x") && !workflow.includes("20.x"), "CI should verify only maintained Node runtime lanes");
assert(workflow.includes("Smoke test standalone server lifecycle") && workflow.includes("npm run smoke:server") && packageManifest.scripts["smoke:server"] === "node scripts/smoke-server.mjs", "CI should exercise and cleanly terminate the standard Node server process");
assert(workflow.includes("docker build --build-arg APP_VERSION=\"$version\" --build-arg VCS_REF=\"$GITHUB_SHA\" --build-arg PUBLIC_REPOSITORY_URL=\"$repository\" --build-arg PUBLIC_DOCUMENTATION_URL=\"$documentation\" --tag llm-field-notes:ci .") && workflow.includes("CONTAINER_EXPECTED_SOURCE: https://github.com/${{ github.repository }}") && workflow.includes("CONTAINER_EXPECTED_DOCUMENTATION: https://github.com/${{ github.repository }}/blob/main/RUNBOOK.md") && workflow.includes("docker image inspect") && workflow.includes("org.opencontainers.image.version") && workflow.includes("org.opencontainers.image.revision") && workflow.includes("org.opencontainers.image.source") && workflow.includes("org.opencontainers.image.documentation"), "CI should build the deployment container and verify package, commit, fork-aware source, and documentation OCI metadata");
assert(workflow.includes("Smoke test hardened container runtime") && workflow.includes("npm run smoke:container") && workflow.includes("CONTAINER_EXPECTED_VERSION") && workflow.includes("CONTAINER_EXPECTED_REVISION") && workflow.includes("CONTAINER_PORT: \"18000\""), "CI should invoke the reusable hardened container smoke with release identity and a deterministic test port");
assert(workflow.includes("echo \"version=$version\" >> \"$GITHUB_OUTPUT\"") && workflow.includes("id: package"), "CI should pass the independently resolved package version to the reusable container smoke");
assert(dockerfile.includes("HEALTHCHECK --interval=30s --timeout=10s --start-period=10s"), "Docker readiness should allow bounded static startup work to complete");
assert(workflow.includes("npm ci --ignore-scripts --no-audit --no-fund") && pagesWorkflow.includes("npm ci --ignore-scripts --no-audit --no-fund") && pagesWorkflow.includes("npm run production:check") && pagesWorkflow.includes("actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa") && pagesWorkflow.includes("path: dist") && pagesWorkflow.includes("actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e"), "CI should install from the committed lockfile before verification and Pages publication");
assert(workflow.includes("npm audit --audit-level=high") && fs.readFileSync(new URL("../scripts/production-check.mjs", import.meta.url), "utf8").includes('"audit", "--audit-level=high"') && !workflow.includes("--omit=dev") && !fs.readFileSync(new URL("../scripts/production-check.mjs", import.meta.url), "utf8").includes("--omit=dev") && releaseWorkflow.includes("npm run production:check"), "normal verification and tagged releases should audit every locked dependency before publishing or shipping");
const pagesBuilder = fs.readFileSync(new URL("../scripts/build-pages.mjs", import.meta.url), "utf8");
const pagesVerifier = fs.readFileSync(new URL("../scripts/verify-pages.mjs", import.meta.url), "utf8");
assert(server.includes("PUBLIC_SITEMAP_ASSETS") && server.includes("renderRuntimeReleaseMetadata") && pagesBuilder.includes("PUBLIC_SITEMAP_ASSETS"), "Node and Pages sitemaps should share the public discovery asset contract and release revision metadata");
assert(pagesBuilder.includes("BUILD_REVISION") && pagesBuilder.includes("revision: buildRevision") && pagesVerifier.includes("Pages release metadata revision does not match BUILD_REVISION") && pagesDeploymentSmoke.includes("expectedRevision"), "Pages artifacts should publish and verify bounded source revision metadata through local and deployed checks");
assert(server.includes("Extraction authentication is not configured.") && server.includes("Metrics authentication is not configured.") && server.includes("inspectReadiness"), "readiness should fail closed when required production authentication is missing");
assert(server.includes("readinessCheck") && server.includes("Promise.race") && server.includes("if (server.isDraining)") && server.includes('error: "SERVER_DRAINING"'), "readiness and extraction should re-check drain state after asynchronous request work");
assert(pagesBuilder.includes("PUBLIC_FILES") && pagesBuilder.includes("PUBLIC_ASSETS") && pagesBuilder.includes("buildStaticFeed") && pagesBuilder.includes("buildLearningNotePage") && pagesBuilder.includes("MAX_STATIC_ASSET_BYTES") && pagesBuilder.includes("MAX_PUBLIC_ASSET_BYTES") && pagesBuilder.includes("preflightPublicAssetBudget") && pagesBuilder.includes("source public assets exceed") && pagesBuilder.includes("overlaps a source asset") && pagesBuilder.includes("totalOutputBytes") && pagesBuilder.includes("renderedIndex") && pagesBuilder.includes("renderNotFoundPage") && pagesBuilder.includes("shellMarker") && pagesBuilder.includes("renderedCache") && pagesBuilder.includes("cacheRevision") && pagesBuilder.includes("finalOutputSet") && pagesBuilder.includes("expected published and generated files") && pagesBuilder.includes("xmlEscape") && pagesBuilder.includes("readBoundedUtf8") && pagesBuilder.includes("copyBoundedFile") && pagesBuilder.includes("sourceHandle") && pagesBuilder.includes("await handle.read") && pagesBuilder.includes("READ_ONLY_NOFOLLOW_FLAGS") && pagesBuilder.includes("O_NOFOLLOW") && !pagesBuilder.includes("cp(sourceRealPath") && pagesBuilder.includes("await rm(output") && pagesBuilder.includes(".nojekyll"), "Pages publication should use a clean, explicitly bounded static allowlist, symlink-resistant reads and copies, generated note pages, and feed");
assert(!pagesBuilder.includes('"server.mjs"') && !pagesBuilder.includes('"Dockerfile"') && !pagesBuilder.includes('"tests/"'), "Pages publication should not expose server, container, or test files");
assert(pagesBuilder.includes("deriveNoteSummary") && pagesBuilder.includes("withoutFrontmatter") && pagesBuilder.includes("paragraphs[0] || title"), "Pages publication should derive non-empty clean feed summaries from Markdown notes");
assert(pagesBuilder.includes("lexicalCompare") && !pagesBuilder.includes("localeCompare"), "Pages feed ordering should be locale-independent");
assert(app.includes("const lexicalCompare") && !app.includes("mergeTargets = graph.nodes.filter((candidate) => candidate.id !== node.id).sort((left, right) => left.label.localeCompare"), "browser merge-target ordering should be locale-independent");
assert(styles.includes("prefers-reduced-motion") && styles.includes(".ticker-track { animation: none; transform: none; }"), "the public workbench should honor users who request reduced motion");
assert(styles.includes(".ingest-actions { display: flex; align-items: center; flex-wrap: wrap;"), "ingest actions should wrap on narrow screens");
assert(app.includes('document.querySelector("#copy-redacted-markdown")') && app.includes("Redacted Markdown copied; source text, evidence, and URIs were removed."), "privacy-safe Markdown should be copyable directly for sharing");
assert(app.includes("function buildRedactedMarkdownProjection()") && app.includes("redactGraph(graphStore.read())") && app.includes('document.querySelector("#share-redacted-markdown")') && app.includes("navigator.canShare") && app.includes("knowledge-graph-redacted.md") && app.includes("File sharing is unavailable; redacted Markdown copied instead."), "privacy-safe Markdown should centralize redaction and support native file sharing with bounded clipboard/download fallbacks");
assert(html.includes('id="share-redacted-link"') && app.includes("buildShareUrl") && app.includes("source text, evidence, URIs, and local IDs were excluded") && fs.existsSync(new URL("../share.html", import.meta.url)) && fs.existsSync(new URL("../share.js", import.meta.url)), "privacy-safe graph sharing should offer a bounded recipient-openable link projection");
const shareHtml = fs.readFileSync(new URL("../share.html", import.meta.url), "utf8");
assert(shareHtml.includes('name="robots" content="noindex,nofollow"') && shareHtml.includes('property="og:image" content="social-card.png"') && shareHtml.includes('name="twitter:card" content="summary_large_image"') && shareHtml.includes('rel="canonical" href="./share.html"') && shareHtml.includes('id="graph-map"') && shareHtml.includes('id="download-share"') && shareHtml.includes('id="copy-correction-context"') && shareHtml.includes('id="map-limit"') && shareHtml.includes('template=graph_correction.yml') && !shareHtml.includes("source text, evidence quotes, or source URIs"), "shared graph pages should expose generic privacy-safe social metadata, a bounded visual map, a redacted download, a copyable correction context, and a correction path without embedding source-bearing content");
assert(fs.readFileSync(new URL("../share.js", import.meta.url), "utf8").includes("createElementNS") && fs.readFileSync(new URL("../share.js", import.meta.url), "utf8").includes("visualNodeLimit") && fs.readFileSync(new URL("../share.js", import.meta.url), "utf8").includes("Visual map capped") && fs.readFileSync(new URL("../share.js", import.meta.url), "utf8").includes("copyCorrectionContext") && fs.readFileSync(new URL("../share.js", import.meta.url), "utf8").includes("Privacy-safe context") && fs.readFileSync(new URL("../share.js", import.meta.url), "utf8").includes("shared-knowledge-graph-redacted.json"), "shared graph visualization and download should use bounded DOM-safe construction with explicit truncation, stable redacted export behavior, and a source-free correction handoff");
assert(security.includes("native graph-share action") && security.includes("projection; browsers without file sharing"), "security guidance should document the native share privacy boundary");
assert(app.includes("sourceSearchTextCache") && app.includes("sliceTextAtCodePointBoundary(document.text, MAX_SEARCH_TEXT_CHARS)"), "graph search should include bounded, code-point-safe source document text");
assert(app.includes("const workbenchSearchCache") && app.includes("workbenchSearchCache.fingerprint === graphFingerprint") && app.includes("sourceSearchTextCache.has(cacheKey)") && app.includes("nodeTextCache.has(node.id)") && app.includes("edgeTextCache.has(edge.id)"), "unchanged graph searches should reuse their bounded index across fresh graph reads by stable IDs");
assert(app.includes("isExternalStorageRemoval") && app.includes("Graph cleared in another tab; this view was refreshed."), "cross-tab graph clears should propagate instead of being repaired as stale updates");
assert(app.includes("HISTORY_KEY") && app.includes("if (event.key === HISTORY_KEY)") && app.includes("Undo history updated in another tab; this view was refreshed."), "cross-tab undo-history changes should refresh the visible revision timeline");
assert(app.includes("GRAPH_ITEM_HASH_PATTERN") && app.includes("buildGraphItemUrl") && app.includes("buildGraphItemShareData") && app.includes("async function shareGraphItem") && app.includes("data-copy-graph-item") && app.includes("data-share-graph-item") && app.includes("navigator.share") && app.includes("syncGraphItemFromLocation"), "graph item inspection should have stable privacy-safe local deep links, tested copy/share fallbacks, and location restoration");
assert(app.includes('id="source-list"') || fs.readFileSync(new URL("../index.html", import.meta.url), "utf8").includes('id="source-list"'), "the list view should expose every ingested source document for inspection");
assert(app.includes('<button type="button" class="source-row') && app.includes('aria-current="${selected ? "true" : "false"}"') && !app.includes('source-row${selectedGraphItem?.kind === "source" && selectedGraphItem.id === document.id ? " selected" : ""}" data-source-id="${escapeHtml(document.id)}" tabindex="0" role="button"') && app.includes("data-source-id"), "source browsing should use native bounded inspection buttons with selected-state semantics");
assert(app.includes('<span class="source-row-main"><strong>') && !app.includes('<button type="button" class="source-row${selected ? " selected" : ""}" data-source-id="${escapeHtml(document.id)}" aria-current="${selected ? "true" : "false"}" aria-label="Inspect source ${escapeHtml(document.title)}"><div>'), "source buttons should use valid phrasing-content wrappers");
assert(styles.includes(".graph-node:focus-visible circle"), "graph nodes should expose a visible keyboard focus ring");
assert(styles.includes(".source-row:focus-visible"), "source buttons should expose a visible keyboard focus ring");
assert(app.includes("matchingSourceDocuments") && app.includes("no matching concepts"), "source-only searches should disclose matching documents instead of appearing empty");
assert(app.includes("no concepts extracted yet"), "source-only workspaces should disclose that documents exist without extracted concepts");
assert(app.includes("reviewCandidates") && app.includes("data-review-queue-kind") && app.includes("priority"), "the active-learning queue should expose bounded candidates and their reasons");
assert(app.includes("reviewQueueTruncated") && app.includes("Review queue capped"), "the workbench should disclose when the active-learning queue is capped");
assert(app.includes("scheduleSearchRender") && app.includes("requestAnimationFrame"), "large-graph search should coalesce expensive renders per animation frame");
assert(app.includes("workbenchDerivedCache") && app.includes("derivedCacheHit"), "unchanged graph searches should reuse expensive diagnostics and projection work");
assert(app.includes("row.dataset.sourceId === sourceId") && app.includes("row.classList.toggle(\"selected\", selected)"), "source selection should stay visible in the source list");
assert(app.includes('const selected = selectedGraphItem?.kind === "source"') && app.includes('class="source-row${selected ? " selected" : ""}"') && app.includes('aria-current="${selected ? "true" : "false"}"'), "source selection should survive list rerenders");
assert(app.includes("Source replacement canceled; the current source was kept.") && app.includes("usingRemoteExtractor") && app.includes('error?.code === "CANCELED"'), "remote source replacement should map adapter cancellation to a safe preserved-source message");
assert(app.includes("Build canceled; no graph changes were written."), "single-document cancellation should disclose that the graph was not mutated");
assert(pagesBuilder.includes("\\u0000-\\u0008") && server.includes("\\u0000-\\u0008"), "XML crawler projections should remove invalid control characters before escaping");
assert(server.includes("MAX_CRAWLER_RESPONSE_BYTES") && pagesBuilder.includes("Generated feed exceeds the 2 MB crawler response limit"), "crawler projections should bound generated response sizes in Node and Pages deployments");
assert(server.includes("feedbackWithinCount") && server.includes("rawFeedback.map(compactFeedbackHint)") && server.includes("const feedbackSerialized = feedbackWithinCount"), "the extraction endpoint should enforce the feedback item bound before mapping or serializing attacker-controlled arrays");
assert(server.includes("new Set(value.aliases).size !== value.aliases.length"), "server feedback validation should reject duplicate aliases like the browser adapter");
assert(server.includes("notes/${encodeURIComponent(noteId)}.html"), "Node sitemaps should publish the same canonical note landing pages as Pages deployments");
assert(server.includes('route: "note-page"') && server.includes("const notFound = [\"ENOENT\", \"ELOOP\", \"ENOTDIR\", \"SYMLINK_ASSET\", \"ASSET_ESCAPE\"]"), "dynamic note routes should distinguish missing, escaped, and symlinked files from rendering failures");
assert(server.includes("Generated learning note page is oversized") && server.includes("for (const asset of learningMapAssets)"), "readiness should validate rendered learning-note pages, not only their Markdown sources");
assert(server.includes("READINESS_CACHE_TTL_MS") && server.includes("readinessCheck") && server.includes("inspectReadiness"), "readiness checks should coalesce concurrent work and use a short-lived bounded cache");
assert(server.includes('readinessCache.status === 503 ? { "retry-after": "5" }'), "failed readiness responses should advertise a bounded retry delay");
assert(server.includes('readinessCache.status === 503 ? { "retry-after": "5" } : {},\n          request.method === "HEAD"'), "readiness HEAD requests should suppress their response body while preserving retry headers");
assert(readme.includes("Transient provider failures return HTTP 502 with `Retry-After: 1`") && readme.includes("schema-incompatible provider output fails closed without a retry hint"), "deployment guidance should distinguish retryable provider failures from deterministic contract violations");
assert(server.includes("Number.isFinite(numericLimit)") && server.includes("MAX_RUNTIME_TEXT_CHARS") && server.includes("MAX_STATIC_ASSET_BYTES") && server.includes("finite file-size limit from 1") && server.includes("finite character limit from 1"), "runtime bounded readers should reject invalid and over-ceiling byte and character limits");
assert(fs.readFileSync(new URL("../experiments/bounded-file.mjs", import.meta.url), "utf8").includes("Number.isFinite(numericLimit)"), "CLI bounded readers should reject invalid limits");
assert(server.includes("RATE_LIMIT_SWEEP_INTERVAL_MS") && server.includes("nextRateLimitSweepAt"), "the in-process rate limiter should avoid scanning every client entry on every request");
assert(server.includes("decodeUtf8(await readBoundedFile(shellPath, MAX_STATIC_ASSET_BYTES))"), "readiness should validate published text asset encoding before reporting the server ready");
assert(server.includes("validateRuntimeReleaseMetadata") && server.includes("release.version !== APP_VERSION"), "readiness should reject stale or malformed runtime release metadata");
assert.equal(packageManifest.scripts["build:pages"], "node scripts/build-pages.mjs", "Pages publication should have a package command");
assert.equal(packageManifest.scripts["verify:service-health"], "node scripts/verify-service-health.mjs examples/sample-service-health.json readiness", "service-health verification should have a reproducible package command");
assert.equal(packageManifest.scripts["serve:pages"], "npm run build:pages && python3 -m http.server 8000 --directory dist", "Pages publication should have a local preview command");
assert(productionCheck.includes('["run", "artifacts:check"]')
  && productionCheck.includes('["run", "accessibility:check", "--", "dist"]')
  && productionCheck.includes('["run", "links:check", "--", "dist"]')
  && productionCheck.includes('["run", "performance:check", "--", "dist"]'), "the canonical production gate should verify public artifacts, generated HTML accessibility, and critical browser-shell performance");
assert(pagesVerifier.includes("checkPublicLinks") && pagesVerifier.includes("asset.endsWith(\".html\")") && pagesVerifier.includes("publishedTargets"), "Pages verification should fail closed on broken or unpublished links in every published HTML page");
const pagesSmoke = fs.readFileSync(new URL("../tests/pages-smoke.mjs", import.meta.url), "utf8");
assert(fs.existsSync(new URL("../tests/pages-smoke.mjs", import.meta.url)) && pagesSmoke.includes("dist-pages-smoke") && pagesSmoke.includes("process.once(\"exit\""), "Pages publication should have an isolated static preview smoke test with failure cleanup");
assert(fs.existsSync(new URL("../tests/deployment-smoke.mjs", import.meta.url)) && testRunner.includes('readdirSync("tests")'), "Pages publication should have a local fixture for its post-deploy smoke probe");
assert(dependabot.includes("package-ecosystem: github-actions") && dependabot.includes("package-ecosystem: npm") && dependabot.includes("package-ecosystem: docker") && dependabot.includes("interval: weekly"), "dependency updates should cover workflow actions, npm metadata, and Docker base images");
assert(app.includes("revision-items"), "revision timeline must remain wired");
assert(app.includes("BACKUP_FORMAT") && graphCore.includes("format: BACKUP_FORMAT") && app.includes("buildBackupEnvelope"), "full backup format must remain wired through the shared contract");
assert(app.includes("graphStore.restore(importedGraph, importedHistory, { expectedVersion, expectedFingerprint, expectedHistoryFingerprint, preserveCurrent: true })"), "full backup restore should reject concurrent undo-history changes before replacing the workspace");
assert(fs.readFileSync(new URL("../extractor-adapter.js", import.meta.url), "utf8").includes("schema: GRAPH_SCHEMA"), "remote extraction should use the shared graph schema contract");
assert(app.includes("replace the current workspace") && app.includes("Graph import canceled"), "destructive graph imports should require explicit confirmation");
console.log("site check ok");
